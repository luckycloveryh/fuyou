<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Linux on 小杨的博客</title><link>https://luckycloveryh.github.io/fuyou/tags/linux/</link><description>Recent content in Linux on 小杨的博客</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><copyright>小杨</copyright><lastBuildDate>Mon, 22 Jun 2026 09:00:00 +0800</lastBuildDate><atom:link href="https://luckycloveryh.github.io/fuyou/tags/linux/index.xml" rel="self" type="application/rss+xml"/><item><title>Linux 学习索引</title><link>https://luckycloveryh.github.io/fuyou/p/linux-19/</link><pubDate>Mon, 22 Jun 2026 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-19/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-19/1200/675" alt="Featured image of post Linux 学习索引" />&lt;h1 id="linux-学习索引">Linux 学习索引
&lt;/h1>&lt;p>本阶段覆盖 Linux 基础操作、软件管理、用户权限、存储、进程、服务与系统启动。&lt;/p>
&lt;h2 id="一基础与常用命令">一、基础与常用命令
&lt;/h2>&lt;ol>
&lt;li>Linux 基础阶段&lt;/li>
&lt;li>常用命令（一）&lt;/li>
&lt;li>常用命令（二）&lt;/li>
&lt;li>常用命令（三）&lt;/li>
&lt;li>常用命令（四）&lt;/li>
&lt;li>常用命令（五）&lt;/li>
&lt;li>Linux 基础补充&lt;/li>
&lt;li>Vim 编辑器&lt;/li>
&lt;/ol>
&lt;h2 id="二软件用户与权限">二、软件、用户与权限
&lt;/h2>&lt;ol start="9">
&lt;li>软件安装（一）&lt;/li>
&lt;li>软件安装（二）&lt;/li>
&lt;li>用户管理&lt;/li>
&lt;li>权限管理&lt;/li>
&lt;/ol>
&lt;h2 id="三存储与系统管理">三、存储与系统管理
&lt;/h2>&lt;ol start="13">
&lt;li>文件系统&lt;/li>
&lt;li>RAID 与 LVM&lt;/li>
&lt;li>进程管理&lt;/li>
&lt;li>计划任务与日志&lt;/li>
&lt;li>服务管理&lt;/li>
&lt;li>系统启动&lt;/li>
&lt;/ol>
&lt;h2 id="复习主线">复习主线
&lt;/h2>&lt;p>&lt;code>命令行 → 文件与文本 → 软件包 → 用户权限 → 磁盘存储 → 进程与服务 → 启动流程&lt;/code>&lt;/p>
&lt;p>返回：知识库首页&lt;/p></description></item><item><title>GRUB 引导修复与 root 密码找回</title><link>https://luckycloveryh.github.io/fuyou/p/linux-10/</link><pubDate>Fri, 28 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-10/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-10/1200/675" alt="Featured image of post GRUB 引导修复与 root 密码找回" />&lt;h1 id="视频主要内容总结">视频主要内容总结
&lt;/h1>&lt;p>视频围绕Linux系统中GRUB菜单（视频中称“刮菜单/广告菜单”，应为表述误差）的相关操作展开，涵盖加密、损坏修复及密码找回三大核心模块，同时涉及系统启动流程与安全增强工具（SELinux）的处理，具体内容如下：&lt;/p>
&lt;h2 id="一grub菜单加密">一、GRUB菜单加密
&lt;/h2>&lt;h3 id="1-加密必要性">1. 加密必要性
&lt;/h3>&lt;p>系统启动时的GRUB菜单默认无需密码即可编辑：在菜单界面按“e”进入编辑模式，在行尾输入“rd.debug”并按“Ctrl+X”，可直接以root身份免密登录，存在安全风险。加密后需输入指定用户名和密码才能编辑，限制本地非法操作。&lt;/p>
&lt;h3 id="2-加密原理与操作">2. 加密原理与操作
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>核心文件判断&lt;/strong>：系统通过&lt;code>/boot/grub2/user.cfg&lt;/code>文件判断是否需要密码——该文件存在则需密码，默认不存在（无需密码）。&lt;/li>
&lt;li>&lt;strong>版本差异&lt;/strong>：RHEL 6及以前版本仅需设置密码，RHEL 7及以后版本需设置用户名（默认root，可自定义）和密码，且该账号与系统用户管理无关，仅用于GRUB菜单验证。&lt;/li>
&lt;li>&lt;strong>实操步骤&lt;/strong>：执行&lt;code>grub2-set-password&lt;/code>命令设置密码，系统会自动生成&lt;code>/boot/grub2/user.cfg&lt;/code>文件；重启后编辑GRUB菜单需先输入用户名和密码。&lt;/li>
&lt;li>&lt;strong>取消加密&lt;/strong>：删除&lt;code>/boot/grub2/user.cfg&lt;/code>文件即可恢复无需密码的状态。&lt;/li>
&lt;/ul>
&lt;h2 id="二grub菜单损坏修复">二、GRUB菜单损坏修复
&lt;/h2>&lt;h3 id="1-模拟损坏">1. 模拟损坏
&lt;/h3>&lt;p>通过&lt;code>dd if=/dev/zero of=/dev/nvme0n1 bs=1 count=446&lt;/code>命令，精确覆盖硬盘前446字节（MBR分区表中的GRUB引导信息），模拟GRUB菜单损坏，系统启动时会因无法引导硬盘而自动尝试光盘引导。&lt;/p>
&lt;h3 id="2-修复流程">2. 修复流程
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>光盘引导与故障排除&lt;/strong>：进入光盘启动界面，选择“故障排除”选项，进入硬盘挂载环境（光盘将硬盘挂载至&lt;code>/mnt/sysroot&lt;/code>）。&lt;/li>
&lt;li>&lt;strong>切换根目录&lt;/strong>：执行&lt;code>chroot /mnt/sysroot&lt;/code>，切换至硬盘的系统根目录，此时操作对象为硬盘文件。&lt;/li>
&lt;li>&lt;strong>重装GRUB&lt;/strong>：执行&lt;code>grub2-install /dev/nvme0n1&lt;/code>重装GRUB引导程序，修复引导信息。&lt;/li>
&lt;li>&lt;strong>重启验证&lt;/strong>：退出&lt;code>chroot&lt;/code>环境后重启系统，GRUB菜单恢复正常，系统可正常启动。&lt;/li>
&lt;/ol>
&lt;h3 id="3-特殊场景grub文件误删修复">3. 特殊场景：GRUB文件误删修复
&lt;/h3>&lt;p>若误删&lt;code>/boot/grub2&lt;/code>目录下的核心文件（如&lt;code>i386-pc&lt;/code>相关文件），修复流程与上述一致，重装GRUB后需额外执行&lt;code>grub2-mkconfig -o /boot/grub2/grub.cfg&lt;/code>，重建GRUB配置文件，确保菜单正常加载。&lt;/p>
&lt;h2 id="三系统密码找回">三、系统密码找回
&lt;/h2>&lt;p>针对root密码遗忘场景，视频提供两种通用找回方法，适用于绝大多数Linux版本（RHEL 7及以后为主）。&lt;/p>
&lt;h3 id="方法一rdbreak模式">方法一：rd.break模式
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>编辑GRUB菜单&lt;/strong>：系统启动时按“e”进入编辑模式，找到内核启动行（以“linux16”或“linux”开头），在行尾添加&lt;code>rd.break&lt;/code>。&lt;/li>
&lt;li>&lt;strong>进入紧急模式&lt;/strong>：按“Ctrl+X”启动，系统进入紧急模式，此时根分区为只读状态。&lt;/li>
&lt;li>&lt;strong>重新挂载根分区&lt;/strong>：执行&lt;code>mount -o remount,rw /sysroot&lt;/code>，将根分区改为可写。&lt;/li>
&lt;li>&lt;strong>切换根目录与改密码&lt;/strong>：执行&lt;code>chroot /sysroot&lt;/code>，再执行&lt;code>passwd root&lt;/code>设置新密码（或通过&lt;code>echo &amp;quot;新密码&amp;quot; | passwd --stdin root&lt;/code>批量设置）。&lt;/li>
&lt;li>&lt;strong>处理SELinux&lt;/strong>：若SELinux开启，需执行&lt;code>touch /.autorelabel&lt;/code>（让系统重启时重新生成安全上下文），或修改&lt;code>/etc/selinux/config&lt;/code>将&lt;code>SELINUX=enforcing&lt;/code>改为&lt;code>SELINUX=disabled&lt;/code>，避免SELinux阻止系统启动。&lt;/li>
&lt;li>&lt;strong>重启登录&lt;/strong>：退出&lt;code>chroot&lt;/code>环境，执行&lt;code>reboot&lt;/code>，使用新密码登录系统。&lt;/li>
&lt;/ol>
&lt;h3 id="方法二initbinsh模式">方法二：init=/bin/sh模式
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>编辑GRUB菜单&lt;/strong>：同方法一，在内核启动行尾添加&lt;code>init=/bin/sh&lt;/code>。&lt;/li>
&lt;li>&lt;strong>进入shell环境&lt;/strong>：按“Ctrl+X”启动，直接进入shell环境（无需切换根目录）。&lt;/li>
&lt;li>&lt;strong>重新挂载根分区&lt;/strong>：执行&lt;code>mount -o remount,rw /&lt;/code>，将根分区改为可写。&lt;/li>
&lt;li>&lt;strong>修改密码&lt;/strong>：直接执行&lt;code>passwd root&lt;/code>设置新密码，无需切换根目录。&lt;/li>
&lt;li>&lt;strong>继续启动系统&lt;/strong>：执行&lt;code>exec /sbin/init&lt;/code>，系统继续加载服务，完成启动后可使用新密码登录。&lt;/li>
&lt;/ol>
&lt;h3 id="版本适配说明">版本适配说明
&lt;/h3>&lt;ul>
&lt;li>多数版本（如RHEL 7/8/9、CentOS 7/8）支持两种方法；&lt;/li>
&lt;li>特殊版本（如Rocket 9.0）若&lt;code>rd.break&lt;/code>失效，可使用&lt;code>init=/bin/sh&lt;/code>模式，两种方法可覆盖几乎所有密码找回场景。&lt;/li>
&lt;/ul>
&lt;h2 id="四补充知识点">四、补充知识点
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>SELinux作用与处理&lt;/strong>：SELinux是系统安全增强工具，默认开启时会监控系统文件修改（如密码文件），若修改后未处理安全上下文，会阻止系统启动，需通过&lt;code>/.autorelabel&lt;/code>或修改配置文件解决。&lt;/li>
&lt;li>&lt;strong>机房物理安全&lt;/strong>：GRUB加密可防止本地非法编辑，但无法阻止物理接触者通过优盘/光盘引导、拔插硬盘等方式绕过限制，因此强调机房物理安全的重要性。&lt;/li>
&lt;li>&lt;strong>命令与文件说明&lt;/strong>：核心命令（如&lt;code>grub2-set-password&lt;/code>、&lt;code>grub2-install&lt;/code>、&lt;code>chroot&lt;/code>）和关键文件（&lt;code>/boot/grub2/user.cfg&lt;/code>、&lt;code>/etc/selinux/config&lt;/code>、&lt;code>/etc/shadow&lt;/code>）的功能与路径，需结合实操记忆。&lt;/li>
&lt;/ol></description></item><item><title>Linux 日志管理与计划任务实践</title><link>https://luckycloveryh.github.io/fuyou/p/linux-09/</link><pubDate>Thu, 27 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-09/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-09/1200/675" alt="Featured image of post Linux 日志管理与计划任务实践" />&lt;h1 id="linux-系统管理核心知识日志管理与定时计划任务">Linux 系统管理核心知识：日志管理与定时计划任务
&lt;/h1>&lt;h2 id="第一部分日志管理log-management">第一部分：日志管理（Log Management）
&lt;/h2>&lt;p>日志是系统的“黑匣子”，记录系统运行细节，是问题排查、安全审计、性能监控和任务执行验证的关键依据，贯穿系统运维全流程。&lt;/p>
&lt;h3 id="一日志基础作用服务与关键文件">一、日志基础：作用、服务与关键文件
&lt;/h3>&lt;h4 id="一日志的核心作用">（一）日志的核心作用
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>任务执行验证&lt;/strong>：查看定时计划任务（如crontab）执行频率，例如验证任务是否按“每分钟一次”设定运行。&lt;/li>
&lt;li>&lt;strong>异常排查定位&lt;/strong>：服务启动失败（如sshd配置错误）或系统异常时，日志精确提示错误位置（如“/etc/ssh/sshd_config第112行配置错误”），Linux 7+版本提示更详细。&lt;/li>
&lt;li>&lt;strong>非工作时段追溯&lt;/strong>：服务器24小时运行，日志记录工作人员离线期间的报错、警告，补充远程连接时本地显示器无法呈现的故障细节。&lt;/li>
&lt;li>&lt;strong>安全审计与性能监控&lt;/strong>：追踪用户登录、文件访问、系统调用（如&lt;code>secure&lt;/code>日志记录暴力破解IP），长期分析日志识别系统瓶颈与资源使用趋势。&lt;/li>
&lt;/ol>
&lt;h4 id="二linux-核心日志服务">（二）Linux 核心日志服务
&lt;/h4>&lt;p>Linux 7/8/9版本中，日志由三类服务协同处理，功能与存储特点差异显著：&lt;/p>
&lt;ul>
&lt;li>&lt;strong>1. &lt;code>systemd-journald&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>启动时机：随系统启动&lt;/li>
&lt;li>存储方式：默认内存（&lt;code>/run/log/journal/&lt;/code>）&lt;/li>
&lt;li>核心特点：收集内核和早期系统日志，重启后日志丢失；可通过配置实现持久化&lt;/li>
&lt;li>适用场景：查看系统启动初期临时日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>2. &lt;code>syslog&lt;/code>（视频口误提及）&lt;/strong>
&lt;ul>
&lt;li>启动时机：随内核启动&lt;/li>
&lt;li>存储方式：默认内存&lt;/li>
&lt;li>核心特点：记录系统启动早期日志，临时性存储，重启后旧日志丢失；可修改配置为持久化存储&lt;/li>
&lt;li>适用场景：辅助查看系统启动初期日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>3. &lt;code>rsyslog&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>启动时机：系统运行中持续工作&lt;/li>
&lt;li>存储方式：默认磁盘（&lt;code>/var/log/&lt;/code>）&lt;/li>
&lt;li>核心特点：接收&lt;code>journald&lt;/code>及其他应用的日志，支持永久存储与自定义存储规则，功能更强大&lt;/li>
&lt;li>适用场景：长期保存、查询各类系统与服务日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h4 id="三varlog-目录下的关键日志文件">（三）&lt;code>/var/log&lt;/code> 目录下的关键日志文件
&lt;/h4>&lt;ul>
&lt;li>&lt;strong>1. &lt;code>messages&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>核心用途：通用日志，记录除mail（邮件）、authpriv（安全隐私）、cron（定时任务）外的大部分系统消息&lt;/li>
&lt;li>查看/操作要点：排除重复类别日志，避免有效信息被淹没；可通过&lt;code>rsyslog&lt;/code>配置文件查看具体排除规则&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>2. &lt;code>secure&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>核心用途：安全相关日志，记录用户登录（成功/失败）、&lt;code>sudo&lt;/code>权限操作、&lt;code>useradd&lt;/code>用户创建等权限相关行为&lt;/li>
&lt;li>查看/操作要点：通过&lt;code>grep &amp;quot;failed password&amp;quot; /var/log/secure&lt;/code>定位暴力破解IP；可执行&lt;code>chattr +a /var/log/secure&lt;/code>添加“仅追加”属性防篡改&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>3. &lt;code>cron&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>核心用途：定时计划任务日志，记录crontab任务的执行情况&lt;/li>
&lt;li>查看/操作要点：日志格式为“时间+主机名+进程名+事件信息”；用&lt;code>vi&lt;/code>打开后按大写“G”可直接跳转至最新记录&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>4. &lt;code>wtmp&lt;/code>/&lt;code>btmp&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>核心用途：均为二进制日志，&lt;code>wtmp&lt;/code>记录用户登录历史，&lt;code>btmp&lt;/code>记录用户登录失败历史&lt;/li>
&lt;li>查看/操作要点：无法用&lt;code>vi&lt;/code>直接查看，需通过&lt;code>last&lt;/code>命令读取&lt;code>wtmp&lt;/code>、&lt;code>lastb&lt;/code>命令读取&lt;code>btmp&lt;/code>；二进制格式具有防篡改、节省系统资源的优势&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>5. &lt;code>dmesg&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>核心用途：内核消息缓冲区日志，包含硬件检测、内核模块加载等内核相关信息&lt;/li>
&lt;li>查看/操作要点：可通过&lt;code>dmesg&lt;/code>命令直接查看，也可查看&lt;code>/var/log/dmesg&lt;/code>文件获取完整记录&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h3 id="二日志查看与故障排查实操">二、日志查看与故障排查实操
&lt;/h3>&lt;h4 id="一基础查看与实时监控命令">（一）基础查看与实时监控命令
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>实时跟踪日志&lt;/strong>：执行&lt;code>tail -f /var/log/[文件名]&lt;/code>（如&lt;code>tail -f /var/log/secure&lt;/code>），可实时捕捉新产生的登录、用户创建等日志信息。&lt;/li>
&lt;li>&lt;strong>关键词过滤&lt;/strong>：执行&lt;code>grep [关键词] /var/log/[文件名]&lt;/code>（如&lt;code>grep &amp;quot;useradd&amp;quot; /var/log/secure&lt;/code>），快速定位与关键词相关的日志记录。&lt;/li>
&lt;li>&lt;strong>关键信息提取与统计&lt;/strong>：
&lt;ul>
&lt;li>截取IP地址：&lt;code>grep &amp;quot;Failed password&amp;quot; /var/log/secure | awk '{print $11}'&lt;/code>&lt;/li>
&lt;li>统计重复IP（判断暴力破解）：&lt;code>grep &amp;quot;Failed password&amp;quot; /var/log/secure | awk '{print $11}' | sort | uniq -c | sort -nr&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>时间范围查找&lt;/strong>：执行&lt;code>sed -n '/2025-11-27 11:00:00/,/2025-11-27 14:00:00/p' /var/log/messages&lt;/code>，精准筛选指定时间段内的日志。&lt;/li>
&lt;/ol>
&lt;h4 id="二journalctl-工具高级日志筛选与查看">（二）&lt;code>journalctl&lt;/code> 工具：高级日志筛选与查看
&lt;/h4>&lt;p>&lt;code>journalctl&lt;/code>是&lt;code>systemd&lt;/code>系统中查询&lt;code>journald&lt;/code>服务收集日志的核心工具，支持多维度组合筛选，功能灵活，具体用法如下：&lt;/p>
&lt;h5 id="1-按日志类型筛选">1. 按日志类型筛选
&lt;/h5>&lt;ul>
&lt;li>&lt;strong>&lt;code>journalctl --system&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：查看系统级日志，默认按时间由远及近显示&lt;/li>
&lt;li>示例：搭配&lt;code>-e&lt;/code>选项（&lt;code>journalctl --system -e&lt;/code>）可直接跳转至最新日志（最后一页），向上翻页可查看更早内容&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl --user&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：查看当前用户相关日志&lt;/li>
&lt;li>示例：默认情况下无用户日志记录，执行后结果为空&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl -k&lt;/code>/&lt;code>journalctl --dmesg&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：仅查看内核日志，与&lt;code>journalctl --system&lt;/code>结果部分重复（因&lt;code>--system&lt;/code>包含内核日志）&lt;/li>
&lt;li>示例：&lt;code>journalctl -k&lt;/code>可专注查看内核日志，排除系统其他日志干扰&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h5 id="2-日志排序与显示方式">2. 日志排序与显示方式
&lt;/h5>&lt;ul>
&lt;li>&lt;strong>&lt;code>journalctl -r&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：按时间由近及远倒序显示日志，最上方为最新日志&lt;/li>
&lt;li>示例：执行后需向下翻页查看更早内容，区别于&lt;code>-e&lt;/code>“直接查看最后一页”的逻辑&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl -f&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：实时跟踪新产生的日志，功能类似&lt;code>tail -f&lt;/code>&lt;/li>
&lt;li>示例：&lt;code>journalctl -fu sshd.service&lt;/code>可实时监控&lt;code>sshd&lt;/code>服务的新日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h5 id="3-按条件精准筛选">3. 按条件精准筛选
&lt;/h5>&lt;ul>
&lt;li>&lt;strong>&lt;code>journalctl -p [级别]&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：查看指定级别及更严重级别的日志（日志级别从轻到重：debug→info→notice→warning→err→crit→alert→emerg，遵循包含关系）&lt;/li>
&lt;li>示例：&lt;code>journalctl -p err&lt;/code>显示错误（err）及更高级别日志；&lt;code>journalctl -p warning&lt;/code>显示警告（warning）、错误（err）、紧急（emerg）等更高级别日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl --since/--until &amp;quot;时间&amp;quot;&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：按时间范围筛选日志，时间格式为“年月日 时分秒”（秒可省略），也支持“最近一小时”等描述&lt;/li>
&lt;li>示例：&lt;code>journalctl --since &amp;quot;2025-11-27 11:30&amp;quot; --until &amp;quot;2025-11-27 14:00&amp;quot;&lt;/code>，查看11月27日11点30分至14点之间的日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl _PID=[进程号]&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：查看指定进程产生的日志，需满足“进程持续存在且有日志产生”的条件&lt;/li>
&lt;li>示例：进程重启后进程号会变化，如&lt;code>sshd&lt;/code>原进程号为11930，重启后需用该旧进程号（&lt;code>journalctl _PID=11930&lt;/code>）查询旧日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl -u [服务名]&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：仅查看指定服务的日志，排除其他日志干扰&lt;/li>
&lt;li>示例：&lt;code>journalctl -xeu sshd.service&lt;/code>，其中&lt;code>-x&lt;/code>选项会显示日志解释性信息，可快速定位&lt;code>sshd&lt;/code>服务启动失败原因&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h5 id="4-高级操作">4. 高级操作
&lt;/h5>&lt;ul>
&lt;li>&lt;strong>&lt;code>journalctl --disk-usage&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：查看&lt;code>journal&lt;/code>日志占用的存储空间&lt;/li>
&lt;li>示例：虚拟机中&lt;code>journal&lt;/code>日志默认约5.9MB，需维持该基础大小，仅当日志占用空间过大时建议清理&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>&lt;code>journalctl --vacuum-size/--vacuum-time&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>功能描述：清理日志，限制日志留存大小或留存时间&lt;/li>
&lt;li>示例：&lt;code>journalctl --vacuum-size=100M&lt;/code>（保留100MB日志）；&lt;code>journalctl --vacuum-time=1week&lt;/code>（保留1周内日志）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h3 id="三日志管理核心操作">三、日志管理核心操作
&lt;/h3>&lt;h4 id="一日志轮替log-rotation防止文件过大">（一）日志轮替（Log Rotation）：防止文件过大
&lt;/h4>&lt;p>通过周期性将旧日志文件改名/移动、创建新日志文件的方式，防止单个日志文件无限增大，保证日志可查性，依赖系统默认工具&lt;code>logrotate&lt;/code>。&lt;/p>
&lt;ol>
&lt;li>
&lt;p>&lt;strong>轮替触发条件&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>时间周期：&lt;code>daily&lt;/code>（每天）、&lt;code>weekly&lt;/code>（每周）、&lt;code>monthly&lt;/code>（每月），适合日志生成量稳定的场景&lt;/li>
&lt;li>大小周期：&lt;code>size 200KB&lt;/code>/&lt;code>size 100M&lt;/code>等，当日志文件达到指定大小后触发轮替，适合日志生成量不稳定的场景&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>配置文件与优先级&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>主配置文件：&lt;code>/etc/logrotate.conf&lt;/code>，包含全局轮替规则（如默认每周轮替、保存4份旧日志）&lt;/li>
&lt;li>局部配置目录：&lt;code>/etc/logrotate.d/&lt;/code>，目录下文件为nginx、mysql等特定服务的独立轮替规则，优先级高于全局规则（如全局按周轮替，局部按月轮替则以局部为准）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>关键配置项&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>rotate 4&lt;/code>&lt;/strong>：功能描述为保留4份旧日志文件，超过数量则删除最旧的日志&lt;/li>
&lt;li>&lt;strong>&lt;code>compress&lt;/code>&lt;/strong>：功能描述为轮替后对旧日志文件进行压缩，节省磁盘空间（可配置“不压缩最近1份旧日志”，方便直接查看）&lt;/li>
&lt;li>&lt;strong>&lt;code>create 0640 root root&lt;/code>&lt;/strong>：功能描述为创建新的空日志文件，并指定文件权限为0640、所有者为root、所属组为root&lt;/li>
&lt;li>&lt;strong>&lt;code>delaycompress&lt;/code>&lt;/strong>：功能描述为延迟压缩，下一次轮替时再压缩当前轮替产生的旧日志文件&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>特殊处理与强制轮替&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>特殊处理：若日志文件设置了&lt;code>a&lt;/code>属性（不可修改/删除），需在轮替前执行&lt;code>chattr -a 文件名&lt;/code>取消属性，轮替后执行&lt;code>chattr +a 文件名&lt;/code>重新添加，确保轮替正常进行&lt;/li>
&lt;li>强制轮替：执行&lt;code>logrotate -vf 配置文件路径&lt;/code>（如&lt;code>logrotate -vf /etc/logrotate.d/nginx&lt;/code>），&lt;code>-v&lt;/code>输出详细轮替过程，&lt;code>-f&lt;/code>强制执行轮替，无需等待周期，便于测试轮替规则是否生效&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="二日志拆分与合并处理已过大日志">（二）日志拆分与合并：处理已过大日志
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>拆分操作&lt;/strong>：使用&lt;code>split&lt;/code>命令拆分文本类日志（二进制文件不适用），核心选项&lt;code>-b&lt;/code>指定拆分后单个文件的最大大小。
&lt;ul>
&lt;li>示例：&lt;code>split -b 500M -d -a 3 large.log log_part_&lt;/code>，其中&lt;code>-d&lt;/code>表示用数字作为文件后缀，&lt;code>-a 3&lt;/code>表示后缀长度为3位（000、001&amp;hellip;），最终生成&lt;code>log_part_000&lt;/code>（500MB）、&lt;code>log_part_001&lt;/code>（500MB）等文件，拆分后文件保存在当前路径，内容与原文件一致（可通过&lt;code>cat -n&lt;/code>对比行数验证）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>合并操作&lt;/strong>：通过&lt;code>cat&lt;/code>命令合并拆分后的文件，合并后文件大小、内容与原文件完全一致。
&lt;ul>
&lt;li>示例：&lt;code>cat log_part_* &amp;gt; large.log.restored&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="三journal-日志持久化配置">（三）&lt;code>journal&lt;/code> 日志持久化配置
&lt;/h4>&lt;p>默认&lt;code>journal&lt;/code>日志存储在内存中，系统重启后丢失，需配置持久化保存至磁盘，步骤如下：&lt;/p>
&lt;ol>
&lt;li>
&lt;p>创建目录：执行&lt;code>mkdir /var/log/journal&lt;/code>&lt;/p>
&lt;/li>
&lt;li>
&lt;p>重启系统：系统会自动配置目录权限（添加&lt;code>sgid&lt;/code>权限、修改所属组为&lt;code>systemd-journal&lt;/code>、设置ACL权限），无需手动操作&lt;/p>
&lt;/li>
&lt;li>
&lt;p>存储特点：持久化后日志保存至&lt;code>/var/log/journal/&lt;/code>目录下，生成&lt;code>system.journal&lt;/code>等二进制文件，无法像文本文档直接打开编辑&lt;/p>
&lt;/li>
&lt;li>
&lt;p>用户日志隔离：不同用户登录系统后，执行&lt;code>journalctl&lt;/code>查看的日志结果不同（普通用户日志更少）；系统会在&lt;code>/var/log/journal/&lt;/code>目录下按用户UID（如1001）单独创建日志文件，实现日志隔离保存&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>限制日志大小&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>编辑配置文件：&lt;code>journald.conf&lt;/code>（通常路径为&lt;code>/etc/systemd/journald.conf&lt;/code>）&lt;/li>
&lt;li>设置参数：&lt;code>SystemMaxUse=500M&lt;/code>（配置持久化日志最大占用空间为500MB）、&lt;code>MaxFileSize=50M&lt;/code>（配置单个日志文件最大大小为50MB）&lt;/li>
&lt;li>生效方式：修改后需重启&lt;code>systemd-journald&lt;/code>服务，执行&lt;code>systemctl restart systemd-journald&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="四日志空间管理与安全维护">（四）日志空间管理与安全维护
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>日志清空方式&lt;/strong>：
&lt;ul>
&lt;li>覆盖清空：执行&lt;code>&amp;gt; /var/log/[文件名]&lt;/code>，保留文件inode号，避免进程因inode变化无法继续写入日志&lt;/li>
&lt;li>删除重建：先执行&lt;code>rm /var/log/[文件名]&lt;/code>删除旧文件，再执行&lt;code>touch /var/log/[文件名]&lt;/code>创建新文件，该方式可能改变文件inode号，多进程运行场景下慎用&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>日志防篡改&lt;/strong>：执行&lt;code>chattr +a /var/log/[文件名]&lt;/code>为日志文件添加“仅追加”属性，添加后文件仅允许追加新内容，无法删除（&lt;code>rm -rf&lt;/code>无效）、无法修改已有内容；执行&lt;code>lsattr /var/log/[文件名]&lt;/code>可查看属性是否生效&lt;/li>
&lt;/ol>
&lt;h3 id="四日志集中管理多服务器场景">四、日志集中管理：多服务器场景
&lt;/h3>&lt;h4 id="一核心目的">（一）核心目的
&lt;/h4>&lt;p>在多台服务器的运维场景中，将所有服务器的日志集中收集到一台“日志收集服务器”，管理员无需逐台登录服务器，仅需登录收集服务器即可统一查看、分析所有日志，提升运维效率。&lt;/p>
&lt;h4 id="二实现原理rsyslog-客户端-服务器架构">（二）实现原理（&lt;code>rsyslog&lt;/code> 客户端-服务器架构）
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>服务器端配置&lt;/strong>：
&lt;ul>
&lt;li>修改配置文件：编辑&lt;code>/etc/rsyslog.conf&lt;/code>，取消TCP/UDP监听端口（默认514）的注释（如&lt;code>$ModLoad imtcp&lt;/code>、&lt;code>$InputTCPServerRun 514&lt;/code>）&lt;/li>
&lt;li>重启服务：执行&lt;code>systemctl restart rsyslog&lt;/code>&lt;/li>
&lt;li>开放端口：防火墙开放514端口（或临时关闭防火墙：&lt;code>systemctl stop firewalld&lt;/code>），确保能接收客户端发送的日志&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>客户端配置&lt;/strong>：
&lt;ul>
&lt;li>修改主机名：执行&lt;code>hostnamectl set-hostname A1&lt;/code>（避免多客户端主机名相同导致日志混淆）&lt;/li>
&lt;li>修改配置文件：编辑&lt;code>/etc/rsyslog.conf&lt;/code>，添加日志发送规则：&lt;code>*.* @@192.168.5.144:514&lt;/code>，其中&lt;code>@@&lt;/code>表示使用TCP协议（&lt;code>@&lt;/code>表示UDP协议），&lt;code>192.168.5.144&lt;/code>为日志收集服务器IP，514为监听端口&lt;/li>
&lt;li>重启服务与防火墙：执行&lt;code>systemctl restart rsyslog&lt;/code>，关闭客户端防火墙（或开放相关端口）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>效果验证&lt;/strong>：客户端执行操作（如&lt;code>useradd xe&lt;/code>创建用户），服务器端可在&lt;code>/var/log/messages&lt;/code>等日志文件中看到含客户端主机名（如A1）的操作记录，实现日志集中查看。&lt;/li>
&lt;/ol>
&lt;h3 id="五日志配置与服务管理">五、日志配置与服务管理
&lt;/h3>&lt;h4 id="一rsyslog-配置文件详解">（一）&lt;code>rsyslog&lt;/code> 配置文件详解
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>核心配置文件&lt;/strong>：&lt;code>/etc/rsyslog.conf&lt;/code>，配置规则格式为&lt;code>facility.priority action&lt;/code>（类别.级别 处理方式）：&lt;/p>
&lt;ul>
&lt;li>类别（facility）：指日志来源，如kern（内核）、cron（定时任务）、mail（邮件）、lpr（打印）、authpriv（安全隐私）等&lt;/li>
&lt;li>级别（priority）：指日志严重程度，从轻到重为debug→info→notice→warning→err→crit→alert→emerg&lt;/li>
&lt;li>处理方式（action）：指日志的存储/转发方式，如保存到文件（&lt;code>/var/log/xxx&lt;/code>）、发送到远程服务器（&lt;code>@@ip:port&lt;/code>）、广播给所有在线用户（&lt;code>*&lt;/code>）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>经典规则示例&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>*.info;mail.none;authpriv.none;cron.none /var/log/messages&lt;/code>：含义为“记录所有类别info级别及更高级别的日志，排除mail、authpriv、cron类别的日志，最终保存至&lt;code>/var/log/messages&lt;/code>文件”&lt;/li>
&lt;li>&lt;code>*.emerg *&lt;/code>：含义为“所有类别emerg级别（致命）的日志，广播通知所有在线用户”&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>配置生效与自定义日志&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>生效方式：修改&lt;code>rsyslog.conf&lt;/code>后，需重启&lt;code>rsyslog&lt;/code>服务（&lt;code>systemctl restart rsyslog&lt;/code>）或重启系统；发送&lt;code>HUP&lt;/code>信号（&lt;code>kill -HUP 进程号&lt;/code>）无法生效，因&lt;code>rsyslog&lt;/code>程序未定义该信号的响应动作&lt;/li>
&lt;li>自定义日志：在&lt;code>rsyslog.conf&lt;/code>中添加规则（如&lt;code>*.* /var/log/my.log&lt;/code>），定义“所有类别、所有级别日志保存至&lt;code>/var/log/my.log&lt;/code>文件”；重启&lt;code>rsyslog&lt;/code>服务后，执行&lt;code>useradd&lt;/code>等操作，可在该自定义文件中看到日志记录&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="二systemd-journald-服务操作注意事项">（二）&lt;code>systemd-journald&lt;/code> 服务操作注意事项
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>命令补全&lt;/strong>：管理&lt;code>systemd-journald&lt;/code>服务时，执行&lt;code>systemctl&lt;/code>相关命令（如&lt;code>systemctl status systemd-journald&lt;/code>），系统会默认补全&lt;code>.service&lt;/code>后缀，无需手动添加&lt;/li>
&lt;li>&lt;strong>服务名正确性&lt;/strong>：若命令执行失败（如提示“cnd journal.service 未找到”），需检查服务名是否正确，需包含“d”（正确服务名为&lt;code>systemd-journald&lt;/code>，而非&lt;code>system-journal&lt;/code>）&lt;/li>
&lt;li>&lt;strong>选项组合使用&lt;/strong>：&lt;code>journalctl&lt;/code>的多数选项可组合使用，例如&lt;code>journalctl -u sshd.service --since &amp;quot;2025-11-27 14:00&amp;quot; --until &amp;quot;2025-11-27 15:00&amp;quot;&lt;/code>，可筛选&lt;code>sshd&lt;/code>服务在11月27日14点至15点之间的日志&lt;/li>
&lt;/ol>
&lt;h2 id="第二部分定时计划任务cron-jobs">第二部分：定时计划任务（Cron Jobs）
&lt;/h2>&lt;p>定时任务是Linux自动化运维的核心，可实现命令或脚本在指定时间自动执行，核心工具分为&lt;code>at&lt;/code>（一次性任务）和&lt;code>crontab&lt;/code>（周期性任务）两类。&lt;/p>
&lt;h3 id="一核心工具对比">一、核心工具对比
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>1. &lt;code>at&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>执行特性：一次性执行&lt;/li>
&lt;li>核心用途：处理突发、单次的自动化任务（如10分钟后执行脚本）&lt;/li>
&lt;li>依赖守护进程：&lt;code>atd&lt;/code>&lt;/li>
&lt;li>适用场景：临时自动化需求，无需重复执行的任务&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>2. &lt;code>crontab&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>执行特性：周期性执行&lt;/li>
&lt;li>核心用途：处理重复、规律性的自动化任务（如每天凌晨执行备份脚本）&lt;/li>
&lt;li>依赖守护进程：&lt;code>crond&lt;/code>&lt;/li>
&lt;li>适用场景：日常运维自动化（如定时备份、临时文件清理）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h3 id="二at-命令一次性任务">二、&lt;code>at&lt;/code> 命令：一次性任务
&lt;/h3>&lt;h4 id="一安装与启动">（一）安装与启动
&lt;/h4>&lt;ol>
&lt;li>安装：RHEL/CentOS系统执行&lt;code>sudo dnf install at&lt;/code>，Debian/Ubuntu系统执行&lt;code>sudo apt install at&lt;/code>&lt;/li>
&lt;li>启动服务：执行&lt;code>sudo systemctl start atd&lt;/code>&lt;/li>
&lt;li>开机自启：执行&lt;code>sudo systemctl enable atd&lt;/code>，确保系统重启后服务自动运行&lt;/li>
&lt;/ol>
&lt;h4 id="二使用方法">（二）使用方法
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>交互模式&lt;/strong>：手动输入任务内容，按&lt;code>Ctrl+D&lt;/code>提交任务。
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">at 14:30 &lt;span class="c1"># 设定任务在14:30执行&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">at&amp;gt; /home/user/script.sh &lt;span class="c1"># 任务内容：执行指定路径下的脚本&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">at&amp;gt; &amp;lt;EOT&amp;gt; &lt;span class="c1"># 按Ctrl+D提交任务，完成任务创建&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>&lt;strong>非交互模式&lt;/strong>：通过管道传递任务内容，适合集成到脚本中自动化执行。
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;/home/user/script.sh&amp;#34;&lt;/span> &lt;span class="p">|&lt;/span> at now + &lt;span class="m">10&lt;/span> minutes &lt;span class="c1"># 10分钟后执行脚本&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>&lt;strong>常用时间格式&lt;/strong>：
&lt;ul>
&lt;li>绝对时间：&lt;code>at 10:00 2024-12-31&lt;/code>（2024年12月31日10点执行）&lt;/li>
&lt;li>相对时间：&lt;code>at now + 3 hours&lt;/code>（3小时后执行）、&lt;code>at 5pm tomorrow&lt;/code>（明天17点执行）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="三任务管理">（三）任务管理
&lt;/h4>&lt;ul>
&lt;li>&lt;strong>&lt;code>atq&lt;/code>/&lt;code>at -l&lt;/code>&lt;/strong>：功能描述为列出当前用户的所有待执行&lt;code>at&lt;/code>任务&lt;/li>
&lt;li>&lt;strong>&lt;code>at -c [任务号]&lt;/code>&lt;/strong>：功能描述为查看指定任务号对应的任务详细内容&lt;/li>
&lt;li>&lt;strong>&lt;code>atrm [任务号]&lt;/code>/&lt;code>at -r [任务号]&lt;/code>&lt;/strong>：功能描述为删除指定任务号对应的&lt;code>at&lt;/code>任务&lt;/li>
&lt;/ul>
&lt;h3 id="三crontab-命令周期性任务">三、&lt;code>crontab&lt;/code> 命令：周期性任务
&lt;/h3>&lt;h4 id="一crond-服务基础">（一）&lt;code>crond&lt;/code> 服务基础
&lt;/h4>&lt;ul>
&lt;li>服务状态查看：执行&lt;code>systemctl status crond&lt;/code>，默认系统开机自启&lt;/li>
&lt;li>服务重启：若服务异常，执行&lt;code>systemctl restart crond&lt;/code>恢复运行&lt;/li>
&lt;/ul>
&lt;h4 id="二crontab-文件格式">（二）&lt;code>crontab&lt;/code> 文件格式
&lt;/h4>&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="line">&lt;span class="cl">* * * * * /path/to/command
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">- - - - -
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | | |
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | | +----- 星期（0-6，星期日=0或7）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | +------- 月份（1-12）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | +--------- 日期（1-31）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| +----------- 小时（0-23）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">+------------- 分钟（0-59）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;ol>
&lt;li>&lt;strong>特殊字符含义&lt;/strong>：
&lt;ul>
&lt;li>&lt;code>*&lt;/code>：匹配所有值，如小时位用&lt;code>*&lt;/code>表示“每小时”&lt;/li>
&lt;li>&lt;code>,&lt;/code>：指定多个离散值，如&lt;code>1,3,5&lt;/code>表示“1、3、5”&lt;/li>
&lt;li>&lt;code>-&lt;/code>：指定连续范围，如&lt;code>1-5&lt;/code>表示“1到5”&lt;/li>
&lt;li>&lt;code>/&lt;/code>：指定步长，如&lt;code>*/10&lt;/code>在分钟位表示“每10分钟”&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>经典任务示例&lt;/strong>：
&lt;ul>
&lt;li>&lt;code>0 2 * * * /usr/bin/backup.sh&lt;/code>：每天凌晨2点整执行&lt;code>/usr/bin/&lt;/code>目录下的备份脚本&lt;/li>
&lt;li>&lt;code>*/15 * * * * /usr/bin/clean_temp.sh&lt;/code>：每15分钟执行&lt;code>/usr/bin/&lt;/code>目录下的临时文件清理脚本&lt;/li>
&lt;li>&lt;code>30 22 * * 1-5 /usr/bin/echo &amp;quot;Good night!&amp;quot;&lt;/code>：每周一至周五晚上22点30分执行“输出提示语”命令&lt;/li>
&lt;li>&lt;code>0 12 1 * * /usr/bin/send_report.sh&lt;/code>：每月1日中午12点执行&lt;code>/usr/bin/&lt;/code>目录下的报告发送脚本&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="三crontab-命令操作">（三）&lt;code>crontab&lt;/code> 命令操作
&lt;/h4>&lt;ul>
&lt;li>&lt;strong>&lt;code>crontab -e&lt;/code>&lt;/strong>：功能描述为编辑当前用户的定时任务列表，默认用vi编辑器打开，编辑后按&lt;code>Esc&lt;/code>+&lt;code>:wq&lt;/code>保存退出&lt;/li>
&lt;li>&lt;strong>&lt;code>crontab -l&lt;/code>&lt;/strong>：功能描述为列出当前用户的所有定时任务&lt;/li>
&lt;li>&lt;strong>&lt;code>crontab -r&lt;/code>&lt;/strong>：功能描述为删除当前用户的所有定时任务（&lt;strong>慎用，无确认步骤，删除后无法恢复&lt;/strong>）&lt;/li>
&lt;li>&lt;strong>&lt;code>sudo crontab -u [用户名] -e&lt;/code>&lt;/strong>：功能描述为编辑指定用户的定时任务，仅root用户有权限执行，示例：&lt;code>sudo crontab -u xe -e&lt;/code>（编辑用户xe的定时任务）&lt;/li>
&lt;/ul>
&lt;h4 id="四系统级-cron-任务">（四）系统级 &lt;code>cron&lt;/code> 任务
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>全局配置文件&lt;/strong>：&lt;code>/etc/crontab&lt;/code>，格式需额外指定执行用户，示例：&lt;code>0 2 * * * root /usr/bin/backup.sh&lt;/code>，含义为“root用户每天凌晨2点执行&lt;code>/usr/bin/backup.sh&lt;/code>脚本”&lt;/li>
&lt;li>&lt;strong>独立任务目录&lt;/strong>：&lt;code>/etc/cron.d/&lt;/code>，目录下存放系统级别的&lt;code>cron&lt;/code>任务文件（如nginx、mysql的定时任务配置）&lt;/li>
&lt;li>&lt;strong>按频率自动执行目录&lt;/strong>：系统会按固定频率自动执行以下目录中的可执行脚本：
&lt;ul>
&lt;li>&lt;code>/etc/cron.hourly/&lt;/code>：每小时执行一次&lt;/li>
&lt;li>&lt;code>/etc/cron.daily/&lt;/code>：每天执行一次&lt;/li>
&lt;li>&lt;code>/etc/cron.weekly/&lt;/code>：每周执行一次&lt;/li>
&lt;li>&lt;code>/etc/cron.monthly/&lt;/code>：每月执行一次&lt;/li>
&lt;li>注意：放入这些目录的脚本需添加可执行权限（&lt;code>chmod +x 脚本路径&lt;/code>），否则无法自动执行&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="四权限控制">四、权限控制
&lt;/h3>&lt;p>系统通过&lt;code>allow&lt;/code>（白名单）和&lt;code>deny&lt;/code>（黑名单）文件控制用户使用&lt;code>at&lt;/code>和&lt;code>crontab&lt;/code>的权限，优先级：白名单 &amp;gt; 黑名单，具体如下：&lt;/p>
&lt;ul>
&lt;li>&lt;strong>1. &lt;code>/etc/at.deny&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>类型：黑名单&lt;/li>
&lt;li>功能描述：文件中列出的用户禁止使用&lt;code>at&lt;/code>命令，默认为空文件，意味着所有用户均可使用&lt;code>at&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>2. &lt;code>/etc/cron.deny&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>类型：黑名单&lt;/li>
&lt;li>功能描述：文件中列出的用户禁止使用&lt;code>crontab&lt;/code>命令，默认为空文件，意味着所有用户均可使用&lt;code>crontab&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>3. &lt;code>/etc/at.allow&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>类型：白名单&lt;/li>
&lt;li>功能描述：文件默认不存在，需手动创建；仅文件中列出的用户可使用&lt;code>at&lt;/code>命令，优先级高于&lt;code>/etc/at.deny&lt;/code>（若该文件存在，&lt;code>/etc/at.deny&lt;/code>自动失效）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>4. &lt;code>/etc/cron.allow&lt;/code>&lt;/strong>
&lt;ul>
&lt;li>类型：白名单&lt;/li>
&lt;li>功能描述：文件默认不存在，需手动创建；仅文件中列出的用户可使用&lt;code>crontab&lt;/code>命令，优先级高于&lt;code>/etc/cron.deny&lt;/code>（若该文件存在，&lt;code>/etc/cron.deny&lt;/code>自动失效）&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;blockquote>
&lt;p>注意：仅在需严格控制用户权限的场景下创建白名单文件，避免过度限制影响运维效率。&lt;/p>
&lt;/blockquote>
&lt;h3 id="五注意事项与排错">五、注意事项与排错
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>环境变量问题&lt;/strong>：&lt;code>cron&lt;/code>的执行环境精简，&lt;code>PATH&lt;/code>等环境变量与用户登录时不同，执行命令或脚本时务必使用&lt;strong>绝对路径&lt;/strong>（如&lt;code>/usr/bin/ls&lt;/code>而非&lt;code>ls&lt;/code>）；也可在脚本开头定义&lt;code>PATH&lt;/code>变量，示例：&lt;code>export PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin&lt;/code>&lt;/li>
&lt;li>&lt;strong>脚本权限&lt;/strong>：确保&lt;code>crontab&lt;/code>中调用的脚本具有可执行权限，执行&lt;code>chmod +x /path/to/script.sh&lt;/code>添加权限&lt;/li>
&lt;li>&lt;strong>输出重定向&lt;/strong>：&lt;code>cron&lt;/code>任务的标准输出（stdout）和标准错误（stderr）默认通过邮件发送给执行用户，易造成邮件轰炸或关键信息丢失，建议将输出重定向到日志文件，示例：
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">* * * * * /path/to/script.sh &amp;gt; /var/log/my_cron.log 2&amp;gt;&lt;span class="p">&amp;amp;&lt;/span>&lt;span class="m">1&lt;/span> &lt;span class="c1"># 2&amp;gt;&amp;amp;1将标准错误合并到标准输出，统一写入日志文件&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>&lt;strong>日志排查&lt;/strong>：&lt;code>cron&lt;/code>任务的执行情况会记录在系统日志中，可通过以下命令排查任务是否执行：
&lt;ul>
&lt;li>RHEL/CentOS系统：&lt;code>grep CRON /var/log/messages&lt;/code>&lt;/li>
&lt;li>Debian/Ubuntu系统：&lt;code>grep CRON /var/log/syslog&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="第三部分学习进度与建议">第三部分：学习进度与建议
&lt;/h2>&lt;h3 id="一进度规划">一、进度规划
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>当日任务&lt;/strong>：完成日志管理核心操作（轮替、持久化、集中管理）、&lt;code>journalctl&lt;/code>工具详解、定时任务基础用法讲解&lt;/li>
&lt;li>&lt;strong>次日（周五）&lt;/strong>：讲解剩余日志配置细节与定时任务进阶案例，完成本阶段全部知识点&lt;/li>
&lt;li>&lt;strong>练习安排&lt;/strong>：周五晚上练习“服务管理（&lt;code>systemctl&lt;/code>）”“系统管理（日志清理、权限配置）”相关操作，周末启动全面复习&lt;/li>
&lt;/ol>
&lt;h3 id="二学习要求与提示">二、学习要求与提示
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>核心目标&lt;/strong>：无需死记&lt;code>logrotate&lt;/code>、&lt;code>journalctl&lt;/code>、&lt;code>crontab&lt;/code>的所有命令选项，重点理解“日志轮替/集中管理/持久化”“定时任务周期逻辑”的原理；能根据日志文件路径、配置文件内容识别轮替规则，通过&lt;code>journalctl&lt;/code>输出或&lt;code>cron&lt;/code>日志定位服务报错、任务执行异常原因&lt;/li>
&lt;li>&lt;strong>后续难度提示&lt;/strong>：当前为基础学习阶段，后续还有3-4个学习阶段，难度会递增（如“命令写文件”“网络服务配置”）；若当前基础不扎实，后续排查网络服务错误、理解复杂命令（如&lt;code>qpqqr&lt;/code>）时会更困难，需及时记忆关键细节（如日志级别、&lt;code>crontab&lt;/code>格式）&lt;/li>
&lt;li>&lt;strong>复习心态&lt;/strong>：复习过程中发现知识漏洞（如配置项含义遗忘）是正常现象，需通过“实操+复盘”强化记忆；时间不充裕，建议每日花30分钟整理当天学习的命令示例，避免问题堆积&lt;/li>
&lt;/ol></description></item><item><title>Linux 进程管理与系统资源监控</title><link>https://luckycloveryh.github.io/fuyou/p/linux-07/</link><pubDate>Wed, 26 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-07/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-07/1200/675" alt="Featured image of post Linux 进程管理与系统资源监控" />&lt;h1 id="视频内容综合总结">视频内容综合总结
&lt;/h1>&lt;p>该视频围绕&lt;strong>Linux系统进程管理&lt;/strong>展开，从核心概念、管理机制、命令工具、实操案例到系统资源监控形成完整知识体系，具体内容如下：&lt;/p>
&lt;h2 id="一进程核心概念与基础认知">一、进程核心概念与基础认知
&lt;/h2>&lt;h3 id="1-进程定义与组成">1. 进程定义与组成
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>定义&lt;/strong>：已启动的可执行程序实例，仅运行期间存在；未运行/结束的仅为“可执行文件”。&lt;/li>
&lt;li>&lt;strong>核心组成&lt;/strong>：内存地址空间（加载程序至内存供CPU运行）、唯一PID（区分同名进程）、程序代码与数据（含用户/系统数据）、进程状态（CPU调度依据）。&lt;/li>
&lt;/ul>
&lt;h3 id="2-进程状态分类">2. 进程状态分类
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>状态标识&lt;/th>
&lt;th>状态名称&lt;/th>
&lt;th>核心场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>R&lt;/td>
&lt;td>运行态&lt;/td>
&lt;td>正在CPU执行，或排队等时间片（如&lt;code>ping&lt;/code>运行中）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>S&lt;/td>
&lt;td>休眠态（可中断）&lt;/td>
&lt;td>等资源暂停：读硬盘文件（&lt;code>vim&lt;/code>开文件）；服务无请求（&lt;code>sshd&lt;/code>无连接），占比超90%&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>T&lt;/td>
&lt;td>停止态&lt;/td>
&lt;td>暂停未结束，可恢复（&lt;code>Ctrl+Z&lt;/code>将&lt;code>vim&lt;/code>放后台）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Z&lt;/td>
&lt;td>僵尸态&lt;/td>
&lt;td>进程结束但内存未释放（父进程未清理），无代码可执行&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>D&lt;/td>
&lt;td>不可中断休眠态&lt;/td>
&lt;td>执行IO（&lt;code>dd&lt;/code>读写、压缩），中断致数据损坏，不可强制结束&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2 id="二进程管理核心机制">二、进程管理核心机制
&lt;/h2>&lt;h3 id="1-进程调度">1. 进程调度
&lt;/h3>&lt;ul>
&lt;li>单CPU通过&lt;strong>计时器&lt;/strong>按10-25毫秒时间片轮询分配，不按代码量分配；多进程切换快，用户感知“同时运行”（如&lt;code>vim&lt;/code>/&lt;code>ping&lt;/code>/&lt;code>password&lt;/code>轮流执行）。&lt;/li>
&lt;/ul>
&lt;h3 id="2-中断处理">2. 中断处理
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>触发&lt;/strong>：用户输新命令（如&lt;code>vim&lt;/code>运行时输&lt;code>ls&lt;/code>），触发键盘中断。&lt;/li>
&lt;li>&lt;strong>流程&lt;/strong>：记当前进程剩余时间片→切新命令执行→执行完恢复原进程，确保“立即响应”。&lt;/li>
&lt;/ul>
&lt;h3 id="3-进程优先级">3. 进程优先级
&lt;/h3>&lt;ul>
&lt;li>不决定顺序，仅定&lt;strong>单次时间片长度&lt;/strong>：NI值-20~19（值越小优先级越高）；高优先级时间片长（25毫秒），总耗时短；普通用户仅能调低优先级。&lt;/li>
&lt;/ul>
&lt;h3 id="4-进程切换">4. 进程切换
&lt;/h3>&lt;ul>
&lt;li>CPU视角：清当前进程上下文（寄存器/缓存数据）→载入下进程上下文；切换耗时间，中断多则性能低。&lt;/li>
&lt;/ul>
&lt;h2 id="三进程生命周期与特殊进程">三、进程生命周期与特殊进程
&lt;/h2>&lt;h3 id="1-正常生命周期">1. 正常生命周期
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>创建&lt;/strong>：父进程&lt;code>fork&lt;/code>复制内存，子进程加载代码运行（如&lt;code>sshd&lt;/code>创子进程处理连接）。&lt;/li>
&lt;li>&lt;strong>结束&lt;/strong>：子进程发退出信号→父进程清理资源；所有进程根为PID=1（CentOS7+为&lt;code>systemd&lt;/code>，早期为&lt;code>init&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;h3 id="2-特殊进程">2. 特殊进程
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>进程类型&lt;/th>
&lt;th>产生原因&lt;/th>
&lt;th>处理方式&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>僵尸进程&lt;/td>
&lt;td>子进程发退出信号，父进程未清理&lt;/td>
&lt;td>1. &lt;code>ps aux&lt;/code>找Z进程；2. &lt;code>ps -ef&lt;/code>找父进程；3. &lt;code>kill&lt;/code>父进程&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>孤儿进程&lt;/td>
&lt;td>父进程提前结束，子进程未结束&lt;/td>
&lt;td>由&lt;code>systemd&lt;/code>接管，仍正常运行（如&lt;code>sshd&lt;/code>父崩，子进程续用）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2 id="四核心命令工具">四、核心命令工具
&lt;/h2>&lt;h3 id="1-进程查看命令">1. 进程查看命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>核心功能&lt;/th>
&lt;th>关键输出/选项&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>ps aux&lt;/code>&lt;/td>
&lt;td>基础进程信息&lt;/td>
&lt;td>USER/PID/%CPU/%MEM/STAT（含优先级标识）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>ps -ef&lt;/code>&lt;/td>
&lt;td>查进程父子关系&lt;/td>
&lt;td>显PPID（定位僵尸进程父进程）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>pstree -p&lt;/code>&lt;/td>
&lt;td>树状显进程层级&lt;/td>
&lt;td>如&lt;code>systemd(1)→sshd(786)→parent(12195)&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="2-进程控制命令kill系列">2. 进程控制命令（kill系列）
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能&lt;/th>
&lt;th>示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>kill&lt;/code>&lt;/td>
&lt;td>按PID发信号&lt;/td>
&lt;td>&lt;code>kill -1 823&lt;/code>（&lt;code>sshd&lt;/code>重载配置）；&lt;code>kill -9 2365&lt;/code>（强制结束）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>killall&lt;/code>&lt;/td>
&lt;td>按名结束所有匹配进程&lt;/td>
&lt;td>&lt;code>killall sshd&lt;/code>（结束所有&lt;code>sshd&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>pkill&lt;/code>&lt;/td>
&lt;td>按名/终端控制&lt;/td>
&lt;td>&lt;code>pkill -9 -t pts/0&lt;/code>（踢&lt;code>pts/0&lt;/code>终端用户）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="3-系统资源监控命令">3. 系统资源监控命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能&lt;/th>
&lt;th>关键操作&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>top&lt;/code>&lt;/td>
&lt;td>动态监控负载/CPU/内存&lt;/td>
&lt;td>&lt;code>p&lt;/code>按CPU排；&lt;code>M&lt;/code>按内存排；&lt;code>-p 9957&lt;/code>查指定进程&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>free&lt;/code>&lt;/td>
&lt;td>查内存/swap使用&lt;/td>
&lt;td>支持&lt;code>-m&lt;/code>（MB）；可用空间=空闲+buffer/cache&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>htop&lt;/code>&lt;/td>
&lt;td>&lt;code>top&lt;/code>增强版&lt;/td>
&lt;td>鼠标操作；&lt;code>F9&lt;/code>发信号结束进程&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>dstat&lt;/code>&lt;/td>
&lt;td>多维度监控（CPU/磁盘/网络）&lt;/td>
&lt;td>&lt;code>dstat -t 2 10&lt;/code>（2秒刷1次，共10次）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="4-资源定位命令">4. 资源定位命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能&lt;/th>
&lt;th>示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>fuser&lt;/code>&lt;/td>
&lt;td>按路径找进程（解挂载占用）&lt;/td>
&lt;td>&lt;code>fuser -k /mnt&lt;/code>（结束占&lt;code>/mnt&lt;/code>的进程）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>lsof&lt;/code>&lt;/td>
&lt;td>按进程找打开的文件/端口&lt;/td>
&lt;td>&lt;code>lsof -p 1765&lt;/code>（查&lt;code>PID1765&lt;/code>开的文件）；&lt;code>lsof -i :22&lt;/code>（查22端口）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2 id="五进程与终端的关系">五、进程与终端的关系
&lt;/h2>&lt;h3 id="1-前后台切换">1. 前后台切换
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>操作/命令&lt;/th>
&lt;th>功能&lt;/th>
&lt;th>示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>命令 &amp;amp;&lt;/code>&lt;/td>
&lt;td>直接放后台运行&lt;/td>
&lt;td>&lt;code>ping 127.0.0.1 &amp;amp;&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>jobs&lt;/code>&lt;/td>
&lt;td>查当前终端后台进程&lt;/td>
&lt;td>显“[1]+ 运行中 ping&amp;hellip;”&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fg %ID&lt;/code>&lt;/td>
&lt;td>后台切前台&lt;/td>
&lt;td>&lt;code>fg %1&lt;/code>（切后台1号进程）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>bg %ID&lt;/code>&lt;/td>
&lt;td>恢复后台暂停进程&lt;/td>
&lt;td>&lt;code>bg %1&lt;/code>（恢复后台1号进程）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>Ctrl + Z&lt;/code>&lt;/td>
&lt;td>前台进程暂停放后台&lt;/td>
&lt;td>&lt;code>vim&lt;/code>运行时按，状态变T&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="2-脱离终端的进程">2. 脱离终端的进程
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>工具&lt;/th>
&lt;th>功能&lt;/th>
&lt;th>用法示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>nohup&lt;/code>&lt;/td>
&lt;td>简单脱离终端，终端关进程续跑&lt;/td>
&lt;td>&lt;code>nohup ping 127.0.0.1 &amp;amp;&lt;/code>，输出存&lt;code>nohup.out&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tmux&lt;/code>&lt;/td>
&lt;td>终端复用，恢复操作场景&lt;/td>
&lt;td>创会话：&lt;code>tmux new -s hf01&lt;/code>；进会话：&lt;code>tmux attach -t hf01&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2 id="六进程优先级调整与资源限制">六、进程优先级调整与资源限制
&lt;/h2>&lt;h3 id="1-优先级调整命令">1. 优先级调整命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>作用对象&lt;/th>
&lt;th>用法示例&lt;/th>
&lt;th>说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>nice&lt;/code>&lt;/td>
&lt;td>未运行程序&lt;/td>
&lt;td>&lt;code>nice -n -5 vim&lt;/code>&lt;/td>
&lt;td>启&lt;code>vim&lt;/code>时定NI=-5（PR=15）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>renice&lt;/code>&lt;/td>
&lt;td>已运行进程&lt;/td>
&lt;td>&lt;code>renice -n -10 993&lt;/code>&lt;/td>
&lt;td>改&lt;code>PID993&lt;/code>的NI为-10（PR=10）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="2-进程数量限制">2. 进程数量限制
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>&lt;code>ulimit&lt;/code>&lt;/strong>：&lt;code>ulimit -u&lt;/code>查默认限制（约7576）；&lt;code>ulimit -u 15&lt;/code>临时限15个进程（重登失效）；写&lt;code>/etc/profile&lt;/code>永久生效。&lt;/li>
&lt;li>&lt;strong>&lt;code>stress&lt;/code>&lt;/strong>：压测验证，&lt;code>stress -c 20&lt;/code>创20进程，超限制则失败。&lt;/li>
&lt;/ul>
&lt;h2 id="七补充工具与实用技巧">七、补充工具与实用技巧
&lt;/h2>&lt;h3 id="1-time命令">1. &lt;code>time&lt;/code>命令
&lt;/h3>&lt;ul>
&lt;li>统计命令执行时间：&lt;code>time ls&lt;/code>；显Real（总耗时）、User（用户态）、Sys（内核态）时间；Real&amp;gt;User+Sys因IO等待/其他进程占用。&lt;/li>
&lt;/ul>
&lt;h3 id="2-系统配置">2. 系统配置
&lt;/h3>&lt;ul>
&lt;li>&lt;strong>SELinux&lt;/strong>：改服务端口（如&lt;code>sshd&lt;/code>22→2200）需关SELinux（临时&lt;code>setenforce 0&lt;/code>，永久改&lt;code>/etc/selinux/config&lt;/code>）。&lt;/li>
&lt;li>&lt;strong>配置生效&lt;/strong>：服务改配置后，&lt;code>kill -1 进程号&lt;/code>重载（如&lt;code>sshd&lt;/code>），免重启。&lt;/li>
&lt;/ul>
&lt;h2 id="八课程后续预告">八、课程后续预告
&lt;/h2>&lt;p>后续讲解：1. 计划任务（&lt;code>crontab&lt;/code>定时执行进程）；2. 系统日志（查进程日志排障）；3. 进程竞争（多进程抢资源处理）。&lt;/p></description></item><item><title>Linux 日志管理与定时任务基础</title><link>https://luckycloveryh.github.io/fuyou/p/linux-08/</link><pubDate>Wed, 26 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-08/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-08/1200/675" alt="Featured image of post Linux 日志管理与定时任务基础" />&lt;h1 id="linux-系统管理核心知识日志管理与定时计划任务">Linux 系统管理核心知识：日志管理与定时计划任务
&lt;/h1>&lt;h2 id="第一部分日志管理log-management">第一部分：日志管理（Log Management）
&lt;/h2>&lt;p>日志是系统的“黑匣子”，记录系统运行细节，是问题排查、安全审计、性能监控和任务执行验证的关键依据，贯穿系统运维全流程。&lt;/p>
&lt;h3 id="一日志基础作用服务与关键文件">一、日志基础：作用、服务与关键文件
&lt;/h3>&lt;h4 id="一日志的核心作用">（一）日志的核心作用
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>任务执行验证&lt;/strong>：查看定时计划任务（如crontab）执行频率，例如验证任务是否按“每分钟一次”设定运行。&lt;/li>
&lt;li>&lt;strong>异常排查定位&lt;/strong>：服务启动失败（如sshd配置错误）或系统异常时，日志精确提示错误位置（如“/etc/ssh/sshd_config第112行配置错误”），Linux 7+版本提示更详细。&lt;/li>
&lt;li>&lt;strong>非工作时段追溯&lt;/strong>：服务器24小时运行，日志记录工作人员离线期间的报错、警告，补充远程连接时本地显示器无法呈现的故障细节。&lt;/li>
&lt;li>&lt;strong>安全审计与性能监控&lt;/strong>：追踪用户登录、文件访问、系统调用（如&lt;code>secure&lt;/code>日志记录暴力破解IP），长期分析日志识别系统瓶颈与资源使用趋势。&lt;/li>
&lt;/ol>
&lt;h4 id="二linux-核心日志服务">（二）Linux 核心日志服务
&lt;/h4>&lt;p>Linux 7/8/9版本中，日志由两类服务协同处理，功能与存储特点差异显著：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>服务名称&lt;/th>
&lt;th>启动时机&lt;/th>
&lt;th>存储方式&lt;/th>
&lt;th>核心特点&lt;/th>
&lt;th>适用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>systemd-journald&lt;/code>&lt;/td>
&lt;td>随系统启动&lt;/td>
&lt;td>默认内存（&lt;code>/run/log/journal/&lt;/code>）&lt;/td>
&lt;td>收集内核和早期系统日志，重启后日志丢失；可通过配置实现持久化&lt;/td>
&lt;td>查看系统启动初期临时日志&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>syslog&lt;/code>（视频口误提及）&lt;/td>
&lt;td>随内核启动&lt;/td>
&lt;td>默认内存&lt;/td>
&lt;td>记录系统启动早期日志，临时性存储，重启后旧日志丢失；可改持久化&lt;/td>
&lt;td>辅助查看启动初期日志&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>rsyslog&lt;/code>&lt;/td>
&lt;td>系统运行中持续工作&lt;/td>
&lt;td>默认磁盘（&lt;code>/var/log/&lt;/code>）&lt;/td>
&lt;td>接收&lt;code>journald&lt;/code>及应用日志，永久存储，支持自定义存储规则，功能更强大&lt;/td>
&lt;td>长期保存、查询各类系统/服务日志&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h4 id="三varlog-目录下的关键日志文件">（三）&lt;code>/var/log&lt;/code> 目录下的关键日志文件
&lt;/h4>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>日志文件&lt;/th>
&lt;th>核心用途&lt;/th>
&lt;th>查看/操作要点&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>messages&lt;/code>&lt;/td>
&lt;td>通用日志，记录除mail、authpriv、cron外的大部分系统消息&lt;/td>
&lt;td>排除重复类别，避免有效信息淹没；可通过&lt;code>rsyslog&lt;/code>配置查看排除规则&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>secure&lt;/code>&lt;/td>
&lt;td>安全日志，记录用户登录（成功/失败）、&lt;code>sudo&lt;/code>操作、&lt;code>useradd&lt;/code>等权限相关行为&lt;/td>
&lt;td>&lt;code>grep &amp;quot;failed password&amp;quot; /var/log/secure&lt;/code>定位暴力破解IP；&lt;code>chattr +a&lt;/code>防篡改&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>cron&lt;/code>&lt;/td>
&lt;td>定时任务日志，记录crontab任务执行情况&lt;/td>
&lt;td>格式：“时间+主机名+进程名+事件”；&lt;code>vi&lt;/code>中按大写“G”跳转最新记录&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>wtmp&lt;/code>/&lt;code>btmp&lt;/code>&lt;/td>
&lt;td>二进制日志，分别记录登录历史、登录失败历史&lt;/td>
&lt;td>无法&lt;code>vi&lt;/code>直接查看；&lt;code>last&lt;/code>读&lt;code>wtmp&lt;/code>，&lt;code>lastb&lt;/code>读&lt;code>btmp&lt;/code>；二进制格式防篡改、省资源&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>dmesg&lt;/code>&lt;/td>
&lt;td>内核消息缓冲区日志，含硬件检测、内核模块加载信息&lt;/td>
&lt;td>可通过&lt;code>dmesg&lt;/code>命令直接查看，或查看&lt;code>/var/log/dmesg&lt;/code>文件&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二日志查看与故障排查实操">二、日志查看与故障排查实操
&lt;/h3>&lt;h4 id="一基础查看与实时监控命令">（一）基础查看与实时监控命令
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>实时跟踪日志&lt;/strong>：&lt;code>tail -f /var/log/[文件名]&lt;/code>（如&lt;code>tail -f /var/log/secure&lt;/code>），实时捕捉新产生的登录、用户创建等日志。&lt;/li>
&lt;li>&lt;strong>关键词过滤&lt;/strong>：&lt;code>grep [关键词] /var/log/[文件名]&lt;/code>（如&lt;code>grep &amp;quot;useradd&amp;quot; /var/log/secure&lt;/code>），快速定位目标记录。&lt;/li>
&lt;li>&lt;strong>关键信息提取与统计&lt;/strong>：
&lt;ul>
&lt;li>截取IP地址：&lt;code>grep &amp;quot;Failed password&amp;quot; /var/log/secure | awk '{print $11}'&lt;/code>&lt;/li>
&lt;li>统计重复IP（判断暴力破解）：&lt;code>grep &amp;quot;Failed password&amp;quot; /var/log/secure | awk '{print $11}' | sort | uniq -c | sort -nr&lt;/code>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>时间范围查找&lt;/strong>：&lt;code>sed -n '/2025-11-27 11:00:00/,/2025-11-27 14:00:00/p' /var/log/messages&lt;/code>，精准筛选时段日志。&lt;/li>
&lt;/ol>
&lt;h4 id="二journalctl-工具高级日志筛选与查看">（二）&lt;code>journalctl&lt;/code> 工具：高级日志筛选与查看
&lt;/h4>&lt;p>&lt;code>journalctl&lt;/code>是&lt;code>systemd&lt;/code>系统中查询&lt;code>journald&lt;/code>日志的核心工具，支持多维度组合筛选，功能灵活。&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>选项/用法&lt;/th>
&lt;th>功能描述&lt;/th>
&lt;th>示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>按日志类型筛选&lt;/strong>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl --system&lt;/code>&lt;/td>
&lt;td>查看系统级日志，默认时间由远及近显示&lt;/td>
&lt;td>搭配&lt;code>-e&lt;/code>直接跳转最新日志（最后一页），向上翻页查更早内容&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl --user&lt;/code>&lt;/td>
&lt;td>查看当前用户日志，默认无记录，结果为空&lt;/td>
&lt;td>-&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl -k&lt;/code>/&lt;code>--dmesg&lt;/code>&lt;/td>
&lt;td>仅查看内核日志，与&lt;code>--system&lt;/code>结果部分重复（&lt;code>--system&lt;/code>含内核日志）&lt;/td>
&lt;td>&lt;code>journalctl -k&lt;/code>专注内核，排除系统其他日志干扰&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>排序与显示方式&lt;/strong>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl -r&lt;/code>&lt;/td>
&lt;td>时间由近及远倒序显示，最上方为最新日志&lt;/td>
&lt;td>需向下翻页查更早内容，区别于&lt;code>-e&lt;/code>的“直接看最后一页”&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl -f&lt;/code>&lt;/td>
&lt;td>实时跟踪新日志，类似&lt;code>tail -f&lt;/code>&lt;/td>
&lt;td>&lt;code>journalctl -fu sshd.service&lt;/code>实时监控sshd服务日志&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>按条件精准筛选&lt;/strong>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl -p [级别]&lt;/code>&lt;/td>
&lt;td>查看指定级别及更严重日志（包含关系：级别从轻到重：debug→info→notice→warning→err→crit→alert→emerg）&lt;/td>
&lt;td>&lt;code>journalctl -p err&lt;/code>显示err及以上；&lt;code>-p warning&lt;/code>含err、emerg等&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl --since/--until &amp;quot;时间&amp;quot;&lt;/code>&lt;/td>
&lt;td>按时间范围筛选，格式“年月日 时分秒”（秒可省），支持“最近一小时”等描述&lt;/td>
&lt;td>&lt;code>journalctl --since &amp;quot;2025-11-27 11:30&amp;quot; --until &amp;quot;2025-11-27 14:00&amp;quot;&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl _PID=[进程号]&lt;/code>&lt;/td>
&lt;td>查看指定进程日志，需进程持续存在且有日志产生&lt;/td>
&lt;td>进程重启后PID变化，旧日志需用旧PID查询（如sshd原PID 11930）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl -u [服务名]&lt;/code>&lt;/td>
&lt;td>仅查看指定服务日志，排除其他干扰&lt;/td>
&lt;td>&lt;code>journalctl -xeu sshd.service&lt;/code>：&lt;code>-x&lt;/code>加解释信息，定位服务启动失败原因&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>高级操作&lt;/strong>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl --disk-usage&lt;/code>&lt;/td>
&lt;td>查看日志占用存储空间&lt;/td>
&lt;td>虚拟机中默认约5.9MB，需维持基础大小，过大时再清理&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>journalctl --vacuum-size/--vacuum-time&lt;/code>&lt;/td>
&lt;td>清理日志，限制留存大小或时间&lt;/td>
&lt;td>&lt;code>journalctl --vacuum-size=100M&lt;/code>；&lt;code>--vacuum-time=1week&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="三日志管理核心操作">三、日志管理核心操作
&lt;/h3>&lt;h4 id="一日志轮替log-rotation防止文件过大">（一）日志轮替（Log Rotation）：防止文件过大
&lt;/h4>&lt;p>通过周期性处理旧日志、创建新日志，避免单个文件无限增大，保证日志可查性，依赖系统默认工具&lt;code>logrotate&lt;/code>。&lt;/p>
&lt;ol>
&lt;li>
&lt;p>&lt;strong>轮替触发条件&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>时间周期：&lt;code>daily/weekly/monthly&lt;/code>，适合日志生成量稳定场景；&lt;/li>
&lt;li>大小周期：&lt;code>size 200KB/100M&lt;/code>，达到指定大小触发，适合生成量不稳定场景。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>配置文件与优先级&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>主配置文件：&lt;code>/etc/logrotate.conf&lt;/code>（全局规则，如默认每周轮替、保存4份旧日志）；&lt;/li>
&lt;li>局部配置目录：&lt;code>/etc/logrotate.d/&lt;/code>（为nginx、mysql等服务配置独立规则，优先级高于全局）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>关键配置项&lt;/strong>：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>配置项&lt;/th>
&lt;th>功能描述&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>rotate 4&lt;/code>&lt;/td>
&lt;td>保留4份旧日志，超过则删除最旧的&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>compress&lt;/code>&lt;/td>
&lt;td>轮替后压缩旧日志（可配置“不压缩最近1份”方便查看）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>create 0640 root root&lt;/code>&lt;/td>
&lt;td>创建新空日志，指定权限（0640）和所有者（root:root）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>delaycompress&lt;/code>&lt;/td>
&lt;td>延迟压缩，下一次轮替时再压缩当前旧日志&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>特殊处理与强制轮替&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>若日志设&lt;code>a&lt;/code>属性（不可修改）：轮替前&lt;code>chattr -a 文件名&lt;/code>取消属性，轮替后&lt;code>chattr +a&lt;/code>恢复；&lt;/li>
&lt;li>强制轮替（测试规则）：&lt;code>logrotate -vf /etc/logrotate.d/nginx&lt;/code>（&lt;code>-v&lt;/code>详细输出，&lt;code>-f&lt;/code>强制执行）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="二日志拆分与合并处理已过大日志">（二）日志拆分与合并：处理已过大日志
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>拆分操作&lt;/strong>：用&lt;code>split&lt;/code>命令拆分文本类日志（二进制文件不适用），核心选项&lt;code>-b&lt;/code>指定单文件大小。&lt;br>
示例：&lt;code>split -b 500M -d -a 3 large.log log_part_&lt;/code>&lt;/p>
&lt;ul>
&lt;li>&lt;code>-d&lt;/code>：数字后缀；&lt;code>-a 3&lt;/code>：后缀3位（000/001&amp;hellip;）；生成&lt;code>log_part_000&lt;/code>（500M）、&lt;code>log_part_001&lt;/code>等。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>合并操作&lt;/strong>：用&lt;code>cat&lt;/code>合并拆分文件，内容与原文件一致。&lt;br>
示例：&lt;code>cat log_part_* &amp;gt; large.log.restored&lt;/code>。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h4 id="三journal-日志持久化配置">（三）&lt;code>journal&lt;/code> 日志持久化配置
&lt;/h4>&lt;p>默认&lt;code>journal&lt;/code>日志存于内存，重启丢失，需配置持久化保存至磁盘：&lt;/p>
&lt;ol>
&lt;li>
&lt;p>创建目录：&lt;code>mkdir /var/log/journal&lt;/code>；&lt;/p>
&lt;/li>
&lt;li>
&lt;p>重启系统：系统自动配置权限（添加&lt;code>sgid&lt;/code>、所属组改为&lt;code>systemd-journal&lt;/code>、设置ACL权限），无需手动操作；&lt;/p>
&lt;/li>
&lt;li>
&lt;p>存储特点：日志保存为&lt;code>/var/log/journal/&lt;/code>下的二进制文件（如&lt;code>system.journal&lt;/code>），无法像文本文档直接打开编辑；&lt;/p>
&lt;/li>
&lt;li>
&lt;p>用户日志隔离：系统按用户UID（如1001）在&lt;code>/var/log/journal/&lt;/code>下单独创建日志文件，普通用户查看的日志更少。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>限制日志大小&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>编辑配置文件：&lt;code>journald.conf&lt;/code>（路径通常为&lt;code>/etc/systemd/journald.conf&lt;/code>）；&lt;/li>
&lt;li>设置参数：&lt;code>SystemMaxUse=500M&lt;/code>（最大占用空间）、&lt;code>MaxFileSize=50M&lt;/code>（单个文件最大大小）；&lt;/li>
&lt;li>生效方式：重启&lt;code>systemd-journald&lt;/code>服务：&lt;code>systemctl restart systemd-journald&lt;/code>。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="四日志空间管理与安全维护">（四）日志空间管理与安全维护
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>日志清空方式&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>覆盖清空：&lt;code>&amp;gt; /var/log/[文件名]&lt;/code>，保留inode号，避免进程因inode变化无法写入；&lt;/li>
&lt;li>删除重建：&lt;code>rm /var/log/[文件名] &amp;amp;&amp;amp; touch /var/log/[文件名]&lt;/code>，可能改变inode，多进程场景慎用。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>日志防篡改&lt;/strong>：用&lt;code>chattr +a /var/log/[文件名]&lt;/code>添加“仅追加”属性，文件仅允许新增内容，无法删除（&lt;code>rm -rf&lt;/code>无效）、修改旧内容；&lt;code>lsattr /var/log/[文件名]&lt;/code>查看属性是否生效。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="四日志集中管理多服务器场景">四、日志集中管理：多服务器场景
&lt;/h3>&lt;h4 id="一核心目的">（一）核心目的
&lt;/h4>&lt;p>多服务器环境中，将所有日志集中到“日志收集服务器”，管理员无需逐台登录，统一查看与分析。&lt;/p>
&lt;h4 id="二实现原理rsyslog-客户端-服务器架构">（二）实现原理（&lt;code>rsyslog&lt;/code> 客户端-服务器架构）
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>服务器端配置&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>修改&lt;code>/etc/rsyslog.conf&lt;/code>：取消TCP/UDP监听端口（默认514）的注释（如&lt;code>$ModLoad imtcp&lt;/code>、&lt;code>$InputTCPServerRun 514&lt;/code>）；&lt;/li>
&lt;li>重启服务：&lt;code>systemctl restart rsyslog&lt;/code>；&lt;/li>
&lt;li>开放端口：防火墙开放514端口（或临时关闭防火墙：&lt;code>systemctl stop firewalld&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>客户端配置&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>修改主机名：&lt;code>hostnamectl set-hostname A1&lt;/code>（避免多客户端主机名重复导致日志混淆）；&lt;/li>
&lt;li>修改&lt;code>/etc/rsyslog.conf&lt;/code>：添加发送规则，&lt;code>*.* @@192.168.5.144:514&lt;/code>（&lt;code>@@&lt;/code>TCP协议，&lt;code>@&lt;/code>UDP协议；&lt;code>192.168.5.144&lt;/code>为服务器IP）；&lt;/li>
&lt;li>重启服务：&lt;code>systemctl restart rsyslog&lt;/code>，关闭客户端防火墙（或开放端口）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>效果验证&lt;/strong>：客户端执行&lt;code>useradd xe&lt;/code>，服务器端在&lt;code>/var/log/messages&lt;/code>中可看到含客户端主机名（A1）的操作记录。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="五日志配置与服务管理">五、日志配置与服务管理
&lt;/h3>&lt;h4 id="一rsyslog-配置文件详解">（一）&lt;code>rsyslog&lt;/code> 配置文件详解
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>核心配置文件&lt;/strong>：&lt;code>/etc/rsyslog.conf&lt;/code>，规则格式为&lt;code>facility.priority action&lt;/code>（类别.级别 处理方式）。&lt;/p>
&lt;ul>
&lt;li>类别（facility）：日志来源，如kern（内核）、cron（定时任务）、mail（邮件）、authpriv（安全）；&lt;/li>
&lt;li>级别（priority）：从轻到重为debug→info→notice→warning→err→crit→alert→emerg；&lt;/li>
&lt;li>处理方式（action）：保存文件（&lt;code>/var/log/xxx&lt;/code>）、发送远程服务器（&lt;code>@@ip:port&lt;/code>）、广播（&lt;code>*&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>经典规则示例&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>*.info;mail.none;authpriv.none;cron.none /var/log/messages&lt;/code>：记录所有类别info及以上日志，排除mail、authpriv、cron，保存至&lt;code>messages&lt;/code>；&lt;/li>
&lt;li>&lt;code>*.emerg *&lt;/code>：所有类别emerg级别日志，广播给所有在线用户。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>配置生效与自定义日志&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>生效方式：修改后需重启&lt;code>rsyslog&lt;/code>（&lt;code>systemctl restart rsyslog&lt;/code>）或重启系统；发送&lt;code>HUP&lt;/code>信号（&lt;code>kill -HUP 进程号&lt;/code>）无效，因&lt;code>rsyslog&lt;/code>未定义该信号响应；&lt;/li>
&lt;li>自定义日志：添加规则&lt;code>*.* /var/log/my.log&lt;/code>，所有日志保存至&lt;code>/var/log/my.log&lt;/code>，重启服务后生效。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="二systemd-journald-服务操作注意事项">（二）&lt;code>systemd-journald&lt;/code> 服务操作注意事项
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>命令补全&lt;/strong>：执行&lt;code>systemctl status systemd-journald&lt;/code>时，系统默认补全&lt;code>.service&lt;/code>后缀，无需手动添加；&lt;/li>
&lt;li>&lt;strong>服务名正确性&lt;/strong>：避免漏写“d”（如误写&lt;code>system-journal&lt;/code>），否则提示“服务未找到”；&lt;/li>
&lt;li>&lt;strong>选项组合使用&lt;/strong>：支持多选项搭配，如&lt;code>journalctl -u sshd.service --since &amp;quot;2025-11-27 14:00&amp;quot; --until &amp;quot;2025-11-27 15:00&amp;quot;&lt;/code>，筛选sshd服务在指定时段的日志。&lt;/li>
&lt;/ol>
&lt;h2 id="第二部分定时计划任务cron-jobs">第二部分：定时计划任务（Cron Jobs）
&lt;/h2>&lt;p>定时任务是Linux自动化运维的核心，实现命令/脚本在指定时间自动执行，核心工具为&lt;code>at&lt;/code>（一次性）和&lt;code>crontab&lt;/code>（周期性）。&lt;/p>
&lt;h3 id="一核心工具对比">一、核心工具对比
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>工具&lt;/th>
&lt;th>执行特性&lt;/th>
&lt;th>核心用途&lt;/th>
&lt;th>依赖守护进程&lt;/th>
&lt;th>适用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>at&lt;/code>&lt;/td>
&lt;td>一次性执行&lt;/td>
&lt;td>突发、单次任务（如10分钟后执行脚本）&lt;/td>
&lt;td>&lt;code>atd&lt;/code>&lt;/td>
&lt;td>临时自动化需求，无需重复执行&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>crontab&lt;/code>&lt;/td>
&lt;td>周期性执行&lt;/td>
&lt;td>重复、规律任务（如每天凌晨备份）&lt;/td>
&lt;td>&lt;code>crond&lt;/code>&lt;/td>
&lt;td>日常运维自动化（备份、清理）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二at-命令一次性任务">二、&lt;code>at&lt;/code> 命令：一次性任务
&lt;/h3>&lt;h4 id="一安装与启动">（一）安装与启动
&lt;/h4>&lt;ol>
&lt;li>安装：&lt;code>sudo dnf install at&lt;/code>（RHEL/CentOS）或&lt;code>sudo apt install at&lt;/code>（Debian/Ubuntu）；&lt;/li>
&lt;li>启动服务：&lt;code>sudo systemctl start atd&lt;/code>；&lt;/li>
&lt;li>开机自启：&lt;code>sudo systemctl enable atd&lt;/code>。&lt;/li>
&lt;/ol>
&lt;h4 id="二使用方法">（二）使用方法
&lt;/h4>&lt;ol>
&lt;li>
&lt;p>&lt;strong>交互模式&lt;/strong>：手动输入任务，按&lt;code>Ctrl+D&lt;/code>提交。&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">at 14:30 &lt;span class="c1"># 设定14:30执行&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">at&amp;gt; /home/user/script.sh &lt;span class="c1"># 任务内容（执行脚本）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">at&amp;gt; &amp;lt;EOT&amp;gt; &lt;span class="c1"># Ctrl+D提交&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>
&lt;p>&lt;strong>非交互模式&lt;/strong>：通过管道传递任务，适合脚本集成。&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="s2">&amp;#34;/home/user/script.sh&amp;#34;&lt;/span> &lt;span class="p">|&lt;/span> at now + &lt;span class="m">10&lt;/span> minutes &lt;span class="c1"># 10分钟后执行&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>
&lt;p>&lt;strong>常用时间格式&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>绝对时间：&lt;code>at 10:00 2024-12-31&lt;/code>（2024年12月31日10点）；&lt;/li>
&lt;li>相对时间：&lt;code>at now + 3 hours&lt;/code>（3小时后）、&lt;code>at 5pm tomorrow&lt;/code>（明天17点）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="三任务管理">（三）任务管理
&lt;/h4>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能描述&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>atq&lt;/code>/&lt;code>at -l&lt;/code>&lt;/td>
&lt;td>列出当前用户的所有待执行任务&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>`at -c [任务号]&lt;/td>
&lt;td>查看指定任务的详细内容&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>atrm [任务号]&lt;/code>/&lt;code>at -r [任务号]&lt;/code>&lt;/td>
&lt;td>删除指定任务&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="三crontab-命令周期性任务">三、&lt;code>crontab&lt;/code> 命令：周期性任务
&lt;/h3>&lt;h4 id="一crond-服务基础">（一）&lt;code>crond&lt;/code> 服务基础
&lt;/h4>&lt;ul>
&lt;li>默认开机自启，查看状态：&lt;code>systemctl status crond&lt;/code>；&lt;/li>
&lt;li>服务异常时重启：&lt;code>systemctl restart crond&lt;/code>。&lt;/li>
&lt;/ul>
&lt;h4 id="二crontab-文件格式">（二）&lt;code>crontab&lt;/code> 文件格式
&lt;/h4>&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="line">&lt;span class="cl">* * * * * /path/to/command
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">- - - - -
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | | |
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | | +----- 星期（0-6，星期日=0或7）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | | +------- 月份（1-12）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| | +--------- 日期（1-31）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">| +----------- 小时（0-23）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">+------------- 分钟（0-59）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;ol>
&lt;li>
&lt;p>&lt;strong>特殊字符含义&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>*&lt;/code>：匹配所有值（如小时位&lt;code>*&lt;/code>表示每小时）；&lt;/li>
&lt;li>&lt;code>,&lt;/code>：指定多个值（如&lt;code>1,3,5&lt;/code>表示1、3、5）；&lt;/li>
&lt;li>&lt;code>-&lt;/code>：指定范围（如&lt;code>1-5&lt;/code>表示1到5）；&lt;/li>
&lt;li>&lt;code>/&lt;/code>：指定步长（如&lt;code>*/10&lt;/code>在分钟位表示每10分钟）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>经典任务示例&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>0 2 * * * /usr/bin/backup.sh&lt;/code>：每天凌晨2点执行备份脚本；&lt;/li>
&lt;li>&lt;code>*/15 * * * * /usr/bin/clean_temp.sh&lt;/code>：每15分钟清理临时文件；&lt;/li>
&lt;li>&lt;code>30 22 * * 1-5 /usr/bin/echo &amp;quot;Good night!&amp;quot;&lt;/code>：每周一至周五22:30输出提示；&lt;/li>
&lt;li>&lt;code>0 12 1 * * /usr/bin/send_report.sh&lt;/code>：每月1日12点发送报告。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h4 id="三crontab-命令操作">（三）&lt;code>crontab&lt;/code> 命令操作
&lt;/h4>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能描述&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>crontab -e&lt;/code>&lt;/td>
&lt;td>编辑当前用户的定时任务（默认用vi打开，按&lt;code>Esc&lt;/code>+&lt;code>:wq&lt;/code>保存退出）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>crontab -l&lt;/code>&lt;/td>
&lt;td>列出当前用户的所有定时任务&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>crontab -r&lt;/code>&lt;/td>
&lt;td>删除当前用户的所有定时任务（&lt;strong>慎用，无确认步骤&lt;/strong>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>sudo crontab -u [用户名] -e&lt;/code>&lt;/td>
&lt;td>编辑指定用户的定时任务（仅root权限可用，如&lt;code>sudo crontab -u xe -e&lt;/code>）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h4 id="四系统级-cron-任务">（四）系统级 &lt;code>cron&lt;/code> 任务
&lt;/h4>&lt;ol>
&lt;li>&lt;strong>全局配置文件&lt;/strong>：&lt;code>/etc/crontab&lt;/code>，格式需额外指定执行用户，示例：&lt;br>
&lt;code>0 2 * * * root /usr/bin/backup.sh&lt;/code>（root用户每天2点执行备份）；&lt;/li>
&lt;li>&lt;strong>独立任务目录&lt;/strong>：&lt;code>/etc/cron.d/&lt;/code>，存放系统级任务文件（如&lt;code>nginx&lt;/code>定时任务）；&lt;/li>
&lt;li>&lt;strong>按频率自动执行目录&lt;/strong>：&lt;code>/etc/cron.hourly/&lt;/code>（每小时）、&lt;code>/etc/cron.daily/&lt;/code>（每天）、&lt;code>/etc/cron.weekly/&lt;/code>（每周）、&lt;code>/etc/cron.monthly/&lt;/code>（每月），放入可执行脚本（&lt;code>chmod +x 脚本&lt;/code>）即可自动执行。&lt;/li>
&lt;/ol>
&lt;h3 id="四权限控制">四、权限控制
&lt;/h3>&lt;p>通过&lt;code>allow&lt;/code>（白名单）和&lt;code>deny&lt;/code>（黑名单）文件控制用户使用&lt;code>at&lt;/code>/&lt;code>crontab&lt;/code>的权限，优先级：白名单 &amp;gt; 黑名单。&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>文件路径&lt;/th>
&lt;th>类型&lt;/th>
&lt;th>功能描述&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>/etc/at.deny&lt;/code>&lt;/td>
&lt;td>黑名单&lt;/td>
&lt;td>列出的用户禁止使用&lt;code>at&lt;/code>，默认空文件（所有用户可使用）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/cron.deny&lt;/code>&lt;/td>
&lt;td>黑名单&lt;/td>
&lt;td>列出的用户禁止使用&lt;code>crontab&lt;/code>，默认空文件（所有用户可使用）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/at.allow&lt;/code>&lt;/td>
&lt;td>白名单&lt;/td>
&lt;td>仅列出的用户可使用&lt;code>at&lt;/code>，默认不存在，需手动创建（优先级高于&lt;code>at.deny&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/cron.allow&lt;/code>&lt;/td>
&lt;td>白名单&lt;/td>
&lt;td>仅列出的用户可使用&lt;code>crontab&lt;/code>，默认不存在，需手动创建（优先级高于&lt;code>cron.deny&lt;/code>）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;blockquote>
&lt;p>注意：若白名单存在，黑名单自动失效；建议仅在严格权限控制场景创建白名单。&lt;/p>
&lt;/blockquote>
&lt;h3 id="五注意事项与排错">五、注意事项与排错
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>&lt;strong>环境变量问题&lt;/strong>：&lt;code>cron&lt;/code>执行环境精简，&lt;code>PATH&lt;/code>等变量与用户登录时不同，务必使用&lt;strong>绝对路径&lt;/strong>（如&lt;code>/usr/bin/ls&lt;/code>而非&lt;code>ls&lt;/code>），或在脚本开头定义&lt;code>PATH&lt;/code>（&lt;code>export PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin&lt;/code>）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>脚本权限&lt;/strong>：确保&lt;code>crontab&lt;/code>调用的脚本有可执行权限：&lt;code>chmod +x /path/to/script.sh&lt;/code>。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>输出重定向&lt;/strong>：&lt;code>cron&lt;/code>的stdout（标准输出）和stderr（标准错误）默认通过邮件发送给执行用户，易造成邮件轰炸或信息丢失，建议重定向到日志文件：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">* * * * * /path/to/script.sh &amp;gt; /var/log/my_cron.log 2&amp;gt;&lt;span class="p">&amp;amp;&lt;/span>&lt;span class="m">1&lt;/span> &lt;span class="c1"># 2&amp;gt;&amp;amp;1将错误输出合并到标准输出&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;/li>
&lt;li>
&lt;p>&lt;strong>日志排查&lt;/strong>：&lt;code>cron&lt;/code>执行情况记录在系统日志中，通过以下命令排查任务是否执行：&lt;/p>
&lt;ul>
&lt;li>RHEL/CentOS：&lt;code>grep CRON /var/log/messages&lt;/code>；&lt;/li>
&lt;li>Debian/Ubuntu：&lt;code>grep CRON /var/log/syslog&lt;/code>。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="第三部分学习进度与建议">第三部分：学习进度与建议
&lt;/h2>&lt;h3 id="一进度规划">一、进度规划
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>当日任务&lt;/strong>：完成日志管理核心操作（轮替、持久化、集中管理）、&lt;code>journalctl&lt;/code>详解、定时任务基础用法讲解；&lt;/li>
&lt;li>&lt;strong>次日（周五）&lt;/strong>：讲解剩余日志配置细节与定时任务进阶案例，完成本阶段全部知识点；&lt;/li>
&lt;li>&lt;strong>练习安排&lt;/strong>：周五晚上练习“服务管理（&lt;code>systemctl&lt;/code>）”“系统管理（日志清理、权限配置）”，周末启动全面复习。&lt;/li>
&lt;/ol>
&lt;h3 id="二学习要求与提示">二、学习要求与提示
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>&lt;strong>核心目标&lt;/strong>：无需死记所有命令选项，重点理解“日志轮替/集中管理/持久化”“定时任务周期逻辑”的原理；能根据日志路径/配置文件识别轮替规则，通过&lt;code>journalctl&lt;/code>/&lt;code>cron&lt;/code>日志定位问题。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>后续难度提示&lt;/strong>：当前为基础阶段，后续还有3-4个阶段，难度递增（如“命令写文件”“网络服务配置”）；若当前基础不扎实，后续排查网络服务错误、理解复杂命令（如&lt;code>qpqqr&lt;/code>）会更困难，需及时记忆细节（如日志级别、&lt;code>crontab&lt;/code>格式）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>复习心态&lt;/strong>：复习中发现知识漏洞（如配置项含义遗忘）是正常现象，需通过“实操+复盘”强化记忆；时间不充裕，建议每日花30分钟整理当天命令示例，避免堆积问题。&lt;/p>
&lt;/li>
&lt;/ol></description></item><item><title>Linux LVM、备份与数据传输</title><link>https://luckycloveryh.github.io/fuyou/p/linux-06/</link><pubDate>Fri, 21 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-06/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-06/1200/675" alt="Featured image of post Linux LVM、备份与数据传输" />&lt;h1 id="视频与系统操作核心内容合并总结">视频与系统操作核心内容合并总结
&lt;/h1>&lt;h2 id="一lvm逻辑卷管理存储管理核心">一、LVM（逻辑卷管理）：存储管理核心
&lt;/h2>&lt;h3 id="一核心概念与优势">（一）核心概念与优势
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>LVM与标准分区的差异&lt;/strong>
&lt;ul>
&lt;li>标准分区局限：无法动态扩容，空间调整需修改扇区数量；扩容空间仅限当前硬盘，无法跨硬盘补充（如Windows C盘无法直接用新硬盘空间扩容）。&lt;/li>
&lt;li>LVM核心优势：支持动态扩容，无需卸载分区、不影响原有数据；可跨硬盘整合空间，将新硬盘加入卷组为逻辑卷补充容量，突破单硬盘限制。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>关键组件与层级关系&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>物理卷（PV）&lt;/strong>：最底层存储单元，由硬盘或分区转换而来（仅需命令标识，无硬件本质变化），是LVM管理的基础。&lt;/li>
&lt;li>&lt;strong>卷组（VG）&lt;/strong>：由1个及以上物理卷组成，空间大小取决于物理卷总容量，类似MBR扩展分区，仅管理空间，需划分逻辑卷后才能存储数据。&lt;/li>
&lt;li>&lt;strong>逻辑卷（LV）&lt;/strong>：基于卷组划分，功能等同于标准分区，需格式化并挂载后使用，是用户实际访问数据的“分区”形式。&lt;/li>
&lt;li>&lt;strong>最小存储单元&lt;/strong>：PE（物理扩展块）默认4MB，避免非整数容量分配损耗（如按PE数量指定逻辑卷大小，充分利用卷组空间）；逻辑卷内对应LE（逻辑扩展块），与PE大小一致、一一对应，格式化后以4K块为最小单元，LE实际使用场景较少。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>系统安装特殊配置&lt;/strong>
&lt;ul>
&lt;li>&lt;code>/boot&lt;/code>分区必须为标准分区：因&lt;code>/boot&lt;/code>存储系统启动文件及LVM驱动，若为LVM分区，启动时需先加载驱动但驱动存于&lt;code>/boot&lt;/code>，会陷入“无钥匙开锁”死循环。&lt;/li>
&lt;li>典型方案：单硬盘分2个标准分区，1个作为&lt;code>/boot&lt;/code>，另1个转换为物理卷，组成卷组后划分&lt;code>/&lt;/code>（根分区）和&lt;code>swap&lt;/code>等逻辑卷。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="二lvm完整操作流程">（二）LVM完整操作流程
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>&lt;strong>前期操作：查看与验证&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>设备查看：登录系统后用&lt;code>lsblk&lt;/code>查看硬盘及分区，LVM设备以卷组名（如VG01）、逻辑卷名（如LV01）标识，类型显示为LVM。&lt;/li>
&lt;li>组件信息查看：
&lt;ul>
&lt;li>&lt;code>pvs&lt;/code>：查看物理卷使用情况（如SDA物理卷总空间近20GB，已用15GB，剩余5GB）。&lt;/li>
&lt;li>&lt;code>vgdisplay VG01&lt;/code>：查看卷组详情（VG01总空间与物理卷对应，显示剩余空间）。&lt;/li>
&lt;li>&lt;code>lvdisplay /dev/VG01/LV01&lt;/code>：查看逻辑卷细节（如LV01大小15GB）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>格式化与挂载：初始LV01未格式化，直接挂载失败，需执行&lt;code>mkfs&lt;/code>（如&lt;code>mkfs.xfs /dev/VG01/LV01&lt;/code>）格式化，再挂载到&lt;code>/DISC1&lt;/code>（&lt;code>mount /dev/VG01/LV01 /DISC1&lt;/code>）；用&lt;code>mount | grep DISC1&lt;/code>和&lt;code>df -h | grep DISC1&lt;/code>验证挂载状态与空间。&lt;/li>
&lt;li>文件操作与空间验证：将&lt;code>/boot&lt;/code>目录内容复制到&lt;code>/DISC1&lt;/code>（&lt;code>cp -r /boot/* /DISC1/&lt;/code>），通过&lt;code>df -h&lt;/code>查看&lt;code>/DISC1&lt;/code>的空间占用变化，用&lt;code>ls /DISC1&lt;/code>确认复制的文件及子目录完整性。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>扩容操作：动态扩展空间&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>扩容前提：先通过&lt;code>vgdisplay VG01&lt;/code>判断卷组剩余空间，若LV01从15GB扩至17GB（VG01剩余空间足够）可直接操作；扩至25GB（空间不足）需先扩卷组。&lt;/li>
&lt;li>卷组扩容：将新硬盘/分区（如NVME0N2）加入卷组，执行&lt;code>vgextend VG01 /dev/nvme0n2&lt;/code>，系统自动将设备转为物理卷并加入，无需提前执行&lt;code>pvcreate&lt;/code>；扩容后用&lt;code>vgdisplay VG01&lt;/code>验证可用空间增加（如至24.09GB）。&lt;/li>
&lt;li>逻辑卷扩容：
&lt;ul>
&lt;li>命令格式：两种指定大小方式，一是增量扩容（&lt;code>lvextend -L +10G /dev/VG01/LV01&lt;/code>，从15GB→25GB），二是直接指定目标大小（&lt;code>lvextend -L 25G /dev/VG01/LV01&lt;/code>）；执行前需确认卷组可用空间充足。&lt;/li>
&lt;li>文件系统识别：XFS文件系统需执行&lt;code>xfs_growfs /dev/VG01/LV01&lt;/code>，或在&lt;code>lvextend&lt;/code>后加&lt;code>-r&lt;/code>选项一次性完成（&lt;code>lvextend -L 27G -r /dev/VG01/LV01&lt;/code>，25GB→27GB），EXT系列文件系统需用&lt;code>resize2fs&lt;/code>。&lt;/li>
&lt;li>验证：用&lt;code>pvs&lt;/code>、&lt;code>vgdisplay&lt;/code>、&lt;code>lvdisplay&lt;/code>、&lt;code>df -h&lt;/code>分别确认PV、VG、LV及文件系统的空间变化。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>缩减操作：注意数据安全&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>注意事项：需先卸载逻辑卷（&lt;code>umount /DISC1&lt;/code>），避免挂载状态操作；缩减可能需重新格式化（会清空数据），部分9版本系统需用&lt;code>wipefs -a /dev/VG01/LV01&lt;/code>清空逻辑卷的文件系统元数据（未扩容过的LV可能无需此操作）。&lt;/li>
&lt;li>流程：
&lt;ul>
&lt;li>卸载：&lt;code>umount /DISC1&lt;/code>。&lt;/li>
&lt;li>缩减尝试：&lt;code>lvreduce -L 10G /dev/VG01/LV01&lt;/code>，若提示元数据错误，执行&lt;code>wipefs -a /dev/VG01/LV01&lt;/code>后重试。&lt;/li>
&lt;li>重新格式化与挂载：执行&lt;code>mkfs.xfs /dev/VG01/LV01&lt;/code>（或对应文件系统格式）格式化，再挂载回&lt;code>/DISC1&lt;/code>（&lt;code>mount /dev/VG01/LV01 /DISC1&lt;/code>），&lt;code>df -h&lt;/code>显示空间为10GB（原有数据丢失）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>数据建议：缩减前必须备份数据（如用&lt;code>dd&lt;/code>或&lt;code>cp&lt;/code>备份&lt;code>/DISC1&lt;/code>内容），完成后还原；日常需求中缩减较少，仅空间过大时使用。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>数据移动（PV迁移）：跨物理卷转移数据&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>前提：卷组中至少存在2个物理卷（如VG01含SDA（已用10GB）和NVME0N2（未用））。&lt;/li>
&lt;li>操作：执行&lt;code>pvmove /dev/sda&lt;/code>，系统自动将SDA上的LVM数据迁移到同卷组的其他可用PV（此处为NVME0N2）；迁移过程需等待，完成后用&lt;code>pvs&lt;/code>查看SDA空间释放（已用空间变为0），NVME0N2占用10GB。&lt;/li>
&lt;li>物理卷移除：迁移完成后，执行&lt;code>vgreduce VG01 /dev/sda&lt;/code>，将SDA从VG01卷组中移除，SDA可后续用于其他用途。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>组件删除：逆序操作&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>删除顺序：与创建顺序相反，需先卸载→删LV→删VG→删PV：
&lt;ul>
&lt;li>卸载：&lt;code>umount /DISC1&lt;/code>（确保无进程占用该挂载点，可通过&lt;code>fuser -m /DISC1&lt;/code>查看）。&lt;/li>
&lt;li>删LV：&lt;code>lvremove /dev/VG01/LV01&lt;/code>，执行时需输入&lt;code>y&lt;/code>确认删除。&lt;/li>
&lt;li>删VG：&lt;code>vgremove VG01&lt;/code>，需确保卷组内无逻辑卷残留。&lt;/li>
&lt;li>删PV：&lt;code>pvremove /dev/sda /dev/nvme0n2&lt;/code>，清除物理卷的LVM标识。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>使用建议：日常无需删除PV，删除LV后卷组释放的空间可用于划分新LV；无需将PV还原为普通硬盘（普通硬盘无LVM动态扩容能力）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="二数据备份与传输保障数据安全">二、数据备份与传输：保障数据安全
&lt;/h2>&lt;h3 id="一备份策略三种核心类型">（一）备份策略：三种核心类型
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>备份类型&lt;/th>
&lt;th>定义&lt;/th>
&lt;th>优缺点&lt;/th>
&lt;th>适用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>完全备份&lt;/td>
&lt;td>完整保存所有需备份的数据（如首次备份1-3章内容，后续备份1-6章、1-9章时，均完整备份对应阶段所有数据）&lt;/td>
&lt;td>优点：恢复简单（直接用备份文件覆盖或替换硬盘即可）；缺点：备份数据量大、耗时久、系统资源消耗多，存在大量重复数据，无法每日高频执行&lt;/td>
&lt;td>初始备份、重要数据定期全量保存（如每周一次）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>增量备份&lt;/td>
&lt;td>仅备份与上一次备份相比新增或修改的数据（如首次备份1-3章，后续备份4-6章时含1-3章中修改的文件，备份7-9章时含前序修改文件）&lt;/td>
&lt;td>优点：无大量重复数据，备份数据量小、效率高，适合高频备份；缺点：恢复需按备份顺序依次进行（如恢复1-9章需先恢复1-3章、再4-6章、最后7-9章），易因顺序错误导致旧文件覆盖新文件&lt;/td>
&lt;td>日常高频备份（如每日一次），数据增量较少的场景（如办公文档、日志文件）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>差异备份&lt;/td>
&lt;td>仅备份与首次完全备份相比新增或修改的数据（如首次备份1-3章，备份1-6章时存4-6章，备份1-9章时存4-9章，始终以首次备份为基准）&lt;/td>
&lt;td>优点：恢复简单（仅需首次完全备份+最后一次差异备份，如恢复1-9章需1-3章+4-9章）；缺点：后续备份数据量逐渐增大（但小于完全备份）&lt;/td>
&lt;td>初始数据量大、后续增长量小的场景（如系统配置文件、大型项目文档），兼顾效率与恢复便捷性&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二核心备份工具与操作">（二）核心备份工具与操作
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>&lt;strong>dd命令：本地完全备份利器&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>基础功能：用于本地完全备份，可操作文件、分区、整盘，需通过&lt;code>if&lt;/code>（数据来源）和&lt;code>of&lt;/code>（数据去向）指定路径；操作逻辑类似&lt;code>cp&lt;/code>，但&lt;code>cp&lt;/code>更简洁，日常单个文件复制优先用&lt;code>cp&lt;/code>，&lt;code>dd&lt;/code>更适合分区/整盘级备份。&lt;/li>
&lt;li>实操案例：将&lt;code>/etc&lt;/code>下的&lt;code>hosts&lt;/code>文件备份到&lt;code>/root/abcd&lt;/code>，执行&lt;code>dd if=/etc/hosts of=/root/abcd&lt;/code>；备份后用&lt;code>cat /root/abcd&lt;/code>与&lt;code>cat /etc/hosts&lt;/code>验证内容一致性，用&lt;code>ls -i&lt;/code>查看两者&lt;code>inode&lt;/code>号不同（因&lt;code>dd&lt;/code>会创建新文件）。&lt;/li>
&lt;li>分区/硬盘备份特性：
&lt;ul>
&lt;li>分区备份：完整读取分区的所有扇区范围（含文件系统），即使原分区实际占用空间小，也会按分区总容量写入目标分区（如&lt;code>dd if=/dev/nvme0n1p1 of=/dev/nvme0n2p1&lt;/code>）。&lt;/li>
&lt;li>整盘备份：包含硬盘的分区表信息，目标硬盘执行后会自动生成与原硬盘一致的分区及文件系统（如&lt;code>dd if=/dev/nvme0n1 of=/dev/nvme0n3&lt;/code>），需确保目标硬盘容量不小于源硬盘。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>特殊问题解决：
&lt;ul>
&lt;li>UUID冲突：备份后目标分区/硬盘因&lt;code>UUID&lt;/code>重复无法挂载，执行&lt;code>mount -o nouuid /dev/目标分区 /挂载点&lt;/code>（如&lt;code>mount -o nouuid /dev/nvme0n2p1 /DISC1&lt;/code>）忽略&lt;code>UUID&lt;/code>校验。&lt;/li>
&lt;li>未格式化却有文件系统：目标分区未执行&lt;code>mkfs&lt;/code>却显示有文件系统，多为从其他分区备份而来（&lt;code>dd&lt;/code>复制了源分区的文件系统信息），直接用&lt;code>mount -o nouuid&lt;/code>挂载即可。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>数据恢复方式：
&lt;ul>
&lt;li>挂载恢复：将备份分区/文件挂载到系统（支持非&lt;code>iso&lt;/code>文件，如&lt;code>dd&lt;/code>生成的&lt;code>.img&lt;/code>文件，需用&lt;code>mount -o loop,nouuid /root/backup.img /DISC1&lt;/code>），提取所需文件到原路径。&lt;/li>
&lt;li>直接覆盖恢复：颠倒&lt;code>dd&lt;/code>的&lt;code>if&lt;/code>和&lt;code>of&lt;/code>路径（如&lt;code>dd if=/root/backup.img of=/dev/nvme0n1p1&lt;/code>），适用于大数据量恢复，但会覆盖源分区所有数据，执行前需确认。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>其他用法：
&lt;ul>
&lt;li>创建指定大小文件：结合&lt;code>bs&lt;/code>（数据块大小）和&lt;code>count&lt;/code>（数据块数量），如&lt;code>dd if=/dev/zero of=/root/test.d bs=1M count=10&lt;/code>（创建10MB空文件），&lt;code>/dev/zero&lt;/code>会持续输出空字节。&lt;/li>
&lt;li>防数据恢复：反复用&lt;code>dd&lt;/code>写满硬盘再删除（&lt;code>dd if=/dev/zero of=/dev/目标硬盘&lt;/code>），覆盖硬盘所有扇区，避免已删除数据被工具恢复（敏感数据销毁场景常用）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>scp命令：服务器间文件传输与备份&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>功能定位：基于&lt;code>ssh&lt;/code>协议的文件传输工具，支持跨服务器上传/下载，可用于简单的服务器间数据备份，需确保源与目标服务器开启&lt;code>ssh&lt;/code>服务（&lt;code>systemctl status sshd&lt;/code>查看）。&lt;/li>
&lt;li>实操格式：
&lt;ul>
&lt;li>上传（本地→远程）：&lt;code>scp 本地文件路径 目标用户@目标IP:目标路径&lt;/code>（如&lt;code>scp /root/photorec.ser root@192.168.5.141:/root&lt;/code>），需替换为实际的文件路径、用户、IP。&lt;/li>
&lt;li>下载（远程→本地）：&lt;code>scp 目标用户@目标IP:远程文件路径 本地路径&lt;/code>（如&lt;code>scp root@192.168.5.141:/root/photorec.ser /root&lt;/code>），若远程路径含空格，需用引号包裹（如&lt;code>&amp;quot;root@192.168.5.141:/root/photo rec.ser&amp;quot;&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>执行细节：首次连接目标服务器会提示指纹验证，输入&lt;code>yes&lt;/code>确认；后续需输入目标用户的密码（若配置SSH密钥可免密登录）；确保目标用户对目标路径有写入权限（如存&lt;code>/root&lt;/code>需用&lt;code>root&lt;/code>身份，普通用户需存于自身&lt;code>/home&lt;/code>目录）。&lt;/li>
&lt;li>类比理解：上传对应“网盘存文件”（本地文件传至远程服务器），下载对应“网盘取文件”（远程服务器文件传至本地），可用于小型文件的跨服务器备份。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>其他备份工具说明&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>&lt;code>sfsw&lt;/code>：原计划讲解的本地备份工具，支持增量备份与差异备份，但仅能在单服务器本地使用，无法满足跨服务器、异地备份需求，实际工作中极少用到，无需记忆命令细节。&lt;/li>
&lt;li>&lt;code>rsync&lt;/code>：工作中常用的异地备份工具，兼具命令与服务属性，可实现跨服务器、异地的增量备份（仅传输变化数据），支持断点续传，兼顾效率与数据安全性，是生产环境中异地备份的首选工具之一。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="三重要数据识别需重点备份的目录">（三）重要数据识别：需重点备份的目录
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>/root目录&lt;/strong>：root用户的默认登录与工作目录，创建系统管理文件、远程传输核心数据时使用频繁，易积累关键操作数据（如脚本、配置备份），需重点备份。&lt;/li>
&lt;li>&lt;strong>/home目录&lt;/strong>：普通用户的工作目录，日常办公、应用操作产生的用户数据（如文档、项目文件）多存于此；装系统时建议将&lt;code>/home&lt;/code>单独划分为独立分区，便于后续针对性备份，避免与系统分区数据混淆。&lt;/li>
&lt;li>&lt;strong>/etc目录&lt;/strong>：系统及所有服务的配置文件存放目录，包含网卡配置（&lt;code>/etc/sysconfig/network-scripts/&lt;/code>）、SSH服务配置（&lt;code>/etc/ssh/&lt;/code>）、挂载配置（&lt;code>/etc/fstab&lt;/code>）、用户权限配置（&lt;code>/etc/passwd&lt;/code>、&lt;code>/etc/group&lt;/code>）等，备份该目录可在系统重装后快速还原系统与服务设置。&lt;/li>
&lt;li>&lt;strong>服务数据&lt;/strong>：无固定存放路径，需根据实际部署的服务确定，如Apache/Nginx的网站根目录（默认&lt;code>/var/www/html&lt;/code>）、MySQL数据库数据目录（默认&lt;code>/var/lib/mysql&lt;/code>）、日志文件目录（&lt;code>/var/log/&lt;/code>）等，需结合具体服务的配置文件确认路径后备份。&lt;/li>
&lt;/ol>
&lt;h2 id="三文件系统损坏与修复故障处理">三、文件系统损坏与修复：故障处理
&lt;/h2>&lt;h3 id="一损坏方式模拟故障">（一）损坏方式：模拟故障
&lt;/h3>&lt;p>卸载目标分区（如&lt;code>umount /DISC1&lt;/code>）后，用&lt;code>dd if=/dev/zero of=/dev/目标分区&lt;/code>直接覆盖分区底层数据（从分区的起始扇区（如2048）开始，覆盖量由&lt;code>bs&lt;/code>和&lt;code>count&lt;/code>控制，如&lt;code>dd if=/dev/zero of=/dev/nvme0n2p1 bs=1K count=50&lt;/code>覆盖前50KB）；该操作未经过文件系统挂载点，数据覆盖不被文件系统记录，可模拟文件系统损坏（如超级块损坏、元数据丢失）。&lt;/p>
&lt;h3 id="二修复工具与结果fsck命令">（二）修复工具与结果：fsck命令
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>修复命令与前提&lt;/strong>：使用&lt;code>fsck&lt;/code>工具修复（如&lt;code>fsck /dev/nvme0n2p1&lt;/code>），修复前必须卸载目标分区（&lt;code>umount /DISC1&lt;/code>），避免挂载状态下操作导致数据进一步损坏；&lt;code>fsck&lt;/code>需指定文件系统类型时，可加&lt;code>-t&lt;/code>选项（如&lt;code>fsck -t xfs /dev/nvme0n2p1&lt;/code>）。&lt;/li>
&lt;li>&lt;strong>修复结果分类&lt;/strong>：
&lt;ul>
&lt;li>损坏数据量小（如覆盖前50KB）：修复后文件系统可正常挂载，所有文件完整保留；执行&lt;code>mount /dev/nvme0n2p1 /DISC1&lt;/code>后，用&lt;code>ls /DISC1&lt;/code>可查看所有原文件。&lt;/li>
&lt;li>损坏数据量中等（如覆盖前500KB）：文件系统可修复，但部分文件可能丢失或仅找回数据片段；找回的片段会默认存于分区的&lt;code>lost+found&lt;/code>目录（需用&lt;code>ls /DISC1/lost+found&lt;/code>查看），片段文件名以数字命名，需手动识别内容用途。&lt;/li>
&lt;li>损坏数据量极大（如覆盖分区总容量的50%以上）：
&lt;ul>
&lt;li>部分7版本系统：会自动尝试重新格式化目标分区，修复后可正常挂载，但原文件全部消失（等同于新分区）。&lt;/li>
&lt;li>部分9版本系统：直接提示“修复失败”，需手动执行&lt;code>mkfs&lt;/code>强制格式化（如&lt;code>mkfs.xfs -f /dev/nvme0n2p1&lt;/code>）；若格式化提示“元数据残留”，先执行&lt;code>wipefs -a /dev/nvme0n2p1&lt;/code>清空残留元数据，再重新格式化。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>影响因素&lt;/strong>：分区大小决定损坏效果——分区越小，覆盖的数据越容易波及核心文件（如&lt;code>/etc&lt;/code>下的配置文件）；分区越大，文件系统元数据（如超级块、inode表）占用的空间越多，精准覆盖目标文件数据的概率越低，部分文件更易保留。&lt;/li>
&lt;/ol>
&lt;h2 id="四dnf管理多linux内核系统稳定性保障">四、dnf管理多Linux内核：系统稳定性保障
&lt;/h2>&lt;h3 id="一安装多个内核版本">（一）安装多个内核版本
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>安装默认内核&lt;/strong>：执行&lt;code>sudo dnf install kernel&lt;/code>，从系统默认仓库安装最新稳定版内核，安装后会自动保留当前运行的旧内核（避免新内核故障导致系统无法启动）。&lt;/li>
&lt;li>&lt;strong>安装特定版本内核&lt;/strong>：执行&lt;code>sudo dnf install kernel-&amp;lt;完整版本号&amp;gt;&lt;/code>（如&lt;code>sudo dnf install kernel-5.14.0-284.30.1.el9_2.x86_64&lt;/code>），需先通过&lt;code>sudo dnf list kernel --showduplicates&lt;/code>查看仓库中可用的完整版本号。&lt;/li>
&lt;li>&lt;strong>安装测试/LTS内核（可选）&lt;/strong>：适用于需要测试新功能或长期稳定支持的场景，需先安装第三方源（如elrepo）：
&lt;ul>
&lt;li>安装elrepo源：&lt;code>sudo dnf install https://www.elrepo.org/elrepo-release-$(rpm -E %rhel).el$(rpm -E %rhel).noarch.rpm&lt;/code>，安装前需确保系统已联网且具备&lt;code>sudo&lt;/code>权限。&lt;/li>
&lt;li>安装最新mainline内核（最新功能测试）：&lt;code>sudo dnf --enablerepo=elrepo-kernel install kernel-ml&lt;/code>（&lt;code>ml&lt;/code>即mainline）。&lt;/li>
&lt;li>安装长期支持（LTS）内核（稳定性优先）：&lt;code>sudo dnf --enablerepo=elrepo-kernel install kernel-lt&lt;/code>（&lt;code>lt&lt;/code>即long-term）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="二管理已安装内核">（二）管理已安装内核
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>查看已安装内核&lt;/strong>：
&lt;ul>
&lt;li>查看所有已安装版本：&lt;code>dnf list installed kernel&lt;/code>，输出结果含内核版本、架构、安装时间。&lt;/li>
&lt;li>查看当前运行内核：&lt;code>uname -r&lt;/code>，仅显示正在使用的内核版本（如&lt;code>5.14.0-284.30.1.el9_2.x86_64&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>查看所有可用内核&lt;/strong>：执行&lt;code>dnf list kernel --showduplicates&lt;/code>，列出系统仓库中所有可用的内核版本（含已安装与未安装），便于选择特定版本安装。&lt;/li>
&lt;li>&lt;strong>删除旧内核&lt;/strong>：
&lt;ul>
&lt;li>删除特定旧版本：&lt;code>sudo dnf remove kernel-&amp;lt;旧版本号&amp;gt;&lt;/code>（如&lt;code>sudo dnf remove kernel-5.14.0-284.26.1.el9_2.x86_64&lt;/code>），需确保删除的不是当前运行内核。&lt;/li>
&lt;li>自动保留最新版本：执行&lt;code>sudo dnf remove --oldinstallonly kernel&lt;/code>，仅删除超出“保留数量”的旧内核，默认保留3个最新版本。&lt;/li>
&lt;li>重要提醒：至少保留2个可用内核（当前运行内核+1个最新备份内核），避免所有内核损坏导致系统无法启动。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="三配置内核保留策略">（三）配置内核保留策略
&lt;/h3>&lt;ul>
&lt;li>配置文件路径：&lt;code>/etc/dnf/dnf.conf&lt;/code>（RHEL 8/9、CentOS Stream等系统，旧系统为&lt;code>/etc/yum.conf&lt;/code>）。&lt;/li>
&lt;li>关键配置项：
&lt;ul>
&lt;li>&lt;code>installonly_limit=3&lt;/code>：设置系统最多保留的内核版本数量，默认3个，可根据需求调整（如生产环境建议设为2-3个）。&lt;/li>
&lt;li>&lt;code>installonlypkgs=kernel,kernel-devel,kernel-headers&lt;/code>：指定需按“保留数量”管理的包，确保内核及相关开发包、头文件同步保留多版本。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>修改方式：用文本编辑器打开配置文件（如&lt;code>sudo nano /etc/dnf/dnf.conf&lt;/code>），修改后保存退出，下次&lt;code>dnf&lt;/code>操作时自动生效。&lt;/li>
&lt;/ul>
&lt;h3 id="四保留多内核的意义">（四）保留多内核的意义
&lt;/h3>&lt;ul>
&lt;li>系统回退：当新版本内核出现兼容性问题（如硬件驱动失效、系统崩溃）时，可在开机GRUB菜单中选择旧内核启动，快速恢复系统正常运行。&lt;/li>
&lt;li>驱动兼容：部分老旧硬件（如工业设备、专用卡）的驱动仅支持特定内核版本，保留对应内核可确保硬件正常工作。&lt;/li>
&lt;li>测试新功能：在生产环境部署新内核前，可在测试环境保留新旧内核，对比测试新内核的功能与稳定性，降低升级风险。&lt;/li>
&lt;li>生产环境建议：平衡稳定性与更新需求，保留2-3个内核版本（当前运行+1-2个备份），定期清理过旧的内核版本（如超过6个月未使用），释放&lt;code>/boot&lt;/code>分区空间。&lt;/li>
&lt;/ul>
&lt;h2 id="五课程重点与学习安排">五、课程重点与学习安排
&lt;/h2>&lt;h3 id="一核心重点内容">（一）核心重点内容
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>LVM相关&lt;/strong>：重点掌握组件关系（PV→VG→LV的层级、PE的作用）、动态扩容完整流程（卷组扩容→逻辑卷扩容→文件系统识别）、核心命令（&lt;code>pvcreate&lt;/code>/&lt;code>vgcreate&lt;/code>/&lt;code>lvcreate&lt;/code>/&lt;code>vgextend&lt;/code>/&lt;code>lvextend&lt;/code>/&lt;code>wipefs&lt;/code>）；硬盘缩减非重点，仅需了解操作风险与前提。&lt;/li>
&lt;li>&lt;strong>数据备份&lt;/strong>：熟记三种备份策略（完全/增量/差异）的定义、优缺点及适用场景，掌握&lt;code>dd&lt;/code>命令的核心参数（&lt;code>if&lt;/code>/&lt;code>of&lt;/code>/&lt;code>bs&lt;/code>/&lt;code>count&lt;/code>）与实操场景（分区备份、指定大小文件创建）、&lt;code>scp&lt;/code>命令的上传/下载格式，了解&lt;code>rsync&lt;/code>的工具特性（异地增量备份）。&lt;/li>
&lt;li>&lt;strong>文件系统&lt;/strong>：掌握&lt;code>fsck&lt;/code>修复的前提（卸载分区）与操作流程，理解&lt;code>dd&lt;/code>模拟文件系统损坏的原理，熟记UUID冲突的解决方法（&lt;code>mount -o nouuid&lt;/code>），了解不同版本系统修复结果的差异。&lt;/li>
&lt;li>&lt;strong>内核管理&lt;/strong>：掌握dnf安装/删除多内核的命令，理解内核保留策略的配置方式（&lt;code>/etc/dnf/dnf.conf&lt;/code>），明确保留多内核的意义与生产环境建议。&lt;/li>
&lt;/ol>
&lt;h3 id="二学习要求与安排">（二）学习要求与安排
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>复习任务&lt;/strong>：
&lt;ul>
&lt;li>范围：周末重点复习第2-7章内容，第1章（分区基础）需掌握MBR/GPT分区的概念、MBR最多4个主分区的限制、分区大小规范（如MBR支持最大2TB硬盘），无需深入复杂操作。&lt;/li>
&lt;li>方法：结合虚拟机实操验证理论（如创建LVM卷组、执行&lt;code>dd&lt;/code>备份分区、用&lt;code>fsck&lt;/code>修复损坏分区），重点巩固命令的参数含义与执行顺序，避免死记硬背。&lt;/li>
&lt;li>频次：每周重复复习已学内容，确保LVM扩容、&lt;code>dd&lt;/code>备份等核心操作能独立完成，无需参考文档。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>考试安排&lt;/strong>：
&lt;ul>
&lt;li>形式：阶段考试包含笔试（理论知识，占50%）与实操（动手操作，占50%）。&lt;/li>
&lt;li>重点：笔试侧重备份策略差异、LVM组件关系、内核保留意义；实操侧重LVM动态扩容、&lt;code>scp&lt;/code>跨服务器传输、&lt;code>fsck&lt;/code>修复文件系统。&lt;/li>
&lt;li>建议：优先投入实操练习，通过反复操作强化命令记忆与流程理解，理论知识结合实操场景记忆（如“增量备份的恢复顺序”可结合&lt;code>scp&lt;/code>下载备份文件的步骤理解）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>后续课程预告&lt;/strong>：
&lt;ul>
&lt;li>内容：下一章讲解“进程管理”，核心概念为“进程是正在运行的程序”，具有抽象性（无物理实体，仅能通过工具查看信息，如&lt;code>ps&lt;/code>/&lt;code>top&lt;/code>）。&lt;/li>
&lt;li>安排：课程将补充进程的状态分类（如运行态、睡眠态）、进程优先级调整、进程管理命令（&lt;code>ps&lt;/code>/&lt;code>top&lt;/code>/&lt;code>kill&lt;/code>），并提供简单程序（如Shell脚本）验证进程行为；因概念抽象，且下午课程易犯困，强调中午需充分休息，避免影响学习效果。&lt;/li>
&lt;li>重要性：进程管理是系统运维的核心内容，若未掌握，后续服务管理（如&lt;code>systemctl&lt;/code>）、资源监控等内容将难以理解，需重点关注。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>实操建议&lt;/strong>：
&lt;ul>
&lt;li>预习：无需预习新内容，重点消化已学知识点（如LVM命令的参数差异、&lt;code>dd&lt;/code>与&lt;code>cp&lt;/code>的适用场景区别）。&lt;/li>
&lt;li>练习：当前课程已完成LVM逻辑卷划分，扩容、数据迁移等操作将在后续课程展开，周末可先在虚拟机中创建LVM组件（PV→VG→LV），熟悉命令执行反馈（如&lt;code>vgdisplay&lt;/code>的输出字段含义）。&lt;/li>
&lt;li>笔记：整理核心操作的步骤流程图（如LVM扩容流程：查看卷组空间→扩展卷组→扩展逻辑卷→扩展文件系统→验证），便于复习时快速回顾。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol></description></item><item><title>Linux 数据恢复、RAID 与磁盘管理</title><link>https://luckycloveryh.github.io/fuyou/p/linux-05/</link><pubDate>Thu, 20 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-05/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-05/1200/675" alt="Featured image of post Linux 数据恢复、RAID 与磁盘管理" />&lt;h1 id="视频内容总结">视频内容总结
&lt;/h1>&lt;h2 id="一误删除数据恢复">一、误删除数据恢复
&lt;/h2>&lt;h3 id="1-模拟误删除环境准备">1. 模拟误删除环境准备
&lt;/h3>&lt;p>（1）基于已有0N2硬盘（剩余近10GB空间），完成分区操作（需熟练掌握中英文分区显示下的操作方法），通过&lt;code>fspl&lt;/code>命令保存退出后，使用&lt;code>mkfs&lt;/code>工具对0N2P2分区进行格式化（原“iPad s”为笔误，应为格式化相关命令）。
（2）格式化完成后，创建挂载点（示例：&lt;code>mkdir /mnt/3&lt;/code>），将0N2P2分区挂载至该目录，通过&lt;code>df -h&lt;/code>命令查看分区使用状态：初始占用约100MB，占比2%，挂载目录默认为空。
（3）向挂载目录复制系统常见关键文件（如&lt;code>/etc/fstab&lt;/code>、&lt;code>/etc/passwd&lt;/code>、&lt;code>/etc/shadow&lt;/code>、&lt;code>/etc/group&lt;/code>等），需明确知晓各文件用途及内容，为后续识别恢复文件提供依据。&lt;/p>
&lt;h3 id="2-数据恢复原理">2. 数据恢复原理
&lt;/h3>&lt;p>（1）文件删除的核心逻辑：仅断开文件名称与inode的关联，同时在磁盘位图中标记文件占用的块为“可用”，文件实际数据仍保留在磁盘块中，未被立即覆盖。
（2）恢复工具工作机制：通过扫描磁盘块，匹配不同文件类型特有的开头数据标识，定位文件数据起始块后，连续读取后续关联块以还原数据，但无法恢复原文件名称。&lt;/p>
&lt;h3 id="3-恢复工具操作">3. 恢复工具操作
&lt;/h3>&lt;h4 id="1工具选择与安装">（1）工具选择与安装
&lt;/h4>&lt;ul>
&lt;li>工具名称：&lt;code>photorec&lt;/code>（全称为&lt;code>photo recovery&lt;/code>，简写形式），所属安装包为&lt;code>testdisk&lt;/code>，需通过EPEL扩展源安装。&lt;/li>
&lt;li>安装步骤：
&lt;ol>
&lt;li>确保系统DNF源为默认网络源（本地源无法下载扩展包）；&lt;/li>
&lt;li>安装EPEL源：&lt;code>dnf install -y epel-release&lt;/code>；&lt;/li>
&lt;li>安装&lt;code>testdisk&lt;/code>包：&lt;code>dnf install -y testdisk&lt;/code>（安装后自动包含&lt;code>photorec&lt;/code>工具）。&lt;/li>
&lt;/ol>
&lt;/li>
&lt;/ul>
&lt;h4 id="2恢复操作流程">（2）恢复操作流程
&lt;/h4>&lt;ol>
&lt;li>执行&lt;code>photorec&lt;/code>命令，选择需恢复数据的目标分区（示例：&lt;code>/dev/sda0N2P2&lt;/code>）；&lt;/li>
&lt;li>进入交互界面后，按默认光标位置选择“继续”，根据分区实际文件系统类型选择对应选项（ext系列文件系统选下方选项，NTFS等选上方选项）；&lt;/li>
&lt;li>恢复路径默认设为当前目录，恢复文件会存储在自动生成的&lt;code>RECUP_DIR&lt;/code>目录中；&lt;/li>
&lt;li>输入字母&lt;code>c&lt;/code>启动恢复进程，等待完成后按&lt;code>q&lt;/code>退出；&lt;/li>
&lt;li>恢复文件以&lt;code>.tst&lt;/code>为后缀，可通过&lt;code>cat&lt;/code>等命令查看文件内容，识别原文件（示例：&lt;code>F00008.tst&lt;/code>对应&lt;code>/etc/fstab&lt;/code>文件）。&lt;/li>
&lt;/ol>
&lt;h3 id="4-恢复局限性">4. 恢复局限性
&lt;/h3>&lt;p>（1）无100%恢复保障：即使专业数据恢复公司，也可能仅恢复数据片段，无法还原完整文件；
（2）数据块不连续场景：若文件创建后经多次修改、穿插其他文件操作，会导致数据块分布不连续，恢复时易出现文件片段化，强调日常数据备份的必要性，不可依赖数据恢复作为核心保障。&lt;/p>
&lt;h2 id="二磁盘阵列raid">二、磁盘阵列（RAID）
&lt;/h2>&lt;h3 id="1-基础概念">1. 基础概念
&lt;/h3>&lt;p>（1）定义：将多块物理硬盘组合为一个逻辑磁盘，核心价值是提升读写速度或实现数据容错，1978年由加州大学伯克利分校提出。
（2）核心特性：&lt;/p>
&lt;ul>
&lt;li>不同RAID级别对应不同存储架构，无级别高低之分，仅适配场景差异；&lt;/li>
&lt;li>一组RAID在操作系统中仅显示为一块逻辑磁盘，屏蔽底层多块物理硬盘的复杂性。&lt;/li>
&lt;/ul>
&lt;h3 id="2-常见raid级别核心参数">2. 常见RAID级别核心参数
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>参数&lt;/th>
&lt;th>具体说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>RAID 0（条带卷）&lt;/strong>&lt;/td>
&lt;td>- 硬盘数量要求：至少2块&lt;br>- 数据存储方式：以固定大小（常见64KB）切分文件，交替存储至多块硬盘&lt;br>- 读写速度：读写速度显著提升（理论随硬盘数量翻倍）&lt;br>- 容错能力：无容错机制，任意一块硬盘损坏则全部数据丢失&lt;br>- 磁盘利用率：100%（需硬盘容量一致，否则超出最小容量的部分无法参与条带化）&lt;br>- 适用场景：对速度要求极高、数据可实时备份的场景（如临时文件处理、高速缓存存储）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>RAID 1（镜像卷）&lt;/strong>&lt;/td>
&lt;td>- 硬盘数量要求：至少2块&lt;br>- 数据存储方式：文件完整复制到每块硬盘，无需条带化分割&lt;br>- 读写速度：写入速度无提升（需所有硬盘写入完成才算结束）；读取可分摊并发请求，提升访问承载能力&lt;br>- 容错能力：单盘容错：一块硬盘损坏，可从另一块硬盘读取完整数据（仅硬件容错，误删除会同步生效）&lt;br>- 磁盘利用率：50%（需硬盘容量一致，否则超出最小容量的部分无法参与镜像）&lt;br>- 适用场景：对数据安全性要求极高、并发读取频繁的场景（如服务器系统盘、核心配置文件存储）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>RAID 10（条带+镜像组合）&lt;/strong>&lt;/td>
&lt;td>- 硬盘数量要求：至少4块（需为偶数块，先分组组成RAID 1，再将两组RAID 1组合为RAID 0）&lt;br>- 数据存储方式：先对文件进行条带化分割，再将每段数据镜像存储至对应RAID 1组&lt;br>- 读写速度：读写速度高效（条带化优化传输效率）&lt;br>- 容错能力：跨组单盘容错：不同RAID 1组各坏一块硬盘可恢复；同组坏两块则数据丢失&lt;br>- 磁盘利用率：50%（受镜像机制限制，容量取两组RAID 1中最小单盘容量的总和）&lt;br>- 适用场景：对速度和安全性均有高要求的场景（如数据库服务器、核心业务数据存储）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>RAID 5（带校验条带卷）&lt;/strong>&lt;/td>
&lt;td>- 硬盘数量要求：至少3块&lt;br>- 数据存储方式：文件条带化存储，校验信息（奇偶校验）轮流分布于所有硬盘（避免单一硬盘过度承载校验写入）&lt;br>- 读写速度：读写速度较快（条带化+分布式校验均衡负载）&lt;br>- 容错能力：单盘容错：一块硬盘损坏，通过剩余数据与校验信息推算恢复；坏两块则数据丢失&lt;br>- 磁盘利用率：(n-1)/n（n为硬盘数量，1块硬盘容量用于存储校验信息）&lt;br>- 适用场景：兼顾速度、安全性与磁盘利用率的场景（如企业日常数据存储、办公系统数据盘）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="3-关键补充概念">3. 关键补充概念
&lt;/h3>&lt;h4 id="1预备磁盘">（1）预备磁盘
&lt;/h4>&lt;ul>
&lt;li>定义：一块或多块不参与RAID日常运行的备用硬盘，类似“汽车备胎”；&lt;/li>
&lt;li>作用：当RAID中某块硬盘损坏时，自动替换坏盘并同步数据，缩短故障处理时间，降低硬盘损坏期间的读取压力，减少二次数据丢失风险。&lt;/li>
&lt;/ul>
&lt;h4 id="2硬raid与软raid">（2）硬RAID与软RAID
&lt;/h4>&lt;ul>
&lt;li>硬RAID：基于独立磁盘阵列卡配置，不依赖操作系统，功能稳定、性能优，是工作中服务器的主流配置方式；&lt;/li>
&lt;li>软RAID：通过操作系统软件模拟实现，无法完全实现硬件级容错，性能受限，工作中极少使用，无需记忆相关配置命令（如&lt;code>mdadm&lt;/code>），重点掌握原理即可。&lt;/li>
&lt;/ul>
&lt;h4 id="3配置顺序">（3）配置顺序
&lt;/h4>&lt;ul>
&lt;li>新服务器部署流程：先配置RAID（如RAID 5、RAID 10），再将RAID逻辑盘作为存储介质，进行操作系统安装和文件系统格式化，底层RAID架构对系统透明。&lt;/li>
&lt;/ul>
&lt;h2 id="三学习与实践建议">三、学习与实践建议
&lt;/h2>&lt;ol>
&lt;li>规范专业术语使用：严格记忆标准术语（如“磁盘容错”“条带化”“镜像卷”等），避免自行命名，确保工作交流无歧义；&lt;/li>
&lt;li>优先掌握核心原理：RAID学习重点在于不同级别优缺点、适用场景及数据存储/容错逻辑，硬RAID配置多依赖硬件设备，无需死记软件命令；&lt;/li>
&lt;li>区分比喻与专业表述：日常可通过比喻（如“饮水机接水”理解RAID 0速度提升）辅助理解，但面试或工作沟通需使用行业标准术语，体现专业性。&lt;/li>
&lt;/ol></description></item><item><title>Linux 文件与目录权限管理</title><link>https://luckycloveryh.github.io/fuyou/p/linux-04/</link><pubDate>Wed, 19 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-04/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-04/1200/675" alt="Featured image of post Linux 文件与目录权限管理" />&lt;h1 id="linux系统权限管理完整总结">Linux系统权限管理完整总结
&lt;/h1>&lt;p>本内容围绕Linux系统权限管理展开，涵盖基础权限配置、传统权限方式的局限性、&lt;code>sudo&lt;/code>命令核心应用、管理员用户配置及工具适用场景，结合实操逻辑与示例，形成完整的权限管理知识体系：&lt;/p>
&lt;h2 id="一权限管理基础文件与目录的核心权限">一、权限管理基础：文件与目录的核心权限
&lt;/h2>&lt;h3 id="1-基础权限rwx的含义差异">1. 基础权限（r/w/x）的含义差异
&lt;/h3>&lt;p>&lt;code>r（读，4）、w（写，2）、x（执行，1）&lt;/code>是权限管理的基础，但其在文件和目录中的作用完全不同，是后续权限组合的核心依据：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>权限&lt;/th>
&lt;th>对文件的作用&lt;/th>
&lt;th>对目录的作用&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>r（读）&lt;/td>
&lt;td>查看文件内容（如&lt;code>cat&lt;/code>、&lt;code>less&lt;/code>）&lt;/td>
&lt;td>查看目录下的子文件/子目录名称（如&lt;code>ls&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>w（写）&lt;/td>
&lt;td>向文件写入、删除、修改内容（如&lt;code>echo&lt;/code>、&lt;code>vim&lt;/code>）&lt;/td>
&lt;td>在目录下创建、删除、重命名子文件/子目录（如&lt;code>touch&lt;/code>、&lt;code>rm&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>x（执行）&lt;/td>
&lt;td>运行文件代码（如脚本、二进制文件，需文件可执行）&lt;/td>
&lt;td>切换到该目录（&lt;code>cd&lt;/code>命令），是目录生效其他权限的前提&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>注意&lt;/strong>：单一权限（如仅&lt;code>w&lt;/code>）组合无实际意义，系统默认不生成，但用户可手动设置，实操中需以“合理功能”为前提搭配权限。&lt;/p>
&lt;h3 id="2-权限的合理组合与实操场景">2. 权限的合理组合与实操场景
&lt;/h3>&lt;h4 id="1目录权限的合理组合">（1）目录权限的合理组合
&lt;/h4>&lt;p>目录需&lt;code>x&lt;/code>权限配合才能生效&lt;code>r&lt;/code>或&lt;code>w&lt;/code>，核心合理组合及应用场景如下：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>权限组合（数字）&lt;/th>
&lt;th>具体权限&lt;/th>
&lt;th>可执行操作&lt;/th>
&lt;th>不可执行操作&lt;/th>
&lt;th>适用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>rwx（7）&lt;/td>
&lt;td>读+写+执行&lt;/td>
&lt;td>1. &lt;code>ls&lt;/code>查看子文件&lt;br>2. &lt;code>cd&lt;/code>进入目录&lt;br>3. 创建/删除/修改子文件&lt;/td>
&lt;td>无&lt;/td>
&lt;td>个人专属目录（如&lt;code>/home/user1&lt;/code>），需完全控制&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>rx（5）&lt;/td>
&lt;td>读+执行&lt;/td>
&lt;td>1. &lt;code>ls&lt;/code>查看子文件&lt;br>2. &lt;code>cd&lt;/code>进入目录&lt;/td>
&lt;td>创建/删除/修改子文件&lt;/td>
&lt;td>系统公共目录（如&lt;code>/usr/local&lt;/code>），仅允许查看和进入&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>wx（3）&lt;/td>
&lt;td>写+执行&lt;/td>
&lt;td>1. &lt;code>cd&lt;/code>进入目录&lt;br>2. 创建/删除子文件（需猜测文件名）&lt;/td>
&lt;td>&lt;code>ls&lt;/code>查看子文件名称&lt;/td>
&lt;td>特定“仅存不看”场景（如临时日志存储目录）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>x（1）&lt;/td>
&lt;td>仅执行&lt;/td>
&lt;td>仅&lt;code>cd&lt;/code>进入目录&lt;/td>
&lt;td>查看、创建、删除子文件&lt;/td>
&lt;td>极少使用，仅需“进入目录”的特殊需求&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>无效组合排除&lt;/strong>：仅&lt;code>r&lt;/code>（无法&lt;code>cd&lt;/code>）、仅&lt;code>w&lt;/code>（无法&lt;code>cd&lt;/code>且无法查看）、&lt;code>rw&lt;/code>（无法&lt;code>cd&lt;/code>，&lt;code>ls&lt;/code>报错），因缺少&lt;code>x&lt;/code>导致核心功能失效，无实际用途。&lt;/p>
&lt;h4 id="2文件权限的合理组合">（2）文件权限的合理组合
&lt;/h4>&lt;p>文件权限需结合“查看、编辑、执行”需求搭配，且需区分解释型（如Shell脚本）与编译型（如C语言二进制文件）的执行差异：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>权限组合（数字）&lt;/th>
&lt;th>具体权限&lt;/th>
&lt;th>可执行操作&lt;/th>
&lt;th>注意事项&lt;/th>
&lt;th>适用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>rwx（7）&lt;/td>
&lt;td>读+写+执行&lt;/td>
&lt;td>1. 查看内容&lt;br>2. 修改/删除内容&lt;br>3. 执行文件&lt;/td>
&lt;td>需确保文件为可执行类型（如脚本、二进制）&lt;/td>
&lt;td>可修改且需运行的脚本（如自定义工具脚本）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>rw（6）&lt;/td>
&lt;td>读+写&lt;/td>
&lt;td>1. 查看内容&lt;br>2. 修改/删除内容&lt;/td>
&lt;td>无法执行文件&lt;/td>
&lt;td>仅编辑的文档（如&lt;code>test.txt&lt;/code>）、配置草稿&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>rx（5）&lt;/td>
&lt;td>读+执行&lt;/td>
&lt;td>1. 查看内容&lt;br>2. 执行文件（仅解释型脚本）&lt;/td>
&lt;td>编译型二进制文件无需&lt;code>r&lt;/code>，仅需&lt;code>x&lt;/code>即可执行&lt;/td>
&lt;td>Shell脚本（需读取脚本代码才能运行）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>r（4）&lt;/td>
&lt;td>仅读&lt;/td>
&lt;td>查看内容&lt;/td>
&lt;td>无法修改、执行&lt;/td>
&lt;td>系统配置文件（如&lt;code>/etc/passwd&lt;/code>）、只读文档&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>关键补充&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>解释型脚本（如&lt;code>a.sh&lt;/code>）：执行需&lt;code>rx&lt;/code>权限（需读取脚本指令）；&lt;/li>
&lt;li>编译型二进制文件（如&lt;code>a.out&lt;/code>）：执行仅需&lt;code>x&lt;/code>权限（无需读取文件内容，直接运行编译后的指令）；&lt;/li>
&lt;li>无效组合（如仅&lt;code>w&lt;/code>、&lt;code>wx&lt;/code>）：虽可强制写入，但无法查看原有内容，易导致文件损坏（如配置文件），不建议使用。&lt;/li>
&lt;/ul>
&lt;h3 id="3-权限的进阶操作">3. 权限的进阶操作
&lt;/h3>&lt;h4 id="1递归设置权限-r选项">（1）递归设置权限（&lt;code>-R&lt;/code>选项）
&lt;/h4>&lt;ul>
&lt;li>作用：批量修改目录及下属所有子文件/子目录的权限、所有者（&lt;code>chown&lt;/code>）或所属组（&lt;code>chgrp&lt;/code>），避免逐个操作；&lt;/li>
&lt;li>示例：
&lt;ul>
&lt;li>&lt;code>chmod -R 700 /testd&lt;/code>：将&lt;code>/testd&lt;/code>及所有子文件/目录设为&lt;code>700&lt;/code>（仅所有者有完全权限）；&lt;/li>
&lt;li>&lt;code>chown -R user1:user1 /testd&lt;/code>：批量将&lt;code>/testd&lt;/code>的所有者和所属组改为&lt;code>user1&lt;/code>。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h4 id="2文件隐藏属性lsattrchattr">（2）文件隐藏属性（&lt;code>lsattr&lt;/code>/&lt;code>chattr&lt;/code>）
&lt;/h4>&lt;ul>
&lt;li>作用：在&lt;code>r/w/x&lt;/code>基础上增加额外限制，默认不显示，需专用命令查看和设置；&lt;/li>
&lt;li>核心属性（重点掌握&lt;code>i&lt;/code>属性）：
&lt;ul>
&lt;li>&lt;code>i&lt;/code>（不可变属性）：
&lt;ul>
&lt;li>对文件：禁止删除、重命名、修改内容，仅允许读和执行；&lt;/li>
&lt;li>对目录：禁止创建、删除子文件/子目录，允许修改已有子文件内容；&lt;/li>
&lt;li>操作：&lt;code>chattr +i 文件名&lt;/code>（设置）、&lt;code>chattr -i 文件名&lt;/code>（取消）；&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;code>a&lt;/code>（追加属性）：仅允许向文件追加内容，禁止删除/修改原有内容，适用于日志文件（如&lt;code>/var/log/messages&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2 id="二传统权限管理方式的局限性">二、传统权限管理方式的局限性
&lt;/h2>&lt;p>此前学习的&lt;code>SUID&lt;/code>/&lt;code>SGID&lt;/code>、&lt;code>ACL&lt;/code>虽能实现部分权限控制，但在“命令权限精准授权”场景下存在明显不足，为&lt;code>sudo&lt;/code>的引入做铺垫：&lt;/p>
&lt;h3 id="1-suidsgid的不足">1. &lt;code>SUID&lt;/code>/&lt;code>SGID&lt;/code>的不足
&lt;/h3>&lt;ul>
&lt;li>核心功能：通过身份切换（如&lt;code>SUID&lt;/code>让普通用户以文件所有者身份执行命令）实现高权限操作（如&lt;code>passwd&lt;/code>修改密码）；&lt;/li>
&lt;li>局限性：
&lt;ul>
&lt;li>权限范围过大：设置后所有用户均可使用该命令，无法指定“单个用户/组”；&lt;/li>
&lt;li>仅对二进制文件有效：非二进制文件（如脚本）设置后无身份切换效果；&lt;/li>
&lt;li>无行为限制：仅能开放命令执行权限，无法限制命令的危险参数（如&lt;code>passwd root&lt;/code>修改管理员密码）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h3 id="2-acl的不足">2. &lt;code>ACL&lt;/code>的不足
&lt;/h3>&lt;ul>
&lt;li>核心功能：给单个用户/组开放文件/目录的精细化权限（如给&lt;code>user1&lt;/code>开放&lt;code>/testd&lt;/code>的&lt;code>rwx&lt;/code>权限）；&lt;/li>
&lt;li>局限性：
&lt;ul>
&lt;li>配置繁琐：面对需操作多个文件的命令（如&lt;code>useradd&lt;/code>需修改&lt;code>/etc/passwd&lt;/code>、&lt;code>/etc/group&lt;/code>等4个文件），需逐个设置&lt;code>ACL&lt;/code>，易遗漏导致命令执行失败；&lt;/li>
&lt;li>无法限制命令行为：仅能控制文件访问权限，无法限制用户执行命令时的具体操作（如禁止&lt;code>passwd root&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2 id="三sudo命令命令权限的精准管理">三、&lt;code>sudo&lt;/code>命令：命令权限的精准管理
&lt;/h2>&lt;p>&lt;code>sudo&lt;/code>是解决“指定用户执行指定命令+行为限制”的核心工具，对比&lt;code>su&lt;/code>更安全、更灵活：&lt;/p>
&lt;h3 id="1-sudo的作用与优势">1. &lt;code>sudo&lt;/code>的作用与优势
&lt;/h3>&lt;ul>
&lt;li>核心功能：
&lt;ul>
&lt;li>精准授权：指定“用户/组”对“特定命令”的执行权限；&lt;/li>
&lt;li>行为限制：禁止危险操作（如禁止修改&lt;code>root&lt;/code>密码）；&lt;/li>
&lt;li>安全验证：执行时仅需输入当前用户密码（无需&lt;code>root&lt;/code>密码），避免&lt;code>root&lt;/code>密码泄露；&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>对比&lt;code>su&lt;/code>命令：
&lt;ul>
&lt;li>&lt;code>su&lt;/code>：需输入&lt;code>root&lt;/code>密码，切换后开放所有&lt;code>root&lt;/code>权限，风险高；&lt;/li>
&lt;li>&lt;code>sudo&lt;/code>：仅开放指定命令权限，执行后自动退回普通用户身份，安全性更高。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h3 id="2-sudo的执行流程">2. &lt;code>sudo&lt;/code>的执行流程
&lt;/h3>&lt;ol>
&lt;li>用户执行&lt;code>sudo + 命令&lt;/code>（如&lt;code>sudo ls /root&lt;/code>）；&lt;/li>
&lt;li>系统检查&lt;code>/etc/sudoers&lt;/code>文件（&lt;code>sudo&lt;/code>的核心配置文件），判断用户是否有该命令的执行权限；&lt;/li>
&lt;li>若有权限，用户输入自身密码（&lt;code>root&lt;/code>用户执行&lt;code>sudo&lt;/code>无需密码）；&lt;/li>
&lt;li>密码验证通过，以配置的身份（默认&lt;code>root&lt;/code>）执行命令；&lt;/li>
&lt;li>短时间内重复执行&lt;code>sudo&lt;/code>无需再次输入密码（避免频繁验证）。&lt;/li>
&lt;/ol>
&lt;h3 id="3-sudoers文件的配置">3. &lt;code>sudoers&lt;/code>文件的配置
&lt;/h3>&lt;h4 id="1编辑方式">（1）编辑方式
&lt;/h4>&lt;ul>
&lt;li>推荐使用&lt;code>visudo&lt;/code>命令：具备语法纠错（如格式错误时提示）和颜色高亮功能，避免配置错误导致&lt;code>sudo&lt;/code>失效；&lt;/li>
&lt;li>不推荐直接编辑：&lt;code>vi /etc/sudoers&lt;/code>无纠错功能，格式错误可能导致无法使用&lt;code>sudo&lt;/code>。&lt;/li>
&lt;/ul>
&lt;h4 id="2核心配置格式">（2）核心配置格式
&lt;/h4>&lt;p>配置规则为“用户/组 主机名=（切换身份） 可执行命令”，各字段用空格或Tab分隔，支持&lt;code>ALL&lt;/code>（表示“所有”）和&lt;code>!&lt;/code>（表示“禁止”）：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>字段&lt;/th>
&lt;th>说明&lt;/th>
&lt;th>示例&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>用户/组&lt;/td>
&lt;td>指定授权对象，组需加&lt;code>%&lt;/code>前缀&lt;/td>
&lt;td>&lt;code>user1&lt;/code>（用户）、&lt;code>%wheel&lt;/code>（组）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>主机名&lt;/td>
&lt;td>指定允许执行&lt;code>sudo&lt;/code>的主机，&lt;code>ALL&lt;/code>表示不限制主机&lt;/td>
&lt;td>&lt;code>x1&lt;/code>（具体主机名）、&lt;code>ALL&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>切换身份&lt;/td>
&lt;td>指定执行命令时切换到的身份，&lt;code>ALL&lt;/code>默认表示&lt;code>root&lt;/code>&lt;/td>
&lt;td>&lt;code>(root)&lt;/code>、&lt;code>(ALL)&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>可执行命令&lt;/td>
&lt;td>指定允许执行的命令（需写绝对路径），多个命令用逗号分隔；&lt;code>!&lt;/code>表示禁止执行&lt;/td>
&lt;td>&lt;code>/usr/sbin/useradd,!/usr/bin/passwd root&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h4 id="3配置示例">（3）配置示例
&lt;/h4>&lt;ul>
&lt;li>允许&lt;code>user1&lt;/code>在任意主机以&lt;code>root&lt;/code>身份执行所有命令：&lt;code>user1 ALL=(ALL) ALL&lt;/code>；&lt;/li>
&lt;li>允许&lt;code>user1&lt;/code>执行&lt;code>useradd&lt;/code>（创建用户）和&lt;code>passwd&lt;/code>（改密码），但禁止修改&lt;code>root&lt;/code>密码：&lt;code>user1 ALL=(ALL) /usr/sbin/useradd,/usr/bin/passwd,!/usr/bin/passwd root&lt;/code>；&lt;/li>
&lt;li>允许&lt;code>user1&lt;/code>非交互式执行&lt;code>ls&lt;/code>（无需输入密码）：&lt;code>user1 ALL=(ALL) NOPASSWD: /usr/bin/ls&lt;/code>。&lt;/li>
&lt;/ul>
&lt;h3 id="4-sudo的实用配置与选项">4. &lt;code>sudo&lt;/code>的实用配置与选项
&lt;/h3>&lt;h4 id="1非交互式执行nopasswd">（1）非交互式执行（&lt;code>NOPASSWD&lt;/code>）
&lt;/h4>&lt;ul>
&lt;li>作用：用户执行指定&lt;code>sudo&lt;/code>命令时无需输入密码，适用于脚本自动化（如定时任务）；&lt;/li>
&lt;li>配置：在命令前加&lt;code>NOPASSWD:&lt;/code>，示例：&lt;code>user1 ALL=(ALL) NOPASSWD: /usr/bin/ls,/usr/bin/cat&lt;/code>。&lt;/li>
&lt;/ul>
&lt;h4 id="2权限查询sudo--l">（2）权限查询（&lt;code>sudo -l&lt;/code>）
&lt;/h4>&lt;ul>
&lt;li>作用：普通用户查询自身可执行的&lt;code>sudo&lt;/code>命令，管理员可协助用户自查权限范围；&lt;/li>
&lt;li>操作：普通用户执行&lt;code>sudo -l&lt;/code>，输入自身密码后即可查看授权命令列表。&lt;/li>
&lt;/ul>
&lt;h4 id="3主机名配置">（3）主机名配置
&lt;/h4>&lt;ul>
&lt;li>主机名存储：默认保存在&lt;code>/etc/hostname&lt;/code>；&lt;/li>
&lt;li>修改方式：&lt;code>hostnamectl set-hostname 新主机名&lt;/code>（需重新登录生效）；&lt;/li>
&lt;li>&lt;code>sudo&lt;/code>配置：主机名字段可填具体主机名（如&lt;code>x1&lt;/code>）或&lt;code>ALL&lt;/code>（不限制主机），需与实际主机名一致，否则&lt;code>sudo&lt;/code>无法执行。&lt;/li>
&lt;/ul>
&lt;h2 id="四管理员用户的定义与配置">四、管理员用户的定义与配置
&lt;/h2>&lt;h3 id="1-管理员用户的本质">1. 管理员用户的本质
&lt;/h3>&lt;p>管理员用户并非独立用户类型，而是“加入&lt;code>wheel&lt;/code>组的普通用户”，其权限来自&lt;code>/etc/sudoers&lt;/code>的默认配置：&lt;code>%wheel ALL=(ALL) ALL&lt;/code>（&lt;code>wheel&lt;/code>组成员可在任意主机以&lt;code>root&lt;/code>身份执行所有命令）。&lt;/p>
&lt;h3 id="2-配置管理员用户的操作">2. 配置管理员用户的操作
&lt;/h3>&lt;ul>
&lt;li>核心步骤：将普通用户加入&lt;code>wheel&lt;/code>组，无需单独配置&lt;code>sudo&lt;/code>；&lt;/li>
&lt;li>命令示例：&lt;code>usermod -aG wheel user1&lt;/code>（&lt;code>-aG&lt;/code>表示“追加到组”，避免覆盖原有组）；&lt;/li>
&lt;li>注意事项：若用户已在&lt;code>wheel&lt;/code>组，单独配置的&lt;code>sudo&lt;/code>限制（如禁止&lt;code>passwd root&lt;/code>）会失效（组权限优先级高于用户单独权限），需先检查用户所属组（&lt;code>groups user1&lt;/code>）。&lt;/li>
&lt;/ul>
&lt;h2 id="五危险操作与注意事项">五、危险操作与注意事项
&lt;/h2>&lt;ol>
&lt;li>
&lt;p>&lt;strong>禁止开放的危险命令&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>vim&lt;/code>：开放后用户可能通过&lt;code>sudo vim /etc/passwd&lt;/code>将自身&lt;code>UID&lt;/code>改为0（变为&lt;code>root&lt;/code>），风险极高；&lt;/li>
&lt;li>&lt;code>su&lt;/code>：开放后用户可通过&lt;code>sudo su&lt;/code>直接切换到&lt;code>root&lt;/code>，获取完整管理员权限，需严格禁止。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>命令路径必须绝对&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>配置&lt;code>sudo&lt;/code>命令时需写绝对路径（如&lt;code>/usr/bin/ls&lt;/code>），避免用户通过自定义同名命令（如在&lt;code>/home/user1&lt;/code>创建&lt;code>ls&lt;/code>恶意脚本）绕过权限限制。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>&lt;code>wheel&lt;/code>组的影响&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>若用户在&lt;code>wheel&lt;/code>组，所有&lt;code>sudo&lt;/code>限制失效，需确保仅信任用户加入&lt;code>wheel&lt;/code>组，避免权限滥用。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>测试权限需用普通用户&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>&lt;code>root&lt;/code>用户权限无限制，无法准确测试权限效果，需切换到普通用户（如&lt;code>su - user1&lt;/code>）进行实操验证。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="六权限管理工具的适用场景与后续预告">六、权限管理工具的适用场景与后续预告
&lt;/h2>&lt;h3 id="1-工具适用场景核心原则对症使用">1. 工具适用场景（核心原则：对症使用）
&lt;/h3>&lt;ul>
&lt;li>开放&lt;strong>命令执行权限&lt;/strong>：用&lt;code>sudo&lt;/code>（精准授权+行为限制，如允许&lt;code>user1&lt;/code>执行&lt;code>useradd&lt;/code>）；&lt;/li>
&lt;li>开放&lt;strong>文件/目录权限&lt;/strong>：用&lt;code>ACL&lt;/code>或基础&lt;code>rwx&lt;/code>权限（如给&lt;code>user1&lt;/code>开放&lt;code>/testd&lt;/code>的&lt;code>rwx&lt;/code>权限）；&lt;/li>
&lt;li>补充说明：&lt;code>ACL&lt;/code>设计目的是“文件/目录权限管理”，不可用于“命令权限授权”（类似“空调不用于热饭”，工具用途需匹配场景）。&lt;/li>
&lt;/ul>
&lt;h3 id="2-后续内容预告">2. 后续内容预告
&lt;/h3>&lt;p>权限管理章节结束后，下节课将讲解“文件系统”相关内容，包括文件系统类型、挂载配置等核心知识。&lt;/p></description></item><item><title>Linux 用户、用户组与服务管理</title><link>https://luckycloveryh.github.io/fuyou/p/linux-03/</link><pubDate>Tue, 18 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-03/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-03/1200/675" alt="Featured image of post Linux 用户、用户组与服务管理" />&lt;h1 id="linux系统用户与组管理及服务配置完整课程总结">Linux系统用户与组管理及服务配置完整课程总结
&lt;/h1>&lt;h2 id="一前期知识回顾与实操任务布置">一、前期知识回顾与实操任务布置
&lt;/h2>&lt;h3 id="一知识回顾">（一）知识回顾
&lt;/h3>&lt;p>回顾前一天讲解的核心内容：局域网基础、原码包（n、x、TR相关）、用户管理核心文件&lt;code>/etc/passwd&lt;/code>，重点强调&lt;code>passwd&lt;/code>文件第3列的用户ID（UID）划分规则——0为root用户专属，1-999分配给系统/服务用户，1000-60000为普通用户默认范围，超过60000可使用但会触发警告（不影响命令执行）。&lt;/p>
&lt;h3 id="二实操任务布置">（二）实操任务布置
&lt;/h3>&lt;ol>
&lt;li>基础验证任务：安装&lt;code>rpm&lt;/code>包（如HTTPD），对比安装前后&lt;code>/etc/passwd&lt;/code>文件变化，观察服务用户的自动创建情况。&lt;/li>
&lt;li>拓展探究任务：参照HTTPD的验证方法，自行验证NX（Nginx，文档中常称ndix）安装是否产生服务用户、NX运行时进程所属用户。&lt;/li>
&lt;li>进阶实践任务：创建可实际使用的服务用户（如模拟Apache服务用户），验证服务用户在服务运行中的作用；练习&lt;code>nsswitch.conf&lt;/code>相关配置效果。&lt;/li>
&lt;/ol>
&lt;h2 id="二服务软件安装与服务用户配置实操">二、服务软件安装与服务用户配置实操
&lt;/h2>&lt;h3 id="一httpd阿帕奇安装与验证">（一）HTTPD（阿帕奇）安装与验证
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>用户创建验证&lt;/strong>：安装HTTPD后，&lt;code>/etc/passwd&lt;/code>文件会新增一行记录，用户名为“apache”，UID为48，shell类型为&lt;code>/sbin/nologin&lt;/code>（需记忆完整路径，避免命令执行失败），该用户为安装过程中自动创建的服务用户。&lt;/li>
&lt;li>&lt;strong>进程运行身份&lt;/strong>：通过&lt;code>systemctl start httpd&lt;/code>启动服务后，使用&lt;code>ps -aux&lt;/code>、&lt;code>pstree -p -u&lt;/code>等命令可查看：HTTPD进程初始由root用户启动，后续衍生的工作进程由apache用户运行。服务用户虽无法登录系统，但运行进程会消耗CPU、内存等系统资源。&lt;/li>
&lt;/ol>
&lt;h3 id="二nginxndix安装与自定义用户配置">（二）Nginx（ndix）安装与自定义用户配置
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>安装前准备&lt;/strong>：将系统恢复到配置dnf后的状态，传输Nginx原版安装包并保留（以备周测），通过远程连接工具（如yscp，连接地址192.168.5.144）登录系统。&lt;/li>
&lt;li>&lt;strong>依赖安装与编译配置&lt;/strong>：先安装gcc、pcr、zlive等依赖包，执行&lt;code>./configure&lt;/code>配置安装路径（如&lt;code>--prefix=/user/local/ndix&lt;/code>），若需特殊功能模块需额外安装对应依赖库；配置成功后执行&lt;code>make &amp;amp;&amp;amp; make install&lt;/code>编译安装，通过&lt;code>echo $?&lt;/code>查看执行结果（返回0表示成功）。&lt;/li>
&lt;li>&lt;strong>默认进程与用户关联&lt;/strong>：启动Nginx（&lt;code>/user/local/ndix/sbin/ndix&lt;/code>）后，通过&lt;code>ss -a n p t&lt;/code>查看80端口监听及进程号，&lt;code>ps aux&lt;/code>可观察到root用户运行Nginx主进程，nobody用户运行工作进程。与Apache差异：Apache通过rpm包安装会自动创建专属服务用户，而Nginx默认使用系统已存在的nobody用户，安装前后&lt;code>/etc/passwd&lt;/code>中用户数量（19个）无变化。&lt;/li>
&lt;li>&lt;strong>自定义用户运行配置&lt;/strong>：
&lt;ul>
&lt;li>停止Nginx进程（&lt;code>/user/local/ndix/sbin/ndix -s stop&lt;/code>），若直接删除目录导致无法停止，可通过重启系统清空进程；删除原安装目录（&lt;code>rm -rf /user/local/ndix&lt;/code>）。&lt;/li>
&lt;li>创建服务用户（&lt;code>useradd -r -s /sbin/nologin ndix&lt;/code>），重新解压安装包并执行&lt;code>./configure --prefix=/user/local/ndix --user=ndix&lt;/code>指定运行用户，编译安装后启动Nginx，此时&lt;code>ps aux&lt;/code>可看到Nginx工作进程由ndix用户运行。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="三核心配置文件详解">三、核心配置文件详解
&lt;/h2>&lt;h3 id="一用户相关配置文件">（一）用户相关配置文件
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>文件名&lt;/th>
&lt;th>核心功能&lt;/th>
&lt;th>字段/关键配置说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>/etc/passwd&lt;/code>&lt;/td>
&lt;td>存储用户基本信息，所有用户可读取&lt;/td>
&lt;td>共7列（冒号分隔）：1.用户名；2.密码占位符（x，实际密码存于&lt;code>/etc/shadow&lt;/code>）；3.UID；4.初始组GID；5.用户描述信息；6.家目录路径（默认&lt;code>/home/用户名&lt;/code>）；7.登录shell（&lt;code>/bin/bash&lt;/code>为可登录，&lt;code>/sbin/nologin&lt;/code>为不可登录）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/shadow&lt;/code>&lt;/td>
&lt;td>存储用户密码及安全相关配置，仅root用户可读写&lt;/td>
&lt;td>共9列（冒号分隔）：1.用户名；2.加密密码（&lt;code>$6$&lt;/code>开头为SHA512加密，未设密码为*或!!，锁定用户前加!!）；3.密码最近修改时间（Unix时间戳，1970年1月1日起天数）；4.密码不可修改天数（默认0）；5.密码最长有效期限（默认99999天）；6.过期前提醒天数（默认7天）；7.过期后宽限天数（0=立即失效，5=5天内可修改，-1=永久有效）；8.账号失效日期（Unix时间戳）；9.保留字段&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二组相关配置文件">（二）组相关配置文件
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>文件名&lt;/th>
&lt;th>核心功能&lt;/th>
&lt;th>字段/关键配置说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>/etc/group&lt;/code>&lt;/td>
&lt;td>存储组基本信息，所有用户可读取&lt;/td>
&lt;td>共4列（冒号分隔）：1.组名；2.组密码占位符（x，实际密码存于&lt;code>/etc/gshadow&lt;/code>）；3.GID；4.组内成员列表（仅显示附加组成员，初始组成员不显示，成员间用逗号分隔）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/gshadow&lt;/code>&lt;/td>
&lt;td>存储组密码及管理员信息，考频较低&lt;/td>
&lt;td>共4列（冒号分隔）：1.组名；2.加密组密码；3.组管理员用户名；4.组内成员列表&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="三配置规则相关文件">（三）配置规则相关文件
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>文件名&lt;/th>
&lt;th>核心功能&lt;/th>
&lt;th>关键配置说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>/etc/login.defs&lt;/code>&lt;/td>
&lt;td>定义创建用户/组的默认规则&lt;/td>
&lt;td>1.UID/GID范围（系统用户1-999，普通用户1000-60000）；2.家目录权限（&lt;code>HOME_MODE=700&lt;/code>）；3.密码规则（默认有效期99999天、不可修改天数0、过期前提醒7天）；4.账号过期相关默认配置&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>/etc/default/useradd&lt;/code>&lt;/td>
&lt;td>补充用户创建默认选项&lt;/td>
&lt;td>1.默认初始组（旧版本为users组GID=100，新版本为私有组）；2.默认Shell（&lt;code>SHELL=/bin/bash&lt;/code>）；3.账号过期设置（&lt;code>EXPIRE=-1&lt;/code>表示永不过期）；4.家目录默认路径等&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="四用户家目录模板文件">（四）用户家目录模板文件
&lt;/h3>&lt;ul>
&lt;li>文件名：&lt;code>/etc/skel&lt;/code>&lt;/li>
&lt;li>功能：存储用户家目录的默认隐藏文件（如&lt;code>.bashrc&lt;/code>、&lt;code>.bash_profile&lt;/code>），创建用户时自动复制到新家目录，仅在用户创建时复制一次。&lt;/li>
&lt;/ul>
&lt;h3 id="五配置文件操作注意事项">（五）配置文件操作注意事项
&lt;/h3>&lt;ol>
&lt;li>&lt;code>/etc/shadow&lt;/code>权限为000，仅root用户可修改，修改后需用&lt;code>wq!&lt;/code>强制保存退出，保存失败则说明无权限。&lt;/li>
&lt;li>不建议手动修改&lt;code>/etc/shadow&lt;/code>、&lt;code>/etc/group&lt;/code>等文件，若改错可能导致用户/组验证失败，优先使用命令修改（错误会报错，更稳妥）。&lt;/li>
&lt;/ol>
&lt;h2 id="四用户管理详解">四、用户管理详解
&lt;/h2>&lt;h3 id="一核心管理命令">（一）核心管理命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能说明&lt;/th>
&lt;th>关键选项与使用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>useradd&lt;/code>&lt;/td>
&lt;td>创建用户&lt;/td>
&lt;td>1.&lt;code>-r&lt;/code>：创建系统/服务用户（自动分配1-999的UID/GID，不创建家目录）；2.&lt;code>-u&lt;/code>：指定UID（需唯一）；3.&lt;code>-g&lt;/code>：指定初始组（需提前存在）；4.&lt;code>-G&lt;/code>：指定附加组；5.&lt;code>-d&lt;/code>：指定自定义家目录；6.&lt;code>-s&lt;/code>：指定登录shell；7.&lt;code>-c&lt;/code>：添加用户说明；8.&lt;code>-f&lt;/code>：账号过期后停用时间（0=立即停用，-1=关闭功能）；9.&lt;code>-m&lt;/code>：不创建家目录&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>usermod&lt;/code>&lt;/td>
&lt;td>修改用户属性&lt;/td>
&lt;td>1.&lt;code>-u&lt;/code>：修改UID；2.&lt;code>-g&lt;/code>：修改初始组；3.&lt;code>-G&lt;/code>：修改附加组；4.&lt;code>-c&lt;/code>：修改描述信息；5.&lt;code>-L&lt;/code>：锁定密码；6.&lt;code>-U&lt;/code>：解锁密码；不推荐优先使用，建议删除用户重新创建（避免保留旧用户使用习惯）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>userdel&lt;/code>&lt;/td>
&lt;td>删除用户&lt;/td>
&lt;td>1.&lt;code>-r&lt;/code>：连带删除用户家目录；2.删除前需检查家目录是否有重要文件，若有需迁移并修改文件所有者/所属组（用户删除后，其创建的文件所有者/所属组会显示为原UID/GID）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>password&lt;/code>&lt;/td>
&lt;td>设置/修改用户密码&lt;/td>
&lt;td>1.权限差异：普通用户仅能修改自身密码（需输入旧密码），root用户可修改任意用户密码（无需旧密码）；2.关键选项：&lt;code>-l&lt;/code>锁定用户、&lt;code>-u&lt;/code>解锁用户、&lt;code>-S&lt;/code>查看密码状态；3.交互模式：默认&lt;code>password 用户名&lt;/code>；非交互模式：`echo &amp;ldquo;密码&amp;rdquo;&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二用户创建关键规则">（二）用户创建关键规则
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>家目录配置&lt;/strong>：普通用户默认家目录为&lt;code>/home/用户名&lt;/code>，权限700；服务用户通常无需家目录，可通过&lt;code>useradd -m&lt;/code>跳过创建。&lt;/li>
&lt;li>&lt;strong>Shell配置&lt;/strong>：普通用户默认Shell为&lt;code>/bin/bash&lt;/code>（可登录），服务用户建议设为&lt;code>/sbin/nologin&lt;/code>（不可登录）。&lt;/li>
&lt;li>&lt;strong>新旧版本差异&lt;/strong>：旧版本（如CentOS 7）默认将所有用户初始组设为users（GID=100），导致用户间权限互通；新版本采用“私有组”机制，每个用户默认创建专属初始组，避免权限泄露。&lt;/li>
&lt;/ol>
&lt;h2 id="五组管理详解">五、组管理详解
&lt;/h2>&lt;h3 id="一核心概念">（一）核心概念
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>初始组&lt;/strong>：用户创建时自动分配的组，默认与用户名同名，无法删除，用户在初始组中的成员身份不在&lt;code>/etc/group&lt;/code>第4列显示，决定用户登录后的默认权限。&lt;/li>
&lt;li>&lt;strong>附加组&lt;/strong>：用户后续通过永久方式加入的组，成员身份会在&lt;code>/etc/group&lt;/code>第4列显示，用户可加入多个附加组。&lt;/li>
&lt;li>&lt;strong>当前组&lt;/strong>：用户当前使用的组身份，默认是初始组，执行&lt;code>newgrp 组名&lt;/code>可切换，切换后新建文件的所属组会变为当前组。&lt;/li>
&lt;/ol>
&lt;h3 id="二核心管理命令">（二）核心管理命令
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>命令&lt;/th>
&lt;th>功能说明&lt;/th>
&lt;th>关键选项与使用场景&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>groupadd&lt;/code>&lt;/td>
&lt;td>创建组&lt;/td>
&lt;td>1.&lt;code>-r&lt;/code>：创建系统/服务组（GID默认1-999）；2.&lt;code>-g&lt;/code>：指定GID（需唯一，普通组默认1000-60000）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>gpasswd&lt;/code>&lt;/td>
&lt;td>管理组密码、成员及管理员&lt;/td>
&lt;td>1.成员管理：&lt;code>-a 用户名 组名&lt;/code>添加成员、&lt;code>-d 用户名 组名&lt;/code>删除成员、&lt;code>-M 用户名列表&lt;/code>覆盖式设置成员（使用较少）；2.组管理员设置：&lt;code>-A 用户名 组名&lt;/code>添加管理员、&lt;code>-A &amp;quot;&amp;quot; 组名&lt;/code>取消管理员；3.设置组密码：直接执行&lt;code>gpasswd 组名&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>newgrp&lt;/code>&lt;/td>
&lt;td>切换当前组&lt;/td>
&lt;td>1.普通用户切换需输入组密码（若组有密码），root用户无需；2.仅在当前终端生效，退出登录或打开新终端后失效；3.切换后用&lt;code>exit&lt;/code>退出当前组，&lt;code>groups&lt;/code>命令可查看用户所属组（排在最前的为当前组）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>groupdel&lt;/code>&lt;/td>
&lt;td>删除组&lt;/td>
&lt;td>若组内有成员或系统中有文件所属组为该组，需先处理（迁移文件、修改文件所属组），否则无法删除&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="三组管理员机制">（三）组管理员机制
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>核心权限&lt;/strong>：组管理员可给组添加（&lt;code>gpasswd -a&lt;/code>）和移除（&lt;code>gpasswd -d&lt;/code>）成员，默认组无管理员，需手动设置。&lt;/li>
&lt;li>&lt;strong>设置方式&lt;/strong>：一是直接在&lt;code>/etc/group&lt;/code>文件第三列写入管理员用户名；二是通过&lt;code>gpasswd -A 用户名 组名&lt;/code>命令添加。&lt;/li>
&lt;li>&lt;strong>取消方式&lt;/strong>：用空内容覆盖原管理员（如&lt;code>gpasswd -A &amp;quot;&amp;quot; 组名&lt;/code>），或手动修改&lt;code>/etc/group&lt;/code>文件，无专门的“删除管理员”命令。&lt;/li>
&lt;/ol>
&lt;h3 id="四组权限获取方式">（四）组权限获取方式
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>&lt;strong>临时获取&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>步骤：先通过&lt;code>gpasswd 组名&lt;/code>给组设密码，再执行&lt;code>newgrp 组名&lt;/code>并输入组密码，即可临时切换到该组，获取组权限。&lt;/li>
&lt;li>特点：仅在当前终端生效，退出登录或打开新终端后失效，临时性切换不会记录到&lt;code>/etc/group&lt;/code>文件。&lt;/li>
&lt;li>示例：HF02用户临时切换到root组后，可修改root组权限的文件，切换后需用&lt;code>exit&lt;/code>退出组身份。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>永久获取&lt;/strong>：&lt;/p>
&lt;ul>
&lt;li>方式一：手动修改&lt;code>/etc/group&lt;/code>文件，在目标组的第4列添加用户名（用逗号分隔）。&lt;/li>
&lt;li>方式二：执行&lt;code>gpasswd -a 用户名 组名&lt;/code>命令。&lt;/li>
&lt;li>特点：两种方式均会修改&lt;code>/etc/group&lt;/code>文件，实现永久性添加，重启系统或重新登录后仍有效。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="五uid与gid的关系">（五）UID与GID的关系
&lt;/h3>&lt;ol>
&lt;li>普通用户创建时，默认生成同名初始组，此时用户UID与初始组GID相同。&lt;/li>
&lt;li>若先独立创建组（占用某一GID），再创建用户，该用户UID会按顺序分配，初始组GID则跳过已占用的GID，导致UID与GID不匹配，属于正常现象。&lt;/li>
&lt;/ol>
&lt;h2 id="六命令执行与进程相关知识">六、命令执行与进程相关知识
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>&lt;code>ps aux&lt;/code>与&lt;code>grep&lt;/code>配合现象&lt;/strong>：&lt;code>ps aux | grep 关键字&lt;/code>中，grep进程会因包含关键字被列出，需区分目标进程与grep自身进程；原因是&lt;code>ps aux&lt;/code>逐行传递结果，grep实时过滤，导致自身进程被显示。&lt;/li>
&lt;li>&lt;strong>命令连接符差异&lt;/strong>：
&lt;ul>
&lt;li>&lt;code>&amp;amp;&amp;amp;&lt;/code>：前一命令执行成功后才执行后一命令，无结果传递（如&lt;code>make &amp;amp;&amp;amp; make install&lt;/code>，编译成功才安装）。&lt;/li>
&lt;li>管道符&lt;code>|&lt;/code>：前一命令的输出作为后一命令的输入（如&lt;code>ps aux | grep nginx&lt;/code>，过滤nginx相关进程）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="七常见考点与学习建议">七、常见考点与学习建议
&lt;/h2>&lt;h3 id="一核心考点">（一）核心考点
&lt;/h3>&lt;ol>
&lt;li>UID/GID范围：系统用户/组（1-999）、普通用户/组（1000-60000）、root用户UID=0。&lt;/li>
&lt;li>配置文件字段：&lt;code>/etc/passwd&lt;/code>、&lt;code>/etc/shadow&lt;/code>、&lt;code>/etc/group&lt;/code>的核心字段含义及作用。&lt;/li>
&lt;li>命令使用：&lt;code>useradd&lt;/code>、&lt;code>gpasswd&lt;/code>、&lt;code>newgrp&lt;/code>等命令的关键选项与适用场景。&lt;/li>
&lt;li>权限机制：初始组与附加组的区别、组权限的临时与永久获取方式。&lt;/li>
&lt;li>非交互式设置密码：&lt;code>echo &amp;quot;密码&amp;quot; | password --stdin 用户名&lt;/code>的用法及安全风险。&lt;/li>
&lt;/ol>
&lt;h3 id="二学习建议">（二）学习建议
&lt;/h3>&lt;ol>
&lt;li>避免死记硬背命令，理解命令用途与解决的实际问题（如创建服务用户是为了隔离服务权限）。&lt;/li>
&lt;li>实操后需明确操作对应的配置文件变化（如创建用户会修改&lt;code>/etc/passwd&lt;/code>和&lt;code>/etc/shadow&lt;/code>）。&lt;/li>
&lt;li>注意命令权限差异（普通用户与root用户的操作限制）及安全风险（如非交互式密码设置的明文泄露问题）。&lt;/li>
&lt;/ol>
&lt;h3 id="三后续安排">（三）后续安排
&lt;/h3>&lt;ol>
&lt;li>上午：理论与基础操作讲解（用户/组管理、配置文件、命令使用）。&lt;/li>
&lt;li>下午：重点实操演示（创建服务用户、验证服务运行中的用户作用）。&lt;/li>
&lt;li>拓展学习：深入练习&lt;code>nsswitch.conf&lt;/code>配置效果，巩固用户/组权限验证场景。&lt;/li>
&lt;/ol></description></item><item><title>Linux 软件安装与系统核心操作</title><link>https://luckycloveryh.github.io/fuyou/p/linux-02/</link><pubDate>Mon, 17 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-02/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-02/1200/675" alt="Featured image of post Linux 软件安装与系统核心操作" />&lt;h1 id="linux系统核心操作视频内容整合总结">Linux系统核心操作视频内容整合总结
&lt;/h1>&lt;p>视频围绕Linux系统的&lt;strong>软件安装（源码包+DNF仓库配置）&lt;strong>与&lt;/strong>用户管理&lt;/strong>两大核心模块展开，结合多版本系统（CentOS 7/9、Rocky Linux）实操演示，详细讲解关键流程、配置方法及注意事项，同时补充局域网DNF源配置细节，具体内容如下：&lt;/p>
&lt;h1 id="一软件安装相关">一、软件安装相关
&lt;/h1>&lt;h2 id="一源码包安装">（一）源码包安装
&lt;/h2>&lt;h3 id="1-两种核心流程">1. 两种核心流程
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>&lt;strong>常见流程&lt;/strong>：获取源码包→解压进入目录→执行&lt;code>configure&lt;/code>（需解决报错）→&lt;code>make&lt;/code>→&lt;code>make install&lt;/code>。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>特殊流程&lt;/strong>（无&lt;code>configure&lt;/code>文件但有&lt;code>makefile&lt;/code>文件）：解压源码包（如&lt;code>trip&lt;/code>相关包，压缩格式&lt;code>tjz&lt;/code>为&lt;code>tar.gz&lt;/code>缩写，可通过&lt;code>file&lt;/code>命令验证、&lt;code>tar&lt;/code>命令解压）→切换至解压目录→安装&lt;code>make&lt;/code>（&lt;code>dnf search make&lt;/code>查找包，&lt;code>dnf install make&lt;/code>安装）→安装&lt;code>GCC&lt;/code>（C语言编译工具，解决&lt;code>make&lt;/code>报错）→执行&lt;code>make&lt;/code>（警告非错误，不影响执行）→执行&lt;code>make install&lt;/code>（复制命令及帮助文档到系统默认目录，如&lt;code>/usr/bin&lt;/code>，实现任意位置调用）。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="2-优势与劣势">2. 优势与劣势
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>类别&lt;/th>
&lt;th>具体内容&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>优势&lt;/strong>&lt;/td>
&lt;td>1. 开放源码，可自行修改功能；2. 安装时可选择所需功能模块；3. 安装文件集中在指定目录，卸载时删除目录即可；4. 适配服务器资源，运行更稳定高效&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>劣势&lt;/strong>&lt;/td>
&lt;td>1. 安装过程复杂，需手动解决编译依赖；2. 需安装编译工具（如GCC）；3. 编译耗时较长，对比rpm包无需编译的特性更繁琐&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="3-实操示例以nginx为例">3. 实操示例（以Nginx为例）
&lt;/h3>&lt;h4 id="1准备工作">（1）准备工作
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>避免端口冲突：Nginx与Apache默认均占用80端口，同一台虚拟机不可同时运行，需关闭其中一个服务。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>解压源码包：将下载的Nginx压缩包（如&lt;code>.tar.gz&lt;/code>格式）解压至目标目录，进入解压后的目录。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="2关键文件查看">（2）关键文件查看
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>&lt;code>README&lt;/code>文件：包含软件功能、官网地址（如&lt;code>nginx.org&lt;/code>）、依赖说明等基础信息。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;code>configure&lt;/code>文件：绿色可执行文件，用于指定安装路径（如&lt;code>./configure --prefix=/usr/local/nginx&lt;/code>）、开启功能模块，同时检测系统是否具备编译条件（如编译工具、依赖库）。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="3解决编译依赖">（3）解决编译依赖
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>安装GCC：执行&lt;code>dnf install gcc&lt;/code>，解决C语言编译工具缺失问题。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>安装依赖库：依次安装&lt;code>pcre-devel&lt;/code>（PCRE扩展库）、&lt;code>zlib-devel&lt;/code>（zlib扩展库），解决&lt;code>configure&lt;/code>检测时的依赖报错。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="4编译与安装">（4）编译与安装
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>编译：执行&lt;code>make&lt;/code>命令，根据&lt;code>configure&lt;/code>生成的&lt;code>Makefile&lt;/code>文件将源码转换为二进制文件。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>安装：执行&lt;code>make install&lt;/code>命令（或合并为&lt;code>make &amp;amp;&amp;amp; make install&lt;/code>，确保编译成功后才执行安装），安装文件会生成在&lt;code>--prefix&lt;/code>指定的路径（如&lt;code>/usr/local/nginx&lt;/code>）。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="5启动与测试">（5）启动与测试
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>启动服务：执行&lt;code>/usr/local/nginx/sbin/nginx&lt;/code>，通过&lt;code>ss -antp&lt;/code>查看80端口是否被Nginx监听。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>验证：关闭防火墙后，在浏览器输入服务器IP，可看到Nginx默认页面，确认安装成功。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="4-适用场景与现状">4. 适用场景与现状
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>适用场景：网络源、本地DNF源无所需软件，或&lt;code>rpm&lt;/code>包安装失败时使用；旧系统中需安装特定版本服务（光盘镜像版本较旧）时常用。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>现状：新系统中&lt;code>rpm&lt;/code>包安装占比更高，但源码包安装仍是重点考点。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="5-卸载与服务管理">5. 卸载与服务管理
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>卸载：先停止对应服务（如&lt;code>/user/local/NDXSB/NDX stop&lt;/code>），再删除安装目录（如&lt;code>/user/local/links&lt;/code>），避免服务运行时删除出问题。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>服务管理差异：&lt;code>rpm&lt;/code>包安装的服务可通过&lt;code>systemctl&lt;/code>命令管理（启动、停止、重启、查看状态），源码包安装的服务暂不支持，后续将讲解解决方案。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="二dnf仓库配置本地网络扩展局域网">（二）DNF仓库配置（本地+网络+扩展+局域网）
&lt;/h2>&lt;h3 id="1-本地dnf仓库">1. 本地DNF仓库
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>核心步骤：剪切&lt;code>/etc/yum.repos.d/&lt;/code>下所有默认&lt;code>.repo&lt;/code>文件到其他位置（使其失效）→创建自定义配置文件（含仓库名称、&lt;code>gpgcheck=0&lt;/code>（关闭GPG校验）、&lt;code>baseurl&lt;/code>（指定挂载点路径））→确保挂载点目录（如&lt;code>/mnt&lt;/code>）存在&lt;code>Packages&lt;/code>（存储rpm包）和&lt;code>repodata&lt;/code>（存储依赖关系）两个核心目录→执行挂载操作→运行&lt;code>dnf clean all&lt;/code>即可安装；除&lt;code>dnf clean all&lt;/code>外，其余步骤无严格顺序要求。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>缺点：每台服务器需挂载约10GB的ISO镜像，常用rpm包仅数百MB，浪费存储资源；无物理光驱的服务器需先传输ISO文件再挂载，操作繁琐。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="2-网络dnf仓库">2. 网络DNF仓库
&lt;/h3>&lt;h4 id="1默认官方源">（1）默认官方源
&lt;/h4>&lt;p>虚拟机恢复初始快照后，默认使用官方网络源，无需额外配置即可通过DNF安装软件。&lt;/p>
&lt;h4 id="2国内网络源配置">（2）国内网络源配置
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>常见国内源：阿里源、清华源、163源、华为源，部分源不支持特定系统版本（如阿里源不提供CentOS 7版本，推荐华为源）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>配置流程：备份&lt;code>/etc/yum.repos.d/&lt;/code>下原有&lt;code>.repo&lt;/code>文件→创建新配置文件（如&lt;code>test.repo&lt;/code>）→为&lt;code>AppStream&lt;/code>和&lt;code>BaseOS&lt;/code>仓库分别配置国内源&lt;code>baseurl&lt;/code>（通过对应开源镜像站获取地址）→添加&lt;code>gpgcheck=0&lt;/code>；部分源提供&lt;code>sed&lt;/code>命令可快速修改配置。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>版本补充：CentOS 9无默认wget工具，需自行安装（安装包名称与命令名称一致）。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="3-扩展网络源epel">3. 扩展网络源（EPEL）
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>引入原因：默认&lt;code>AppStream&lt;/code>和&lt;code>BaseOS&lt;/code>仓库的rpm包有限，无法满足特殊软件安装需求（如&lt;code>ntfs-3g&lt;/code>，用于NTFS文件系统挂载）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>安装与使用：执行&lt;code>dnf -y install epel-release&lt;/code>，自动生成&lt;code>epel.repo&lt;/code>配置文件→安装后可通过&lt;code>dnf -y install ntfs-3g&lt;/code>成功获取扩展包，该包显示来自EPEL仓库。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="4-dnf安装包保存设置">4. DNF安装包保存设置
&lt;/h3>&lt;ul>
&lt;li>
&lt;p>默认机制：安装完成后自动删除rpm包，多台服务器安装同一软件需重复下载，浪费资源。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>修改配置：编辑&lt;code>/etc/dnf/dnf.conf&lt;/code>文件，添加&lt;code>keepcache=1&lt;/code>（1表示保存，0表示删除）和&lt;code>cachedir=/指定路径&lt;/code>（如&lt;code>/var/cache/dnf&lt;/code>）→修改后即时生效，安装包按仓库分类存储在指定路径，便于跨服务器复用。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="5-局域网dnf源配置以9版本为例">5. 局域网DNF源配置（以9版本为例）
&lt;/h3>&lt;h4 id="1核心原理与前提">（1）核心原理与前提
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>局域网DNF源优势：无需像网络源重复下载，也无需像本地源存储完整光盘文件，仅需存储一次即可供多客户端使用。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>硬件要求：至少需两台虚拟机，分别作为服务器端和客户端，不可单台虚拟机同时兼任。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="2服务器端配置步骤">（2）服务器端配置步骤
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>安装HTTP服务（阿帕奇）：目的是通过HTTP协议向客户端传输rpm包及依赖关系，服务对应安装包名为&lt;code>httpd&lt;/code>，安装后系统会生成默认目录&lt;code>/var/www/html&lt;/code>（未安装时该目录不存在）；安装方式可通过本地源（如挂载光盘）或网络源安装，无强制限制。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>存放rpm包及依赖：将系统中&lt;code>/media&lt;/code>目录下的rpm包及依赖关系复制到&lt;code>/var/www/html&lt;/code>目录，可通过&lt;code>du -sh&lt;/code>命令查看目录占用空间。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>启动服务与环境配置：启动HTTP服务执行&lt;code>systemctl start httpd.service&lt;/code>（&lt;code>.service&lt;/code>可省略），通过&lt;code>ss -ant&lt;/code>查看是否监听80端口；关闭防火墙执行&lt;code>systemctl stop firewalld&lt;/code>，避免阻碍客户端连接；通过&lt;code>ip addr&lt;/code>查看服务器端IP（如示例中的192.168.5.141）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>测试服务：在服务器端浏览器输入自身IP，可看到HTTP测试界面，且能访问&lt;code>/var/www/html&lt;/code>下的rpm包及依赖文件（如&lt;code>AppStream&lt;/code>、&lt;code>BaseOS&lt;/code>目录）。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h4 id="3客户端配置步骤">（3）客户端配置步骤
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>基础检查：确保客户端与服务器端在同一网段，通过&lt;code>ping&lt;/code>服务器IP验证连通性；检查本地光盘（SR0）未挂载，避免优先使用本地源。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>修改DNF配置文件：路径为进入&lt;code>/etc/yum.repos.d/&lt;/code>目录，将原有&lt;code>.repo&lt;/code>文件移至备份目录（如新建的&lt;code>bak&lt;/code>目录），新建自定义&lt;code>.repo&lt;/code>文件；内容需配置仓库名称、基础URL（指向服务器端&lt;code>/var/www/html&lt;/code>下的包存放路径，如&lt;code>http://192.168.5.141/AppStream&lt;/code>）、&lt;code>gpgcheck=0&lt;/code>（关闭GPG校验）、&lt;code>enabled=1&lt;/code>（启用该仓库）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>测试安装：执行&lt;code>dnf install&lt;/code>命令安装软件（如&lt;code>tree&lt;/code>、&lt;code>vim&lt;/code>），验证是否通过服务器端DNF源获取包及依赖。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3 id="6-7版本dnf源配置差异">6. 7版本DNF源配置差异
&lt;/h3>&lt;h4 id="1国内源配置简化">（1）国内源配置简化
&lt;/h4>&lt;p>部分7版本（如Super S7 7.6）可通过&lt;code>wget&lt;/code>命令直接下载国内源（如阿里源、清华源）的配置文件，默认保存至&lt;code>/etc/yum.repos.d/&lt;/code>，覆盖原有文件后无需手动修改，直接生效。&lt;/p>
&lt;h4 id="2本地源配置差异">（2）本地源配置差异
&lt;/h4>&lt;ul>
&lt;li>
&lt;p>配置文件模板：系统自带&lt;code>media.repo&lt;/code>模板文件，可直接复制修改，支持多光盘（如SR0、SR1、SR2）路径配置，无需使用时可注释或删除对应行。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>光盘挂载与路径：7版本光盘挂载后（如挂载至&lt;code>/mnt&lt;/code>），rpm包及依赖直接存放在挂载根目录，无需进入&lt;code>AppStream&lt;/code>、&lt;code>BaseOS&lt;/code>子目录，配置文件中&lt;code>baseurl&lt;/code>直接写挂载路径即可。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>命令兼容性：7版本默认使用&lt;code>yum&lt;/code>命令（&lt;code>dnf&lt;/code>需手动安装或通过&lt;code>dnf-3&lt;/code>兼容），配置逻辑与9版本一致，仅文件路径和命令名称有细微差异。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h1 id="二用户管理相关">二、用户管理相关
&lt;/h1>&lt;h2 id="一核心关联与操作特性">（一）核心关联与操作特性
&lt;/h2>&lt;ul>
&lt;li>
&lt;p>关联关系：与软件安装（观察服务安装后是否生成服务用户）、权限设置（用户增多需管控权限）紧密相关，需先学用户管理再学权限管理。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>操作特性：用户创建、软件安装等操作均为永久性，相关信息记录在配置文件中，关机重启后仍有效。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="二用户身份识别">（二）用户身份识别
&lt;/h2>&lt;ul>
&lt;li>
&lt;p>识别方式：使用者通过用户名区分登录账号，操作系统通过&lt;code>UID&lt;/code>（用户ID）辨别身份，&lt;code>UID&lt;/code>相同则视为同一用户。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>合法要求：使用系统资源（内存、CPU、硬盘空间等）需拥有合法账号，部分服务用户无需登录系统。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="三关键配置文件etcpasswd">（三）关键配置文件&lt;code>/etc/passwd&lt;/code>
&lt;/h2>&lt;p>该文件以冒号为分隔符，每行分为7列，各列含义如下：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>列数&lt;/th>
&lt;th>内容&lt;/th>
&lt;th>详细说明&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>1&lt;/td>
&lt;td>用户名&lt;/td>
&lt;td>与&lt;code>lastlog&lt;/code>等文件用户信息对应，用于标识用户&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>2&lt;/td>
&lt;td>密码占位符&lt;/td>
&lt;td>旧版存加密密码，现因安全（权限644，所有用户可查看），密码加密后存于&lt;code>/etc/shadow&lt;/code>，此处用&lt;code>x&lt;/code>占位；有&lt;code>x&lt;/code>需验证密码登录，无&lt;code>x&lt;/code>输对用户名即可登录（仅本地可见，工作中禁止取消&lt;code>x&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>3&lt;/td>
&lt;td>UID（用户ID）&lt;/td>
&lt;td>1. 范围：常见0-65535，现支持2^32次方（约42亿9000万）；2. 分类：0为root，1-999为系统/服务用户，1000-65535为普通用户（7版本前普通用户UID从500开始）；3. 例外：65534为特殊UID，了解即可&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>4&lt;/td>
&lt;td>GID（组ID）&lt;/td>
&lt;td>记录初始组ID（创建时默认加入，唯一）；用户可加多个附加组（获取权限），附加组信息不显示&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>5&lt;/td>
&lt;td>解释说明信息&lt;/td>
&lt;td>最初记录部门、联系方式等，现多为空或填用户名；图形化本地登录优先显示该列，纯字符界面无限制&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>6&lt;/td>
&lt;td>家目录&lt;/td>
&lt;td>登录后默认所在目录（root为&lt;code>/root&lt;/code>，普通用户为&lt;code>/home/用户名&lt;/code>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>7&lt;/td>
&lt;td>shell类型&lt;/td>
&lt;td>1. 常见：&lt;code>/bin/bash&lt;/code>（默认，支持正常登录）、&lt;code>/sbin/nologin&lt;/code>（服务用户禁用登录）；2. 其他：如&lt;code>/bin/sh&lt;/code>、&lt;code>/sbin/sync&lt;/code>等，部分仅支持特定命令&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2 id="四组相关概念">（四）组相关概念
&lt;/h2>&lt;ul>
&lt;li>
&lt;p>初始组：用户创建时默认加入的组，唯一，&lt;code>/etc/passwd&lt;/code>第4列记录其GID。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>附加组：用户为获取权限额外加入的组，不在&lt;code>/etc/passwd&lt;/code>中显示。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>当前组：默认是初始组，可通过命令切换为任意附加组；用户新建文件的所属组由当前组决定。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="五相关配置文件与系统限制">（五）相关配置文件与系统限制
&lt;/h2>&lt;ul>
&lt;li>
&lt;p>UID范围限制：系统通过&lt;code>/etc/login.defs&lt;/code>文件限制普通用户UID范围（默认1000-60000），超出可修改该文件调整。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>组信息文件：&lt;code>/etc/group&lt;/code>记录GID与组名的对应关系，后续单独讲解。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="六实操注意事项">（六）实操注意事项
&lt;/h2>&lt;ul>
&lt;li>
&lt;p>配置生效：修改&lt;code>/etc/passwd&lt;/code>等文件后，用户需重新登录，配置才生效（系统仅登录时读取配置）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>避坑点：禁止创建&lt;code>UID&lt;/code>相同的用户，避免系统误判身份；取消密码占位符&lt;code>x&lt;/code>等操作为反面案例，工作中禁止操作。&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h1 id="三补充知识点">三、补充知识点
&lt;/h1>&lt;ul>
&lt;li>
&lt;p>命令执行结果判断：通过&lt;code>echo $?&lt;/code>查看上一条命令执行结果，返回0表示成功，非0表示失败（绝大多数场景适用，存在极少数人为定义异常的情况）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>路径切换必要性：源码包安装时建议进入解压目录执行命令，便于生成和查找&lt;code>Makefile&lt;/code>等临时文件，避免因路径问题导致命令执行失败。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>视频包含大量实操演示（修改配置文件、安装依赖工具、验证安装效果等），部分操作需区分系统版本。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>学习建议：课后需复习命令与配置流程（如&lt;code>mount -o loop 镜像路径 挂载点&lt;/code>），结合视频回顾实操细节；主动记忆基础内容，面试重点考察操作系统、服务集群、虚拟化等相关知识。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>时间调整：课程当前12点下课，后续上课时间可能延迟（如11点后开始），以避开就餐高峰。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>后续预告：将深入讲解&lt;code>/etc/login.defs&lt;/code>、&lt;code>/etc/group&lt;/code>等配置文件，以及源码包服务管理问题的解决方法、局域网DNF源具体配置步骤。&lt;/p>
&lt;/li>
&lt;/ul></description></item><item><title>Linux DNF/YUM 软件仓库管理</title><link>https://luckycloveryh.github.io/fuyou/p/linux-18/</link><pubDate>Fri, 14 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-18/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-18/1200/675" alt="Featured image of post Linux DNF/YUM 软件仓库管理" />&lt;h1 id="视频主要内容总结">视频主要内容总结
&lt;/h1>&lt;h2 id="一dnf与yum包管理核心概念">一、DNF与YUM包管理核心概念
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>DNF/YUM的核心作用&lt;/strong>：二者均基于RPM包进行安装，核心优势是自动解决RPM包的依赖关系，而依赖关系的解决依赖于“仓库”（或“源”）——仓库需同时包含RPM包文件和依赖关系数据，支持本地目录或网络地址两种形式。&lt;/li>
&lt;li>&lt;strong>仓库配置文件基础&lt;/strong>：系统默认仓库配置文件存于&lt;code>/etc/yum.repos.d/&lt;/code>路径下，且需以&lt;code>.repo&lt;/code>为后缀才能被DNF/YUM识别。以Rocky系统为例，默认生效的仓库为&lt;code>AppStream&lt;/code>和&lt;code>BaseOS&lt;/code>，其配置文件（如&lt;code>rocky.repo&lt;/code>）包含关键信息：
&lt;ul>
&lt;li>&lt;strong>仓库名称&lt;/strong>：置于中括号内（如&lt;code>[BaseOS]&lt;/code>），是安装过程中显示的仓库标识。&lt;/li>
&lt;li>&lt;strong>镜像/地址配置&lt;/strong>：&lt;code>mirrorlist&lt;/code>（支持多个网络地址）或&lt;code>baseurl&lt;/code>（可填网络地址或本地目录，本地需用&lt;code>file://&lt;/code>格式），用于指定RPM包和依赖的获取来源。&lt;/li>
&lt;li>&lt;strong>启用状态&lt;/strong>：&lt;code>enabled=1&lt;/code>表示启用仓库，&lt;code>enabled=0&lt;/code>表示禁用；&lt;code>gpgcheck=1&lt;/code>启用GPG安全验证（需通过&lt;code>gpgkey&lt;/code>指定密钥文件路径，默认路径文件已存在）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="二仓库类型配置实操">二、仓库类型配置实操
&lt;/h2>&lt;h3 id="一网络仓库默认">（一）网络仓库（默认）
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>默认机制&lt;/strong>：系统安装后默认使用官方网络仓库（如Rocky的&lt;code>https://mirrors.rockylinux.org/&lt;/code>），配置文件中&lt;code>baseurl&lt;/code>或&lt;code>mirrorlist&lt;/code>指向官方网址，安装时自动从网络下载RPM包及依赖。&lt;/li>
&lt;li>&lt;strong>验证方式&lt;/strong>：查看&lt;code>/etc/yum.repos.d/&lt;/code>下的&lt;code>.repo&lt;/code>文件，确认&lt;code>enabled=1&lt;/code>且地址为官方网络路径，安装时仓库列显示默认仓库名（如&lt;code>AppStream&lt;/code>）即可验证。&lt;/li>
&lt;/ol>
&lt;h3 id="二本地仓库配置">（二）本地仓库配置
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>配置原因&lt;/strong>：工作中反复使用网络仓库会占用带宽，本地仓库可避免此问题，需基于本地挂载的光盘或目录配置。&lt;/li>
&lt;li>&lt;strong>配置步骤&lt;/strong>：
&lt;ul>
&lt;li>&lt;strong>创建自定义配置文件&lt;/strong>：在&lt;code>/etc/yum.repos.d/&lt;/code>下新建&lt;code>.repo&lt;/code>文件（如&lt;code>hf.repo&lt;/code>），需包含仓库名称（建议与默认区分，如&lt;code>[appstream-r9.4-hf]&lt;/code>）、&lt;code>name&lt;/code>（描述信息）、&lt;code>gpgcheck=1&lt;/code>（启用验证）、&lt;code>enabled=1&lt;/code>（启用仓库）、&lt;code>gpgkey&lt;/code>（复制默认密钥路径），关键是&lt;code>baseurl&lt;/code>指向本地目录（如&lt;code>file:///media/AppStream&lt;/code>，需注意Linux严格区分大小写）。&lt;/li>
&lt;li>&lt;strong>禁用默认网络仓库&lt;/strong>：通过三种方式实现：删除默认&lt;code>.repo&lt;/code>文件（不推荐，后续无法恢复）、修改默认文件后缀（如改为&lt;code>.repo.bak&lt;/code>）、将默认文件移动到其他目录（如&lt;code>/etc/yum.repos.d/backup.d/&lt;/code>）。&lt;/li>
&lt;li>&lt;strong>清空缓存并验证&lt;/strong>：执行&lt;code>dnf clean all&lt;/code>清空原有缓存，再通过&lt;code>dnf install&lt;/code>（如安装&lt;code>vim&lt;/code>）测试，若仓库列显示自定义名称，则本地仓库配置成功。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="三dnf常用命令与进阶操作">三、DNF常用命令与进阶操作
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>基础命令&lt;/strong>：
&lt;ul>
&lt;li>&lt;strong>安装&lt;/strong>：&lt;code>dnf install &amp;lt;包名&amp;gt;&lt;/code>（需手动确认）或&lt;code>dnf -y install &amp;lt;包名&amp;gt;&lt;/code>（默认确认）；支持通配符（如&lt;code>dnf -y install bash-completion*&lt;/code>），安装&lt;code>bash-completion&lt;/code>后重新登录可实现命令选项补全。&lt;/li>
&lt;li>&lt;strong>软件组安装&lt;/strong>：&lt;code>dnf group install &amp;quot;&amp;lt;软件组名&amp;gt;&amp;quot;&lt;/code>（需加双引号确保识别为整体），适用于安装大量关联包（如图形化环境&lt;code>&amp;quot;Server with GUI&amp;quot;&lt;/code>，需安装数百个包）。&lt;/li>
&lt;li>&lt;strong>卸载与升级&lt;/strong>：&lt;code>dnf remove &amp;lt;包名&amp;gt;&lt;/code>（卸载）、&lt;code>dnf update &amp;lt;包名&amp;gt;&lt;/code>（升级指定包），升级内核需执行&lt;code>dnf -y update kernel&lt;/code>（作业要求：联网恢复快照后执行，完成后关机做快照）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>查询命令&lt;/strong>：
&lt;ul>
&lt;li>&lt;code>dnf list&lt;/code>：查看所有可安装包，通过最后一列仓库名确认当前使用的仓库类型（本地/网络）。&lt;/li>
&lt;li>&lt;code>dnf info &amp;lt;包名&amp;gt;&lt;/code>：查看包的详细信息（如描述、版本），与&lt;code>rpm -qi &amp;lt;包名&amp;gt;&lt;/code>功能类似。&lt;/li>
&lt;li>&lt;code>dnf provides &amp;lt;命令/文件路径&amp;gt;&lt;/code>：查询命令或文件所属的安装包（如&lt;code>dnf provides vim&lt;/code>），7版本后常用。&lt;/li>
&lt;li>&lt;code>dnf search &amp;lt;关键词&amp;gt;&lt;/code>：根据包名或描述中的关键词查询（如&lt;code>dnf search rz&lt;/code>），7版本前更常用。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>缓存管理&lt;/strong>：&lt;code>dnf clean all&lt;/code>清空缓存，&lt;code>dnf makecache&lt;/code>手动创建缓存，后续安装会自动生成缓存，无强制执行要求，但建议配置仓库后清空缓存。&lt;/li>
&lt;/ol>
&lt;h2 id="四常见问题与解决方案">四、常见问题与解决方案
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>安装进程冲突&lt;/strong>：系统中同一时间只能有一个DNF/YUM安装进程运行，多用户同时安装时，后执行的进程会等待前一进程结束（可通过&lt;code>ps -ef | grep dnf&lt;/code>查看进程，等待或结束前一进程）。&lt;/li>
&lt;li>&lt;strong>开机检测更新导致安装报错&lt;/strong>：7版本系统开机可能触发&lt;code>packagekit&lt;/code>进程检测更新，此时安装会报错，解决方案：等待检测完成、重启系统、执行&lt;code>kill &amp;lt;进程号&amp;gt;&lt;/code>结束&lt;code>packagekit&lt;/code>进程，或长期关闭开机检测更新功能。&lt;/li>
&lt;li>&lt;strong>配置文件语法错误&lt;/strong>：如&lt;code>enabled=o&lt;/code>（误将数字0写为字母o）导致仓库不生效，需注意区分数字0/1与字母o/l，建议复制默认配置文件中的关键字段（如&lt;code>gpgkey&lt;/code>路径）避免手动输入错误。&lt;/li>
&lt;/ol>
&lt;h2 id="五作业与后续安排">五、作业与后续安排
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>周末作业&lt;/strong>：联网恢复快照（确保使用网络仓库），执行&lt;code>dnf -y update kernel&lt;/code>升级内核，完成后关机做快照；练习本地仓库配置、软件组安装（图形化环境）及查询命令。&lt;/li>
&lt;li>&lt;strong>后续内容&lt;/strong>：下周讲解网络仓库的国内源（如阿里、清华源，解决官方源速度慢的问题）和局域网仓库配置，需先掌握本次本地/网络仓库及命令基础，无需提前预习。&lt;/li>
&lt;/ol></description></item><item><title>Linux Vim 进阶与网卡配置</title><link>https://luckycloveryh.github.io/fuyou/p/linux-17/</link><pubDate>Fri, 14 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-17/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-17/1200/675" alt="Featured image of post Linux Vim 进阶与网卡配置" />&lt;h1 id="视频主要内容总结">视频主要内容总结
&lt;/h1>&lt;h2 id="一vim相关操作与模式">一、VIM相关操作与模式
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>VIM工作模式补充&lt;/strong>：在命令模式、编辑模式、末号模式基础上，新增“微模式”（可视框模式），通过&lt;code>Ctrl + v&lt;/code>进入，可纵向选中多行内容，输入大写&lt;code>i&lt;/code>切换至编辑模式，添加字符后按&lt;code>ESC&lt;/code>能实现多行批量添加字符（如批量注释）。&lt;/li>
&lt;li>&lt;strong>VIM使用技巧&lt;/strong>：取消文本选中高亮可查找不存在的字符串；需记忆&lt;code>GG&lt;/code>（跳转到最后一行）、&lt;code>20G&lt;/code>（跳转到第20行）、&lt;code>DD&lt;/code>（删除一行）、&lt;code>YYP&lt;/code>（复制粘贴）等常用命令，提升操作效率。&lt;/li>
&lt;li>&lt;strong>交换文件（.swp文件）&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>产生机制&lt;/strong>：执行&lt;code>VIM&lt;/code>命令打开文件时会默认创建&lt;code>.swp&lt;/code>备份文件，无修改时，保存/不保存退出或断开连接后文件会自动删除；有修改但未保存意外退出，文件会永久保存。&lt;/li>
&lt;li>&lt;strong>功能作用&lt;/strong>：可恢复未保存的修改数据，也能提示多用户修改冲突，避免文件内容被覆盖。&lt;/li>
&lt;li>&lt;strong>操作处理&lt;/strong>：恢复数据后，可在提示界面输入&lt;code>d&lt;/code>或执行&lt;code>rm -rf&lt;/code>命令删除&lt;code>.swp&lt;/code>文件，避免重复出现提示。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>行号设置&lt;/strong>：临时开启行号在Vim末行模式输入&lt;code>:set nu&lt;/code>，永久开启需编辑&lt;code>/etc/vimrc&lt;/code>文件，在最后一行添加&lt;code>set nu&lt;/code>并保存。&lt;/li>
&lt;/ol>
&lt;h2 id="二网卡配置相关操作">二、网卡配置相关操作
&lt;/h2>&lt;h3 id="一不同系统版本的网卡配置差异">（一）不同系统版本的网卡配置差异
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>文件路径差异&lt;/strong>：九版本系统网卡配置文件位于&lt;code>/etc/NetworkManager/&lt;/code>目录；七版本系统配置文件保存路径为&lt;code>/etc/sysconfig/network-scripts/&lt;/code>，文件名称为&lt;code>ifcfg-ens33&lt;/code>。&lt;/li>
&lt;li>&lt;strong>配置内容差异&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>IP获取方式&lt;/strong>：九版本需手动将默认的自动（DHCP）改为“手动”模式；七版本通过配置项指定，&lt;code>dhcp&lt;/code>为自动获取，&lt;code>static&lt;/code>或&lt;code>no&lt;/code>为静态IP（二者效果一致）。&lt;/li>
&lt;li>&lt;strong>参数设置&lt;/strong>：九版本静态IP配置格式为“IP地址/子网掩码,网关”，DNS可配置多个并以分号分隔；七版本需配置&lt;code>IPADDR&lt;/code>、&lt;code>NETMASK&lt;/code>（无需写“/24”）、&lt;code>GATEWAY&lt;/code>、&lt;code>DNS&lt;/code>（可写&lt;code>DNS1&lt;/code>/&lt;code>DNS2&lt;/code>或直接&lt;code>DNS&lt;/code>），且配置项等号左侧为大写、右侧为小写。&lt;/li>
&lt;li>&lt;strong>开机自启&lt;/strong>：七版本&lt;code>ONBOOT=yes&lt;/code>表示网卡开机运行，对应九版本的“自动”选项，两个版本均有“自动、手动、禁用”三个选项，仅存在细微差异。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>配置生效方式&lt;/strong>：九版本修改配置文件后，需执行&lt;code>nmcli connection load&lt;/code>载入配置，再执行&lt;code>nmcli connection up&lt;/code>使配置生效，也可重启系统；七版本未提及特殊的生效命令差异，旧版本通用的&lt;code>ifdown&lt;/code>和&lt;code>ifup&lt;/code>命令在九版本中不适用。&lt;/li>
&lt;/ol>
&lt;h3 id="二新增网卡配置文件">（二）新增网卡配置文件
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>问题背景&lt;/strong>：新添加的网卡（如虚拟机新增网络适配器）无默认配置文件，无法直接设置IP，旧网卡会随系统安装自动生成配置文件。&lt;/li>
&lt;li>&lt;strong>创建方法&lt;/strong>：九版本可通过&lt;code>nmcli connection add con-name [网卡名] type ethernet ifname [网卡名]&lt;/code>命令直接创建，系统自动生成&lt;code>UUID&lt;/code>；七版本可复制现有网卡配置文件，修改其中的&lt;code>UUID&lt;/code>（删除原&lt;code>UUID&lt;/code>）和网卡名。&lt;/li>
&lt;/ol>
&lt;h3 id="三uuid相关说明">（三）UUID相关说明
&lt;/h3>&lt;p>&lt;code>UUID&lt;/code>是硬件的唯一识别编号，硬盘分区名可能因硬件插拔或重启变化，在&lt;code>/etc/fstab&lt;/code>文件中用&lt;code>UUID&lt;/code>挂载分区可实现永久挂载；网卡配置文件中的&lt;code>UUID&lt;/code>用于唯一标识网卡，新增配置文件时系统会自动生成。&lt;/p>
&lt;h3 id="四nmcli命令补充">（四）NMCLI命令补充
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>连接管理&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>查看连接信息&lt;/strong>：执行&lt;code>nmcli connection show&lt;/code>（可简写）查看所有网卡连接信息，添加网卡名可查看指定网卡信息。&lt;/li>
&lt;li>&lt;strong>启动与停止连接&lt;/strong>：执行&lt;code>nmcli connection up/down [网卡名]&lt;/code>启动/停止指定网卡连接。&lt;/li>
&lt;li>&lt;strong>删除连接&lt;/strong>：执行&lt;code>nmcli connection delete [网卡名]&lt;/code>可删除指定网卡的配置文件，后续可通过&lt;code>nmcli connection add&lt;/code>重新创建。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>设备管理&lt;/strong>：执行&lt;code>nmcli device show&lt;/code>查看硬件设备信息，执行&lt;code>nmcli device status&lt;/code>查看设备状态，停用设备后需执行&lt;code>nmcli connection up&lt;/code>重新激活。&lt;/li>
&lt;li>&lt;strong>reload命令局限&lt;/strong>：&lt;code>nmcli connection reload&lt;/code>仅能重新载入配置文件，无法真正使网卡配置生效，仍需通过&lt;code>load&lt;/code>和&lt;code>up&lt;/code>命令或重启系统。&lt;/li>
&lt;/ol>
&lt;h2 id="三软件安装相关知识">三、软件安装相关知识
&lt;/h2>&lt;h3 id="一安装包类型及差异">（一）安装包类型及差异
&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>类型&lt;/th>
&lt;th>核心特点&lt;/th>
&lt;th>安装要求&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>源码包&lt;/td>
&lt;td>包含完整源代码，可自定义修改功能，需编译为二进制才能安装&lt;/td>
&lt;td>需对应编译工具（如C语言需&lt;code>gcc&lt;/code>），且需解决函数库依赖&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>RPM包&lt;/td>
&lt;td>无源代码，已编译为二进制，可直接安装&lt;/td>
&lt;td>无需编译工具和手动解决函数库，但需处理依赖问题（如A依赖B、B依赖C）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="二rpm包相关">（二）RPM包相关
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>核心特点&lt;/strong>：支持升级、具备强大查询功能、可安全验证、自动指定安装位置，缺点是依赖问题难解决，可能出现“循环依赖”。&lt;/li>
&lt;li>&lt;strong>命名规则&lt;/strong>：格式为“软件名-版本-发布次数-操作系统平台-硬件平台.rpm”，需注意“-”分隔的各部分含义，避免混淆包名。&lt;/li>
&lt;li>&lt;strong>常用命令&lt;/strong>
&lt;ul>
&lt;li>安装：&lt;code>rpm -ivh 包全名&lt;/code>（需指定路径）&lt;/li>
&lt;li>卸载：&lt;code>rpm -e 包名&lt;/code>（无需路径）&lt;/li>
&lt;li>升级：&lt;code>rpm -Uvh 包全名&lt;/code>（需新版本，光盘无法升级，需网络源）&lt;/li>
&lt;li>查询：&lt;code>rpm -q 包名&lt;/code>（查是否安装）、&lt;code>rpm -qa&lt;/code>（查所有已装包）、&lt;code>rpm -qi 包名&lt;/code>（查包详情）、&lt;code>rpm -ql 包名&lt;/code>（查包生成文件路径）、&lt;code>rpm -qpl 包全名&lt;/code>（查未安装包的预期文件路径）、&lt;code>rpm -qf 文件路径&lt;/code>（查文件所属包）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="三dnfyum工具解决rpm依赖">（三）DNF/YUM工具（解决RPM依赖）
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>工具差异与关联&lt;/strong>：旧版本（RHEL 5/6/7）用&lt;code>yum&lt;/code>，新版本用&lt;code>dnf&lt;/code>，二者选项和核心功能一致，仅命令名不同，均依赖“仓库”。&lt;/li>
&lt;li>&lt;strong>核心功能&lt;/strong>：可自动解决依赖，只需指定目标包，工具会自动查找并安装所有依赖包。&lt;/li>
&lt;li>&lt;strong>仓库配置&lt;/strong>：仓库来源可为光盘（需指定&lt;code>/media/AppStream&lt;/code>和&lt;code>/media/BaseOS&lt;/code>路径）或网络源（系统默认使用官方网络源）。&lt;/li>
&lt;li>&lt;strong>常用命令&lt;/strong>：安装包执行&lt;code>dnf install 包名&lt;/code>，网速过慢可能导致安装失败，后续会补充讲解DNF安装的更多操作细节。&lt;/li>
&lt;/ol>
&lt;h2 id="四实践练习与注意事项">四、实践练习与注意事项
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>实践任务&lt;/strong>：关闭虚拟机后将网卡改为桥接模式，修改为分配的IP地址（修改前用&lt;code>ping&lt;/code>命令检测IP是否被占用），修改成功后同一教室设备可通过该IP远程连接虚拟机，实操理解&lt;code>w&lt;/code>（查看登录用户）、&lt;code>who&lt;/code>（查看当前登录用户）等命令的作用。&lt;/li>
&lt;li>&lt;strong>注意事项&lt;/strong>：远程连接失败可能是防火墙开启，需检查防火墙状态；创建测试用户后，他人可通过该用户登录系统，在&lt;code>w&lt;/code>命令结果中能看到登录用户及对应IP；上午讲解上述内容，下午将进行周测。&lt;/li>
&lt;/ol></description></item><item><title>Linux 入门：发行版、分区与虚拟机安装</title><link>https://luckycloveryh.github.io/fuyou/p/linux-16/</link><pubDate>Thu, 13 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-16/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-16/1200/675" alt="Featured image of post Linux 入门：发行版、分区与虚拟机安装" />&lt;p>本次视频为Linux系统学习课程内容，涵盖&lt;strong>课程基础保障、Linux系统核心知识、虚拟机部署与系统安装、安装后操作要求&lt;/strong>四大核心板块，同时明确了课程进度安排，具体内容如下：&lt;/p>
&lt;h2 id="一课程基础保障与学习要求">一、课程基础保障与学习要求
&lt;/h2>&lt;h3 id="一课程基础保障">（一）课程基础保障
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>画面与传输&lt;/strong>：因局域网传输特性，画面清晰度越高数据量越大，班级人数多时易卡顿，学员需观察操作与讲解同步性，有延迟可反馈调整。&lt;/li>
&lt;li>&lt;strong>资料分发&lt;/strong>：每日课程录屏、PDF课件、TXT随堂记录（有则发）会按“年份月份-学习阶段-课程内容”路径存于网盘，学员仅可查看对应班级资料；网盘还提供镜像仓库、软件工具等共享资源，可直接下载。&lt;/li>
&lt;/ol>
&lt;h3 id="二学习与作业要求">（二）学习与作业要求
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>作业任务&lt;/strong>：每日需写两遍笔记，核心是背诵Linux命令（如复制、设置网卡等），而非单纯抄写。&lt;/li>
&lt;li>&lt;strong>学习方法&lt;/strong>：上课记牢知识点可先实操再写笔记，未记住则看录屏边看边写，写完再实操验证；实操占比高，需多投入时间练习。&lt;/li>
&lt;li>&lt;strong>知识记忆&lt;/strong>：Linux系统目录规范需逐步记忆，建议每天记3-5个，一周内掌握核心目录用途；无需一次性背完，可结合“见句号记忆”等方法辅助。&lt;/li>
&lt;/ol>
&lt;h2 id="二linux系统核心知识">二、Linux系统核心知识
&lt;/h2>&lt;h3 id="一系统发展与发行版">（一）系统发展与发行版
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>发展历程&lt;/strong>：从1965年malnix计划停滞，到1969年Unix诞生、1980年代minix出现，再到李纳斯·托瓦茨基于minix开发Linux内核，结合GNU工具形成完整Linux系统。&lt;/li>
&lt;li>&lt;strong>主流发行版&lt;/strong>：内核均为Linux，搭配不同工具形成不同版本，其中Ubuntu适合入门，红帽（Red Hat）用于服务器且提供收费售后，CentOS曾为社区版红帽（后被收购），Rocky Linux由CentOS创始人打造；课程主打Rocky Linux，建议先掌握一个发行版再学其他。&lt;/li>
&lt;li>&lt;strong>系统特点&lt;/strong>：应用于云计算、影视特效渲染等领域，与Windows相比更注重稳定性和权限控制，默认权限严格，不易误删系统文件。&lt;/li>
&lt;/ol>
&lt;h3 id="二分区与目录核心概念">（二）分区与目录核心概念
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>核心分区功能&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>boot分区&lt;/strong>：存放启动引导与内核文件，建议至少512MB，必为第一个分区；若不单独划分，相关文件会存于根分区的/boot目录。&lt;/li>
&lt;li>&lt;strong>根分区（/）&lt;/strong>：绝大多数文件的默认存储位置，未单独划分的分区文件会存于其对应目录，建议按需求分配空间（如15GB），文件系统为xfs。&lt;/li>
&lt;li>&lt;strong>swap分区&lt;/strong>：与物理内存交换数据，大小依物理内存而定（≤4GB时设为2倍，＞4GB时设1-4GB），需通过内存命令查看，文件系统为swap。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>分区与目录区别&lt;/strong>：根目录下的文件夹若为分区挂载点则是分区，否则为普通目录（如未划分/home分区，/home就是根分区下的普通目录）。&lt;/li>
&lt;li>&lt;strong>分区方案&lt;/strong>：标准分区通常先分boot、再分swap，最后将剩余空间分给根分区，划分后需确认写入分区表、格式化并挂载。&lt;/li>
&lt;/ol>
&lt;h2 id="三虚拟机部署与linux系统安装">三、虚拟机部署与Linux系统安装
&lt;/h2>&lt;h3 id="一vmware虚拟机配置">（一）VMware虚拟机配置
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>版本要求&lt;/strong>：建议用VMware 17.5及以上版本，低版本可能缺少Rocky Linux选项，导致安装异常。&lt;/li>
&lt;li>&lt;strong>参数配置&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>存储与系统&lt;/strong>：虚拟机存储选永久性分区（如D盘），且为每个虚拟机单独建文件夹；系统类型需精准选择Rocky Linux，否则易导致命令执行出错。&lt;/li>
&lt;li>&lt;strong>硬件资源&lt;/strong>：处理器默认2个、内存2GB即可（后续可调整）；网络初期用NAT模式，暂不使用桥接模式；虚拟磁盘需创建新盘（20GB），实操中需分别选择SCSI和NVMe类型做对比。&lt;/li>
&lt;li>&lt;strong>ISO镜像&lt;/strong>：关联Rocky Linux 9.4镜像文件，路径确定后不可更改，需勾选“启动时连接”。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h3 id="二系统安装流程与差异">（二）系统安装流程与差异
&lt;/h3>&lt;ol>
&lt;li>&lt;strong>Rocky Linux 9.4安装&lt;/strong>
&lt;ul>
&lt;li>开机选第一个“安装”选项，分区表默认2TB以下为MBR、2TB及以上为GPT，可通过Tab键强制设置GPT。&lt;/li>
&lt;li>语言选简体中文，时区设为亚洲/上海；软件选择分“带GUI的服务器”（图形化）和“最小化安装”（字符界面）两类。&lt;/li>
&lt;li>硬盘分区可选择标准分区（无法扩容）或LVM逻辑卷（可动态扩容），需按要求创建boot、swap、根分区。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>不同版本安装差异&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>RHEL 9.4&lt;/strong>：默认网卡开启有IP，图形化安装重启后需创建普通用户，字符界面则不强制。&lt;/li>
&lt;li>&lt;strong>CentOS 7.6&lt;/strong>：默认网卡关闭，需手动开启获取IP；开始安装后才设置root密码，安装完成重启后需做快照。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>安装配置要点&lt;/strong>
&lt;ul>
&lt;li>网络需开启网卡以获取IP，主机名暂不修改；root用户需取消锁定并设置密码，勾选允许ssh登录。&lt;/li>
&lt;li>图形化安装会强制创建普通用户，用户名避免全数字或“root1”类名称；学习场景下建议关闭kdump功能以节省内存。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="四系统安装后操作与课程安排">四、系统安装后操作与课程安排
&lt;/h2>&lt;h3 id="一安装后必做操作">（一）安装后必做操作
&lt;/h3>&lt;ol>
&lt;li>系统安装完成重启后，需关机并拍摄快照，便于后续系统故障时恢复；删除虚拟机需在管理界面操作，不可直接修改安装文件。&lt;/li>
&lt;li>若安装过程中遇到密码长度过短提示，按两次确认即可完成设置；图形化安装重启后需完成许可协议接受、时区设置等步骤。&lt;/li>
&lt;/ol>
&lt;h3 id="二课程进度安排">（二）课程进度安排
&lt;/h3>&lt;ol>
&lt;li>首节课讲解课程基础、Linux理论与虚拟机使用建议，下节课开始虚拟机安装教学。&lt;/li>
&lt;li>Rocky Linux 9.4安装课程中，上午讲解配置与安装核心流程，下午推进后续操作；因麦克风硬件问题，部分课程提前结束，预留20分钟供学员实操。&lt;/li>
&lt;li>后续需完成RHEL 9.4（2台，含图形化、字符界面）和CentOS 7.6（1台，图形化）的安装练习。&lt;/li>
&lt;/ol>
&lt;hr>
&lt;p>我可以帮你将这份整合总结梳理成&lt;strong>Linux系统学习课程的核心知识点速记表&lt;/strong>，方便你快速查阅重点，需要吗？&lt;/p></description></item><item><title>Linux 移动介质挂载、Vim 与压缩打包</title><link>https://luckycloveryh.github.io/fuyou/p/linux-15/</link><pubDate>Thu, 13 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-15/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-15/1200/675" alt="Featured image of post Linux 移动介质挂载、Vim 与压缩打包" />&lt;p>Linux视频主要内容总结（修正版）&lt;/p>
&lt;h2 id="一linux挂载相关操作与知识">一、Linux挂载相关操作与知识
&lt;/h2>&lt;h3 id="一挂载信息查看与特殊选项">（一）挂载信息查看与特殊选项
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>讲解了挂载信息的查看操作，提及远程连接命令与默认设置，说明这类默认设置通常不变，求职时一般不会被问及，深入了解可查看mount帮助信息（man mount）或相关书籍。（注：原文“美甲”为笔误，推测为“默认”；“mod”应为mount）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>介绍七个特殊选项含义，包含rw（读写）、suid（用户级SetUID权限）、sgid（组级SetGID权限）、dev（允许访问挂载设备的特殊文件）、exec（允许执行挂载目录下的可执行文件）、是否自动挂载stable内容（稳定挂载选项）、是否允许普通用户挂载、async（异步写入，数据先存内存再同步到磁盘，默认设置），还提到部分选项是此前课程见过的缩写或例外情况，相关练习已完成，本次不再重复。（注：修正术语错误：“suyd”→suid，“dv”→dev，“psec”→exec，“s table”→stable，“asc”→async）&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="二移动硬盘u盘挂载">（二）移动硬盘/U盘挂载
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>接口与虚拟机设置：USB接口分2.0、3.0、3.1、3.2等类型，虚拟机默认可能不识别3.0及以上接口设备，需在虚拟机右键设置中将“USB控制器”改为3.1（建议先关机再修改），否则物理机能识别但虚拟机无法识别。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>设备连接选择：U盘插入物理机后，VM（虚拟机软件如VMware）会检测到新USB设备，提供“连接到主机（Windows）”或“连接到虚拟机”选项，重启虚拟机后仍可通过虚拟机右下角USB设备图标重新选择连接目标。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>文件系统识别与挂载结果可在Windows中通过属性查看U盘文件系统类型（如exFAT、fat32、fat16、ntfs等），Linux对不同文件系统识别情况不同，不识别时虽能在设备监控（如lsblk）中发现U盘，但无法挂载。（注：“espad”为笔误，修正为exFAT）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>以exFAT文件系统U盘为例，连接虚拟机后，可通过fdisk -l、lsblk、lsblk -f等命令查询设备信息，fdisk -l能查看磁盘标签类型与分区情况，lsblk -f可明确文件系统类型。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>挂载需选正确分区（如/dev/sdb1，注：sda1通常为系统分区，外接设备多为sdb及以后编号），执行挂载命令（如mount /dev/sdb1 /mnt/usb）后可通过挂载点（/mnt/usb）访问、读写数据，比如将history命令输出结果重定向保存到U盘（history &amp;gt; /mnt/usb/history.log）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>卸载与断开连接：使用umount命令卸载，可指定挂载点（umount /mnt/usb）或设备文件名称（umount /dev/sdb1），卸载后才能断开U盘与虚拟机的连接（点击虚拟机右下角USB设备图标选择“断开连接”），在物理机查看U盘数据。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>不同文件系统的处理exFAT文件系统：在Linux 7.6等版本中默认不识别，需安装exfat-utils和fuse-exfat包（EPEL源提供，命令：yum install -y exfat-utils fuse-exfat），依赖相对较少，Linux 9版本默认支持，建议在9版本中操作。（注：“espat”为笔误，修正为exFAT）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>ntfs文件系统：Linux默认不识别，需安装ntfs-3g包（可通过EPEL源安装：yum install -y ntfs-3g，无需手动下载传输），安装后可正常挂载读写。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>fat32/fat16文件系统：在新版本Linux中通常可直接挂载，无需额外操作。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="三光盘与iso文件挂载">（三）光盘与ISO文件挂载
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>光盘挂载：虚拟机中的光盘设备（/dev/sr0）实际映射Windows中的ISO镜像文件，将ISO文件加载到虚拟机的“虚拟光驱”后，虚拟机开机即可识别为真实光盘。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>ISO文件挂载ISO文件在Windows中是镜像文件，传到Linux后为普通文件，Linux不通过文件后缀区分文件类型，需通过file命令（file xxx.iso）识别。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>ISO文件虽非块设备，但可使用mount -o loop xxx.iso /mnt/iso命令挂载（-o loop模拟块设备），挂载后能查看其中与光盘一致的内容（如系统安装文件）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>卸载同样使用umount命令，指定挂载点（umount /mnt/iso）或ISO文件路径（umount xxx.iso）即可。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="四挂载注意事项">（四）挂载注意事项
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>同一文件系统无需重复挂载到多个位置，仅需一个读写入口即可实现所有操作。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>同一挂载点不能挂载多个块设备，否则后挂载的设备会覆盖前一个设备的内容（仅显示后挂载设备的数据）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>挂载点必须是空目录，非空目录挂载后原文件会被临时覆盖（文件未删除，仅隐藏），卸载后恢复可见，建议自行创建空目录（如mkdir /mnt/usb）作为挂载点。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h2 id="二vim编辑器相关教学">二、VIM编辑器相关教学
&lt;/h2>&lt;h3 id="一vim与vi的核心差异">（一）VIM与VI的核心差异
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>功能与显示：VIM（VI Improved）功能更丰富，支持语法高亮（打开文件有字体颜色区分）、代码补全、语法错误提示等；VI功能简化，无语法高亮功能，若系统中vi是vim的软链接（ls -l /usr/bin/vi可查看），则打开文件会继承VIM的语法高亮效果。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>安装与使用场景：最小化系统默认未安装VI/VIM，安装VIM可通过命令（yum install -y vim），依赖较少；基础文本编辑可使用VI，复杂操作（如代码编写）建议用VIM。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>字体调整：图形化本地登录时可通过Ctrl + 加号/减号调整终端字体大小，远程连接（如Xshell、SecureCRT）时需在终端软件的“外观设置”中调整字体。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="二vim的工作模式及切换">（二）VIM的工作模式及切换
&lt;/h3>&lt;p>VIM有命令模式、插入模式、末行模式三种核心模式，默认打开文件进入命令模式：&lt;/p>
&lt;ol>
&lt;li>
&lt;p>命令模式：可移动光标、执行复制粘贴/删除/撤销等操作，输入字符仅作为操作指令，不写入文件。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>插入模式：在命令模式下按i（光标前插入）/I（行首插入）/a（光标后插入）/A（行尾插入）/o（下一行插入）/O（上一行插入）可进入，输入内容直接写入文件，按ESC键返回命令模式。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>末行模式：在命令模式下按:（Shift + 分号）进入，可执行保存、退出、替换等操作（如:wq保存退出），完成后自动退出VIM或返回命令模式。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="三vim基础操作">（三）VIM基础操作
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>光标移动：基础移动用上下左右键或H（左）/J（下）/K（上）/L（右）；翻页用PGUP（上一页）/PGDN（下一页）；行内跳转用^（Shift + 6，行首）/$（Shift + 4，行尾）；行间跳转用“数字 + G”（如5G跳转到第5行）、G（末行）、gg（首行）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>文件内容修改：删除（dd删除当前行，3dd删除3行，x删除光标所在字符）；复制粘贴（yy复制当前行，3yy复制3行，p粘贴到光标后，P粘贴到光标前）；替换（r替换单个字符，R进入替换模式）；撤销（u撤销上一步操作，Ctrl + r反撤销）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>保存与退出进阶：末行模式输入:w单独保存，:w /path/newfile另存为，:q退出（未修改时），:q!强制退出（丢弃修改），:wq保存退出；命令模式按ZZ（Shift + zz）可快捷保存退出。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="四vim文件操作原理">（四）VIM文件操作原理
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>VIM打开文件时会将硬盘文件复制到内存（创建缓冲区），用户修改的是内存中的副本，执行:w命令后，修改内容才会同步写入硬盘原文件。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>VIM运行时为交互式进程（可通过ps aux | grep vim查看），仅完成保存退出（:wq）或强制退出（:q!）后，进程才终止。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="五vim进阶替换功能">（五）VIM进阶替换功能
&lt;/h3>&lt;p>在末行模式下通过:s（substitute）命令实现替换，格式与效果如下：&lt;/p>
&lt;ul>
&lt;li>
&lt;p>基础替换：:s/旧内容/新内容（替换当前行第一个匹配项）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>行内全替换：:s/旧内容/新内容/g（g即global，当前行所有匹配项）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>指定行范围替换：:3,5s/旧内容/新内容/g（替换第3-5行所有匹配项）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>全文件替换：:%s/旧内容/新内容/g（%代表整个文件）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>询问式替换：:%s/旧内容/新内容/gc（c即confirm，每处匹配均询问是否替换）&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2 id="三打包与压缩相关知识">三、打包与压缩相关知识
&lt;/h2>&lt;h3 id="一基础概念">（一）基础概念
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>打包：将多个分散的文件/目录合并为一个打包文件（如.tar），方便传输与管理，解包后还原为原始文件结构，文件总体大小无明显变化。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>压缩：通过压缩算法（如gzip、bzip2）优化二进制数据存储，减少文件体积，重复数据越多压缩效果越明显，不同压缩算法的压缩率与速度不同。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="二linux系统中的打包压缩命令tar命令为主">（二）Linux系统中的打包压缩命令（tar命令为主）
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>tar命令核心用法基础选项：-c（创建打包文件）、-x（解包/解压缩）、-v（显示操作过程）、-f（指定目标文件，必须放在选项最后）、-t（查看压缩包内文件列表）。常见组合：tar -cvf file.tar /path（打包）、tar -xvf file.tar（解包）、tar -tvf file.tar（查看内容）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>排除文件：使用&amp;ndash;exclude选项跳过指定文件，格式为tar -cvf file.tar &amp;ndash;exclude=file1 &amp;ndash;exclude=dir2 /path，一个&amp;ndash;exclude对应一个排除对象。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>tar结合不同压缩方式：支持三种主流压缩方式，命令格式与后缀对应如下：
gzip压缩：tar -zcvf file.tar.gz /path（压缩）、tar -zxvf file.tar.gz（解压缩），后缀为.tar.gz或.tgz，压缩速度快，压缩率中等。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>bzip2压缩：需先安装bzip2工具（yum install -y bzip2），命令：tar -jcvf file.tar.bz2 /path（压缩）、tar -jxvf file.tar.bz2（解压缩），后缀为.tar.bz2，压缩率高于gzip，速度稍慢。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>xz压缩：tar -Jcvf file.tar.xz /path（压缩）、tar -Jxvf file.tar.xz（解压缩），后缀为.tar.xz，压缩率最高，耗时相对较长。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h3 id="三其他压缩工具zip">（三）其他压缩工具（ZIP）
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>安装：需先安装unzip再安装zip（避免依赖检测失败），命令：yum install -y unzip zip。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>操作命令：压缩文件zip file.zip file1 file2；压缩目录需加-r（递归）选项：zip -r dir.zip /path/dir；解压缩：unzip file.zip，指定解压路径用-d：unzip file.zip -d /mnt。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;font color="#ff0000">与tar对比：zip压缩软链接时会将链接指向的原文件一并压缩（tar默认仅打包链接本身）；相同目录压缩后，zip文件体积通常大于tar结合xz/gzip的压缩包。&lt;/font>&lt;/p>
&lt;/li>
&lt;/ol>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>特性&lt;/th>
&lt;th>zip 压缩工具&lt;/th>
&lt;th>tar + 压缩算法（xz/gzip/bzip2）&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>默认处理逻辑&lt;/strong>&lt;/td>
&lt;td>不打包软链接本身，而是跟随软链接，将其指向的&lt;strong>原文件/目录完整压缩&lt;/strong>（相当于“解引用”后打包）。&lt;br>例：若 &lt;code>link.txt -&amp;gt; /home/user/file.txt&lt;/code>，zip 会压缩 &lt;code>/home/user/file.txt&lt;/code> 而非 &lt;code>link.txt&lt;/code> 链接文件。&lt;/td>
&lt;td>仅打包软链接本身（保留链接属性：目标路径、权限、创建时间等），不跟随指向原文件，&lt;strong>原文件不会被包含&lt;/strong>。&lt;br>例：打包后仅保留 &lt;code>link.txt&lt;/code> 作为链接文件（大小仅几字节），解压后仍为指向原路径的软链接（需原文件存在才有效）。&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>是否可调整行为&lt;/strong>&lt;/td>
&lt;td>支持通过参数修改：&lt;br>&lt;code>zip -y 压缩包.zip 软链接&lt;/code> → 强制打包软链接本身（不跟随原文件），与 tar 默认行为一致。&lt;/td>
&lt;td>支持通过参数跟随链接：&lt;br>&lt;code>tar -h -zcvf 压缩包.tar.gz 软链接&lt;/code> → &lt;code>-h&lt;/code>（&amp;ndash;dereference）参数表示解引用，打包原文件而非链接（与 zip 默认行为一致）。&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>适用场景&lt;/strong>&lt;/td>
&lt;td>适合需要“完整备份软链接指向内容”的场景（如迁移文件时需包含依赖数据），但可能导致压缩包包含冗余文件。&lt;/td>
&lt;td>适合仅需备份链接关系（无需原文件）或避免重复打包的场景（如原文件已在其他位置备份），压缩包更精简。&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3 id="四辅助命令与工具">（四）辅助命令与工具
&lt;/h3>&lt;ol>
&lt;li>
&lt;p>du命令：查看目录/文件大小，du -h /etc（-h人性化显示，以KB/MB/GB为单位）显示/etc下各文件/目录大小；du -sh /etc（-s仅显示总大小）直接输出/etc目录递归总大小。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>file命令：判断文件类型，可识别无规范后缀的压缩文件，如file file.tar.gz会显示“gzip compressed data”，辅助确定正确的解压缩命令。&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h2 id="四学习建议与课程预告">四、学习建议与课程预告
&lt;/h2>&lt;ol>
&lt;li>
&lt;p>学习建议执行tar命令时“删除开头的根”（如解压时提示“Removing leading `/&amp;rsquo; from member names”）并非报错，是tar的安全机制，避免解压缩覆盖系统根目录下的同名文件；-f选项后必须紧跟目标文件，其他选项顺序可任意调整。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>安装工具时若提示依赖缺失（如“requires xxx”），需先通过yum install -y xxx安装依赖包，再安装目标工具；高强度学习中要合理分配休息时间，命令用法需通过实际操作验证（如多练习不同文件系统的挂载、不同压缩方式的打包）。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>修改/etc/passwd、/etc/fstab等系统配置文件时，建议先备份（如cp /etc/passwd /etc/passwd.bak）或修改副本，避免错误操作导致系统运行异常。&lt;/p>
&lt;/li>
&lt;li>
&lt;p>课程预告挂载内容讲解完毕后，下节课讲解打包压缩相关知识。（注：此处与文档结构重复，推测为视频原始顺序，保留原文）&lt;/p>
&lt;/li>
&lt;li>
&lt;p>VIM编辑器课程结尾提及次日将讲解“微博式”Linux相关工具或功能（具体名称待后续课程明确）。&lt;/p>
&lt;/li>
&lt;/ol></description></item><item><title>Linux 系统查询、运行级别与挂载管理</title><link>https://luckycloveryh.github.io/fuyou/p/linux-14/</link><pubDate>Wed, 12 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-14/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-14/1200/675" alt="Featured image of post Linux 系统查询、运行级别与挂载管理" />&lt;p>本次视频围绕&lt;strong>Linux系统操作与管理&lt;/strong>展开，从基础命令工具使用、系统运行管控，到文件存储与磁盘管理层层深入，核心内容整合为&lt;strong>命令工具与系统查询&lt;/strong>、&lt;strong>系统运行与操作管理&lt;/strong>、&lt;strong>文件系统与存储管理&lt;/strong>三大板块，具体如下：&lt;/p>
&lt;h2 id="一命令工具与系统查询">一、命令工具与系统查询
&lt;/h2>&lt;h3 id="一命令帮助相关">（一）命令帮助相关
&lt;/h3>&lt;ol>
&lt;li>内部命令是shell内嵌的命令（如&lt;code>cd&lt;/code>），可通过&lt;code>type&lt;/code>命令查看某个命令是否为内部命令。&lt;/li>
&lt;li>帮助类命令/选项各有特点：&lt;code>--help&lt;/code>能列出命令常见长选项与格式，&lt;code>man&lt;/code>提供基础帮助信息，&lt;code>info&lt;/code>讲解更详细（含选项细节与组合案例），但最小化系统默认未安装，需挂载光盘（&lt;code>/dev/sr0&lt;/code>挂载到&lt;code>/mnt/media&lt;/code>）并通过&lt;code>rpm -ivh&lt;/code>安装，安装后按空格翻页、&lt;code>q&lt;/code>退出查看。&lt;/li>
&lt;/ol>
&lt;h3 id="二网络相关命令与操作">（二）网络相关命令与操作
&lt;/h3>&lt;ol>
&lt;li>&lt;code>ping&lt;/code>用于测试网络连通性，Linux下默认持续发包，可通过&lt;code>-c&lt;/code>指定次数（如&lt;code>ping -c 4 127.0.0.1&lt;/code>），执行后会显示发包数、丢包率等信息。&lt;/li>
&lt;li>&lt;code>ip addr&lt;/code>（可简写为&lt;code>ip a&lt;/code>/&lt;code>ip ad&lt;/code>）显示网卡信息，不同Linux版本网卡命名规则不同：7版本前按&lt;code>eth0&lt;/code>/&lt;code>eth1&lt;/code>命名，7-9版本命名更随机（如&lt;code>ens33&lt;/code>）；&lt;code>ifconfig&lt;/code>功能与其部分重复，需安装&lt;code>net-tools&lt;/code>包后使用，还能统计数据收发错误。&lt;/li>
&lt;li>网卡启停可通过&lt;code>nmcli connection up/down 网卡名&lt;/code>指定操作，或&lt;code>networking on/off&lt;/code>启停所有网卡，远程操作时停止网卡会导致连接断开。&lt;/li>
&lt;/ol>
&lt;h3 id="三用户登录与系统状态查询">（三）用户登录与系统状态查询
&lt;/h3>&lt;ol>
&lt;li>&lt;code>w&lt;/code>命令可查询当前登录用户及操作状态，第一行显示系统时间、运行时长、平均负载等，用户信息行包含用户名、终端类型、闲置时间、正在执行的操作等；终端分本地&lt;code>tty&lt;/code>（通过&lt;code>Ctrl+Alt+F2&lt;/code>-&lt;code>F6&lt;/code>切换）和虚拟&lt;code>pts&lt;/code>（远程连接使用）。&lt;/li>
&lt;li>&lt;code>last&lt;/code>命令列出用户登录详细记录（含登录时间范围），还会记录系统启动信息；&lt;code>lastlog&lt;/code>显示所有账号最后一次登录事件，系统用户分为权限最高的&lt;code>root&lt;/code>用户、普通用户、无法登录的系统/服务用户（如IIS、DHCP对应的用户）。&lt;/li>
&lt;li>&lt;code>w&lt;/code>命令与“户”命令均可显示登录用户名称、终端及登录时间，后者还能显示远程用户IP地址。&lt;/li>
&lt;/ol>
&lt;h3 id="四网络连接与端口查看命令">（四）网络连接与端口查看命令
&lt;/h3>&lt;ol>
&lt;li>&lt;code>ss&lt;/code>命令查看IP和端口信息（监听状态、对应进程等），常用选项有&lt;code>-a&lt;/code>（显示所有端口）、&lt;code>-n&lt;/code>（显示端口号）、&lt;code>-p&lt;/code>（显示进程）等，效率高于&lt;code>netstat&lt;/code>且默认安装，可用于检查服务端口监听状态。&lt;/li>
&lt;li>&lt;code>netstat&lt;/code>功能与&lt;code>ss&lt;/code>相近，常用选项组合如&lt;code>netstat -antp&lt;/code>，但效率较低且默认不预装，需手动安装相关包。&lt;/li>
&lt;/ol>
&lt;h2 id="二系统运行与操作管理">二、系统运行与操作管理
&lt;/h2>&lt;h3 id="一广播命令wall">（一）广播命令（wall）
&lt;/h3>&lt;p>&lt;code>wall&lt;/code>是交互式广播命令，输入命令后撰写内容，按&lt;code>ctrl+d&lt;/code>完成输入，所有在线用户均可收到信息；适用于多用户服务器环境，操作（关机、重启）前广播可避免用户数据丢失。&lt;/p>
&lt;h3 id="二系统运行级别与关机重启命令">（二）系统运行级别与关机、重启命令
&lt;/h3>&lt;ol>
&lt;li>系统运行级别0-6中，常用的有0（关机）、3（字符界面）、5（图形界面）、6（重启）；可通过&lt;code>runlevel&lt;/code>查看级别，&lt;code>init + 数字&lt;/code>临时切换（重启后恢复原级别）。&lt;/li>
&lt;li>关机命令包括&lt;code>poweroff&lt;/code>（直接关机）、&lt;code>init 0&lt;/code>（通过级别关机）、&lt;code>shutdown&lt;/code>（可指定时间并广播提示，&lt;code>shutdown -c&lt;/code>取消关机）；重启命令有&lt;code>reboot&lt;/code>（直接重启）、&lt;code>init 6&lt;/code>（通过级别重启）、&lt;code>shutdown -r&lt;/code>（指定时间重启并广播）。&lt;/li>
&lt;/ol>
&lt;h2 id="三文件系统与存储管理">三、文件系统与存储管理
&lt;/h2>&lt;h3 id="一sync数据同步命令与文件读写机制">（一）sync数据同步命令与文件读写机制
&lt;/h3>&lt;ol>
&lt;li>系统为提升效率，会将文件修改暂存至内存缓冲区，满足条件后再一次性写入硬盘；非正常关机可能导致缓冲区数据丢失，&lt;code>sync&lt;/code>命令可强制将缓冲区数据写入硬盘，仅紧急情况需手动执行。&lt;/li>
&lt;/ol>
&lt;h3 id="二挂载相关知识">（二）挂载相关知识
&lt;/h3>&lt;ol>
&lt;li>仅带有文件系统的块设备分区可挂载，新硬盘需先分区再格式化；块设备按“主设备号+次设备号”命名（如光盘&lt;code>/dev/sr0&lt;/code>、SATA硬盘分区&lt;code>/dev/sda1&lt;/code>）。&lt;/li>
&lt;li>分区表分MBR和GPT：MBR最多支持4个主分区，逻辑分区编号从5开始；GPT无分区类型限制，编号按顺序排列。&lt;/li>
&lt;li>挂载需指定挂载点，可通过&lt;code>mount&lt;/code>命令查询已挂载设备，挂载时可用&lt;code>-mt&lt;/code>指定文件系统类型、&lt;code>-o&lt;/code>指定特殊选项。&lt;/li>
&lt;/ol>
&lt;h3 id="三mount命令特殊选项">（三）mount命令特殊选项
&lt;/h3>&lt;ol>
&lt;li>常见选项及作用：&lt;code>atime/noatime&lt;/code>控制文件访问时间更新，&lt;code>sync/async&lt;/code>决定数据写入方式，&lt;code>defaults&lt;/code>为组合选项（等效于rw、suid等），&lt;code>rw/ro&lt;/code>设置分区读写权限，&lt;code>exec/noexec&lt;/code>控制文件执行权限。&lt;/li>
&lt;li>实用场景：&lt;code>ro&lt;/code>（只读）用于系统修复或数据恢复，&lt;code>remount&lt;/code>可修改已挂载分区的选项（如&lt;code>mount -o remount,ro /boot&lt;/code>）。&lt;/li>
&lt;/ol>
&lt;h3 id="四永久挂载配置文件etcfstab">（四）永久挂载配置文件（/etc/fstab）
&lt;/h3>&lt;ol>
&lt;li>该文件中的挂载配置开机自动执行，实现永久挂载；配置错误会导致系统启动失败，修改后需用&lt;code>mount -a&lt;/code>验证（无报错则配置正确）。&lt;/li>
&lt;li>&lt;code>user/nouser&lt;/code>选项控制普通用户挂载权限，默认&lt;code>nouser&lt;/code>仅允许&lt;code>root&lt;/code>挂载。&lt;/li>
&lt;/ol>
&lt;h3 id="五磁盘配额disk-quota">（五）磁盘配额（Disk Quota）
&lt;/h3>&lt;ol>
&lt;li>核心目的是在多用户场景下，限制用户/组对分区资源的使用，避免单个用户耗尽资源。&lt;/li>
&lt;li>限制维度包括空间（如单用户最多用10GB）和inode（如最多创建100个文件），主要适用于Linux服务器，Windows系统必要性较低。&lt;/li>
&lt;/ol>
&lt;h3 id="六后续实践预告">（六）后续实践预告
&lt;/h3>&lt;p>次日将使用U盘练习挂载操作（可与同桌共用），U盘在服务器场景更贴合实际（服务器多无光驱，常通过U盘读写数据）。&lt;/p>
&lt;hr>
&lt;p>是否需要我为你整理视频中涉及的&lt;strong>Linux命令分类速记表&lt;/strong>，按功能模块梳理各命令的用法和关键选项？&lt;/p></description></item><item><title>Linux 文件操作与权限管理进阶</title><link>https://luckycloveryh.github.io/fuyou/p/linux-13/</link><pubDate>Tue, 11 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-13/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-13/1200/675" alt="Featured image of post Linux 文件操作与权限管理进阶" />&lt;h1 id="linux系统文件操作与权限管理核心知识">Linux系统文件操作与权限管理核心知识
&lt;/h1>&lt;p>本章围绕Linux系统的实操核心展开，整合了文件类型识别、权限配置、文件查找命令、命令交互逻辑、辅助工具使用、帮助命令查询、umask值与默认权限设置及操作生效范围等关键内容，系统讲解Linux文件操作与权限管理的底层逻辑和实操方法。&lt;/p>
&lt;h2 id="一文件类型识别">一、文件类型识别
&lt;/h2>&lt;p>Linux系统中的文件可通过属性标识区分类型，除此前学习的普通文件（-）、目录（d）、块设备文件（b）、软链接（l）外，还有三类重要文件类型需掌握：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>字符设备文件（c）&lt;/strong>：系统将鼠标、键盘等外设识别为此类文件，无需挂载即可直接使用，实操中只需能识别该类型标识即可。&lt;/li>
&lt;li>&lt;strong>套接字文件（s）&lt;/strong>：存储IP与端口的组合信息，是服务器与服务器、服务器与客户端或不同主机间建立通信连接的基础，类似远程连接依赖IP和端口（22、80等默认端口可省略）的逻辑。&lt;/li>
&lt;li>&lt;strong>管道文件（p）&lt;/strong>：用于实现系统中两个隔离进程的信息交互，是管道符（|）能将一个命令（进程）的执行结果传递给另一个命令（进程）处理的底层支撑，在文件查找等实操场景中会频繁应用。&lt;/li>
&lt;/ol>
&lt;h2 id="二文件权限管理">二、文件权限管理
&lt;/h2>&lt;p>文件权限是Linux系统安全管理的核心，围绕所有者、所属组、其他人三类身份划分权限，支持灵活的修改与配置：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>权限基本结构&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>身份与权限对应&lt;/strong>：文件权限信息的后九位字符为权限标识，每三位为一组，依次对应所有者（文件创建者）、所属组（创建者所在用户组）、其他人（非所有者且不在所属组的用户），权限字符包括r（读，数字4）、w（写，数字2）、x（执行，数字1）。&lt;/li>
&lt;li>&lt;strong>权限的表示方式&lt;/strong>：可通过字符（如rwxr-xr&amp;ndash;）或数字（如755，即所有者rwx=7、所属组r-x=5、其他人r&amp;ndash;=4）表示，不同数字组合可精准区分权限配置。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>权限修改方式&lt;/strong>
&lt;ul>
&lt;li>&lt;strong>符号修改法&lt;/strong>：通过u（所有者）、g（所属组）、o（其他人）指定身份，结合+（添加权限）、-（移除权限）、=（设置权限）操作，如&lt;code>u+x&lt;/code>为所有者添加执行权限，&lt;code>g=wx&lt;/code>为所属组设置写和执行权限，该方式可避免意外取消已有权限。&lt;/li>
&lt;li>&lt;strong>数字修改法&lt;/strong>：直接通过三位数字批量设置权限，如&lt;code>chmod 644 文件名&lt;/code>，操作高效但可能误删权限，需根据实际场景选择。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>所有者与所属组修改&lt;/strong>：文件默认所有者为创建者，所属组为创建者所在的用户组，可通过&lt;code>chown&lt;/code>命令修改，格式为&lt;code>chown 新所有者:新所属组 文件名&lt;/code>，也可单独修改所有者（&lt;code>chown 新所有者 文件名&lt;/code>）或所属组，修改的用户需是系统中已存在的用户，创建用户时会自动生成同名的用户组。&lt;/li>
&lt;/ol>
&lt;h2 id="三文件查找核心命令">三、文件查找核心命令
&lt;/h2>&lt;p>Linux提供&lt;code>locate&lt;/code>和&lt;code>find&lt;/code>两大文件查找命令，分别适配快速模糊查找与精准多条件查找的需求：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>locate命令&lt;/strong>：基于系统预先建立的文件数据库实现关键字模糊查找，不仅匹配文件名，路径含关键字的结果也会被列出，查找速度极快。但数据库默认24小时更新，新文件需通过&lt;code>updatedb&lt;/code>命令手动更新数据库才能被查到；系统默认未安装该命令，需先挂载光盘（文件系统类型为iso9660，RHEL 7及以上版本可省略），再安装&lt;code>mlocate&lt;/code>包（该包同时提供&lt;code>locate&lt;/code>和&lt;code>updatedb&lt;/code>命令），安装后可通过&lt;code>which locate&lt;/code>验证安装结果。&lt;/li>
&lt;li>&lt;strong>find命令&lt;/strong>：功能灵活的多条件精确查找工具，语法为&lt;code>find [查找范围] [查找条件] [后续操作]&lt;/code>，默认精确匹配文件名，可通过通配符实现模糊匹配，支持的查找条件包括：
&lt;ul>
&lt;li>按名称（&lt;code>-name&lt;/code>）、大小（&lt;code>-size&lt;/code>，结合+/-指定范围，单位常用k）、所有者/所属组（&lt;code>-user&lt;/code>/&lt;code>-group&lt;/code>）、权限（&lt;code>-perm&lt;/code>）、时间（&lt;code>mtime&lt;/code>/&lt;code>atime&lt;/code>/&lt;code>ctime&lt;/code>，分别对应内容修改、访问、属性修改时间）、文件类型（&lt;code>-type&lt;/code>，如f为普通文件、d为目录、l为软链接）、inode号（&lt;code>-inum&lt;/code>，用于查找硬链接文件）；&lt;/li>
&lt;li>结合&lt;code>-a&lt;/code>（逻辑与）、&lt;code>-o&lt;/code>（逻辑或）组合多个查找条件；&lt;/li>
&lt;li>通过&lt;code>-exec&lt;/code>选项对查找结果执行后续操作，格式为&lt;code>find [范围] [条件] -exec [命令] {} \;&lt;/code>，其中&lt;code>{}&lt;/code>为结果占位符，&lt;code>\;&lt;/code>为命令结束标识，可实现查看（&lt;code>ls -lh&lt;/code>）、复制（&lt;code>cp&lt;/code>）、删除（&lt;code>rm -rf&lt;/code>）等操作。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="四命令交互与辅助工具">四、命令交互与辅助工具
&lt;/h2>&lt;p>掌握命令的交互模式与辅助工具，能大幅提升Linux操作的效率与灵活性：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>命令交互模式&lt;/strong>：分为交互式与非交互式，交互式命令（如&lt;code>less&lt;/code>、&lt;code>rm&lt;/code>）执行后需人工介入才能继续，非交互式命令（如&lt;code>ls&lt;/code>、&lt;code>rm -f&lt;/code>）可自动完成操作，非交互式命令是shell脚本实现自动化批量操作的关键。&lt;/li>
&lt;li>&lt;strong>管道符与xargs工具&lt;/strong>
&lt;ul>
&lt;li>管道符（|）：将前一个命令的标准输出作为后一个命令的标准输入，简化多步骤操作，如&lt;code>ls /etc | grep LD&lt;/code>可筛选出&lt;code>/etc&lt;/code>目录下名称含LD的文件/目录，但需注意并非所有命令都能通过管道符组合，需满足“前一命令有输出且后一命令可处理该输出”的条件。&lt;/li>
&lt;li>xargs工具：解决管道符对批量结果的“整体传递”问题，可将&lt;code>find&lt;/code>等命令的多个输出结果拆分为个体，让后续命令逐个处理，如&lt;code>find /root -name &amp;quot;?aaa&amp;quot; | xargs ls -lhi&lt;/code>，可逐个显示符合条件文件的详细信息；与&lt;code>find -exec&lt;/code>相比，&lt;code>xargs&lt;/code>在拆分批量结果时更便捷，而&lt;code>find -exec&lt;/code>支持直接执行复制、移动等操作。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>which与alias工具&lt;/strong>
&lt;ul>
&lt;li>which命令：用于查找系统命令的保存路径及别名，如&lt;code>which mkdir&lt;/code>可查看其在&lt;code>/usr/bin&lt;/code>下的位置；在写脚本、定时任务等场景中，建议使用命令的绝对路径执行，避免因目录查找失败导致命令无法运行。&lt;/li>
&lt;li>alias命令：可创建命令别名，一是简化带常用选项的命令（如&lt;code>alias lld=&amp;quot;ls -ldih --color&amp;quot;&lt;/code>），二是添加安全选项防止误操作（如&lt;code>rm&lt;/code>别名&lt;code>rm -i&lt;/code>，删除前提示确认）；执行&lt;code>alias&lt;/code>可查看当前终端的所有别名，设置的别名默认仅对当前终端生效，写入配置文件可实现永久生效。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="五帮助命令与内外命令区分">五、帮助命令与内外命令区分
&lt;/h2>&lt;p>了解帮助命令的使用方法和内外命令的差异，是自主学习Linux命令的基础：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>帮助命令使用&lt;/strong>
&lt;ul>
&lt;li>&lt;code>man 命令&lt;/code>：用于查看外部命令的官方帮助信息，包括命令功能、格式、选项含义等，是最准确的查询方式；&lt;code>man -f 命令&lt;/code>可查看命令对应的帮助级别（共9级，日常仅需关注1级命令帮助），若执行时出现“无缓存”错误，可通过&lt;code>mandb&lt;/code>命令生成帮助缓存。&lt;/li>
&lt;li>&lt;code>help 命令&lt;/code>：仅能查询内部命令的帮助信息，无法用于外部命令。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>内外命令差异&lt;/strong>
&lt;ul>
&lt;li>内部命令：内置在&lt;code>bash&lt;/code>（shell命令解释器）中，执行时直接从内存调用，速度更快，如&lt;code>cd&lt;/code>、&lt;code>pwd&lt;/code>，可通过&lt;code>type 命令&lt;/code>判断（显示“shell内置”）。&lt;/li>
&lt;li>外部命令：是独立存在于&lt;code>/bin&lt;/code>、&lt;code>/usr/bin&lt;/code>等目录的可执行文件，执行时需先从硬盘读取到内存，如&lt;code>ls&lt;/code>、&lt;code>mkdir&lt;/code>，&lt;code>type 命令&lt;/code>会显示其所在路径；系统通过&lt;code>hash&lt;/code>表缓存频繁执行的外部命令路径，加速后续查找。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;h2 id="六umask值与默认权限">六、umask值与默认权限
&lt;/h2>&lt;p>umask值决定了用户创建文件和目录的默认权限，是Linux权限管理的重要基础：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>umask值作用&lt;/strong>：其值为四位数字，实操中重点关注后三位，分别对应所有者、所属组、其他人“不具备的权限”，第一位暂不生效无需关注；可通过&lt;code>umask&lt;/code>命令查看当前值，也可直接设置（如&lt;code>umask 002&lt;/code>）。&lt;/li>
&lt;li>&lt;strong>默认权限计算&lt;/strong>
&lt;ul>
&lt;li>目录最大权限为777，用777减去umask后三位（按身份对应相减，无借位），如umask为022时，目录默认权限为755（7-0=7、7-2=5、7-2=5）。&lt;/li>
&lt;li>文件最大权限为666（默认无执行权限，防止恶意代码自动运行），用666减去umask后三位，如umask为022时，文件默认权限为644（6-0=6、6-2=4、6-2=4）。&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>生效范围&lt;/strong>：通过&lt;code>umask&lt;/code>命令设置的值仅对当前终端生效，新创建的文件和目录权限会随umask值变化而调整。&lt;/li>
&lt;/ol>
&lt;h2 id="七操作生效范围">七、操作生效范围
&lt;/h2>&lt;p>Linux系统中不同操作的生效范围存在差异，主要分为三类：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>永久生效&lt;/strong>：创建文件、目录、用户等操作，无论系统重启还是用户退出登录，结果均会保留。&lt;/li>
&lt;li>&lt;strong>临时生效&lt;/strong>：&lt;code>mount&lt;/code>挂载等操作，系统重启后失效，但在重启前切换终端仍保持生效状态。&lt;/li>
&lt;li>&lt;strong>仅当前终端生效&lt;/strong>：umask值设置、别名创建等操作，开启新终端后原设置不再生效，需写入配置文件才能永久生效。&lt;/li>
&lt;/ol>
&lt;hr>
&lt;p>我可以帮你将这章内容整理成&lt;strong>Linux系统操作核心考点清单&lt;/strong>，突出重点和常考内容，需要吗？&lt;/p></description></item><item><title>Linux 文件链接、复制与删除机制</title><link>https://luckycloveryh.github.io/fuyou/p/linux-12/</link><pubDate>Mon, 10 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-12/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-12/1200/675" alt="Featured image of post Linux 文件链接、复制与删除机制" />&lt;p>history #查看历史命令。
mount -t iso9660 /dev/sr0 /media/ #mount用于挂载-t指定光盘文件系统类型，/dev/sr0表示块设备文件，/media/表示挂载点。
rpm -ivh /media/BaseOS/Packages/t/tree-1.8.0-10.el9.x86_64.rpm #rpm命令可用于rpm包安装，i表示安装，v显示详细信息，h以#表示安装进度。&lt;/p>
&lt;p>inode：文件权限、时间、所有者、所属组和文件保存占用block
block：文件数据保存位置&lt;/p>
&lt;p>创建文件：找到文件系统中可用的inode和block，进行写入数据。
复制：cp /root/a /opt/b 将现有文件block读取出来，写入到新分配的inode和block中。
剪切：跨分区/不跨分区
删除：将文件inode和block标为可用状态，等待后续覆盖式写入。&lt;/p>
&lt;p>读写：文件名称&amp;mdash;&amp;gt;inode&amp;ndash;&amp;gt;block&lt;/p>
&lt;p>文件名 inode block
X 10 3
Y 10
Z 10&lt;/p>
&lt;p>硬链接：创建文件用于记录当前现有文件的inode号
1.每创建一次硬链接，文件链接数量+1
2.硬链接文件之间每进行一次删除，文件链接数量-1，当文件链接数量为1时再次删除为永久删除。
3.互为硬链接的文件之间，修改任意一个其余均会发生变化。
4.硬链接无法跨分区创建。
5.不允许将硬链接指向目录&lt;/p>
&lt;p>软链接（符号链接）：软链接是创建文件记录指定路径。
1.创建软链接建议使用绝对路径。
2.软链接和原文件之间inode不同。
3.软链接文件类型l，权限默认最大。
4.软链接创建不会让文件链接数量+1，
5.软链接删除，原文件不受影响。原文件删除，软链接变为失效状态。
6.软链接可以跨分区创建。
7.目录可以创建软链接。&lt;/p>
&lt;hr>
&lt;p>6、 命令名称： tree（ 显示目录树）&lt;/p>
&lt;pre>&lt;code>命令格式： tree 目录
-d 只显示目录
（ 在 centos7&amp;amp;rocky9 下需要安装后才可使用）
&lt;/code>&lt;/pre>
&lt;p>6.5、 命令名称： pstree（ 查看进程树之间的关系）&lt;/p>
&lt;pre>&lt;code>格式： pstree [选项]
-A： 进程树之间的链接以 ASCII 码字符链接。-p： 同时列出每个进程的 PID。-u： 同时列出每个进程的所属账号名称。
&lt;/code>&lt;/pre>
&lt;p>7、 命令名称： cp（ 复制、 复制并改名）&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>命令格式： cp&lt;/td>
&lt;td>源文件或目录&lt;/td>
&lt;td>目标目录&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>-r&lt;/td>
&lt;td>复制目录&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>-p&lt;/td>
&lt;td>保留文件属性&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>-a&lt;/td>
&lt;td>相当于-rp&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>8、 命令名称： mv（ 剪切、 改名）&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>9、 命令名称： rm&lt;/td>
&lt;td>（ 删除）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>-r -f&lt;/td>
&lt;td>删除目录强制删除&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>10、 命令名称： rmdir（ 删除空目录）&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>-p -v&lt;/td>
&lt;td>递归删除空目录显示指令执行过程&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>11、 命令名称： cat（ 显示文件内容）&lt;/p>
&lt;pre>&lt;code>命令格式： cat 文件名
-n 列出行号
cat 显示多个文件内容
cat 将多个文件合并（ 按照文件先后顺序）
（ 如文件超过一页内容后只能显示最后一页的内容）
&lt;/code>&lt;/pre>
&lt;p>12、 命令名称： more（ 分页显示文件内容）&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>b 空格 Enter q 或 Q&lt;/td>
&lt;td>向上翻页（ 打开文件时使用）向下翻页换行退出&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>13、 命令名称： less（ 分页显示文件内容）&lt;/p>
&lt;pre>&lt;code>命令格式： less 文件名
显示文件内容的时候支持关键词查找。
&lt;/code>&lt;/pre>
&lt;p>14、 查看文件首部指定行： head&lt;/p>
&lt;pre>&lt;code>-n 数字 #查看文件指定首部 n 行。
&lt;/code>&lt;/pre>
&lt;p>15、 查看文件指定尾行： tail&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>-n 数字 -f&lt;/td>
&lt;td>#查看文件尾行。#持续显示文件变化情况。&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>16、 命令名称： ln（ 生成链接文件）&lt;/p>
&lt;pre>&lt;code>-s 创建符号链接
&lt;/code>&lt;/pre>
&lt;p>文件系统：&lt;/p>
&lt;pre>&lt;code>1.block： 文件系统中存储数据的最小单元， ext4、 xfs 等文件系统中， 创建时默认 4k，
分为存储文件数据的 data block 和存储目录数据的 directory block。
2.inode： 又称“ 索引节点” ， 每一个 inode 对应一个文件或目录， 记录了文件的大小、
所占用的 block 以及目录的 directory block 信息。
3.Inode Bitmap： 翻译成中文就是“ inode 位图” ， 用二进制的方式记录了 inode 的使
用情况。
4.Block Bitmap： 翻译成中文就是“ 块位图” ， 同 Inode Bitmap， 用二进制方式记录了
块的使用情况。&lt;/code>&lt;/pre></description></item><item><title>Linux 常用命令：目录、文件与路径基础</title><link>https://luckycloveryh.github.io/fuyou/p/linux-11/</link><pubDate>Fri, 07 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-11/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-11/1200/675" alt="Featured image of post Linux 常用命令：目录、文件与路径基础" />&lt;p>ls -l /usr/bin/
命令 [选项] [执行对象]&lt;/p>
&lt;p>用户家目录：root用户在/root/ 普通用户在/home/&lt;/p>
&lt;p>文件类型：普通文件、目录文件、块设备文件、字符设备文件
-rw&amp;mdash;&amp;mdash;-. 1 root root 1207 10月 10 2024 anaconda-ks.cfg
第一个字符表示文件类型（Linux中不以后缀名决定文件类型）
-表示普通文件
d表示目录
b表示块设备文件&lt;/p>
&lt;p>从第二个字符开始连续9位字符表示文件权限：
读 写 执行 不具备某项权限
r w x -&lt;/p>
&lt;p>文件所有者：默认创建文件的用户是文件所有者
文件所属组：默认创建文件时用户所在的组
文件其他人：既不属于所有者也不在所属组的用户&lt;/p>
&lt;p>组：拥有相同权限用户的集合。&lt;/p>
&lt;p>.表示SELinux，SELinux是Linux系统中默认存在的安全增强工具。&lt;/p>
&lt;p>链接数量：&lt;/p>
&lt;p>用户名
组名&lt;/p>
&lt;p>inode：文件权限、时间、所有者、所属组和文件保存占用block
block：文件数据保存位置&lt;/p>
&lt;p>在创建目录是默认目录中以.记录当前目录inode号以..记录上级目录inode号。
绝对路径：在路径表达中以根为起始字符的表达方式。
相对路径：在路径表达中不以根为起始字符的表达方式。&lt;/p>
&lt;p>1、 命令名称： ls（ 显示目录文件）&lt;/p>
&lt;p>命令的格式： 命令 [-选项] [参数（ 执行对象） ] -a 显示所有文件， 包括隐藏文件&lt;/p>
&lt;p>-l 详细信息显示&lt;/p>
&lt;p>-d 查看目录属性&lt;/p>
&lt;p>-h 文件大小显示为常见单位&lt;/p>
&lt;p>-i 查询文件的 inode 号&lt;/p>
&lt;p>-R 递归显示目录及其子目录&lt;/p>
&lt;p>-n 显示文件的 UID 和 GID&lt;/p>
&lt;p>2、 命令名称： touch（ 创建空文件/更新修改和访问时间）&lt;/p>
&lt;p>命令格式： touch 文件名&lt;/p>
&lt;p>Linux 文件命名规则：&lt;/p>
&lt;p>（ 1） 除了/之外， 所有字符都合法。&lt;/p>
&lt;p>（ 2） 有些字符最好不用。&lt;/p>
&lt;p>（ 3） 避免使用.作为普通文件开头。&lt;/p>
&lt;p>（ 4） Linux 严格区分大小写。&lt;/p>
&lt;p>（ 5） 长度不能大于 255 字符。&lt;/p>
&lt;p>3、 命令名称： mkdir（ 创建新目录）&lt;/p>
&lt;p>命令格式： 命令： [-选项] [参数]&lt;/p>
&lt;p>-v 显示创建过程&lt;/p>
&lt;p>-p 递归创建&lt;/p>
&lt;p>4、 命令名称： cd（ 切换目录）&lt;/p>
&lt;p>命令格式： 命令 [-选项] [参数]&lt;/p>
&lt;p>语法： cd 目录&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>cd cd cd&lt;/td>
&lt;td>~ 进入当前用户的家目录- 进入上次目录.. 返回上一级目录&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>cd ../.. 返回上两级目录&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>cd&lt;/td>
&lt;td>. 进入当前目录&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>5、 命令名称： pwd（ 显示当前所在目录）&lt;/p>
&lt;p>6、 命令名称： tree（ 显示目录树）&lt;/p>
&lt;p>命令格式： tree 目录&lt;/p>
&lt;p>-d 只显示目录&lt;/p>
&lt;p>（ 在 centos7&amp;amp;rocky9 下需要安装后才可使用）&lt;/p></description></item><item><title>Linux 基础：操作系统、硬件设备与分区概念</title><link>https://luckycloveryh.github.io/fuyou/p/linux-01/</link><pubDate>Thu, 06 Nov 2025 09:00:00 +0800</pubDate><guid>https://luckycloveryh.github.io/fuyou/p/linux-01/</guid><description>&lt;img src="https://picsum.photos/seed/fuyou-linux-01/1200/675" alt="Featured image of post Linux 基础：操作系统、硬件设备与分区概念" />&lt;hr>
&lt;p>操作系统：工具+管理控制硬件
操作系统内核：管理控制硬件
基于Linux内核的发行版&lt;/p>
&lt;hr>
&lt;p>不同的磁盘类型对应不同的设备文件名称（Linux中一切皆文件）
创建文件，当系统识别到硬件设备时系统中会出现对应设备的设备文件
硬盘、不同的类型&amp;mdash;SCSI、Nvme
SCSI：sda、sdb、sdc
Nvme：Nvme0n1、Nvme0n2、Nvme0n3
光盘：sr（设备文件名），sr0第一个光盘&lt;/p>
&lt;hr>
&lt;p>设备文件：字符设备、块设备（硬盘、U盘、光盘）
字符设备：鼠标、键盘
挂载：给设备一个访问入口
挂载点：挂载点原本是系统中存在的一个空目录，将某个块设备文件挂载至空目录后，这个空间目录作为块设备文件的访问入口&lt;/p>
&lt;hr>
&lt;p>标准分区：MBR、GPT
LVM：逻辑卷，动态扩容分区空间
分区：boot系统启动分区，内核和系统启动引导文件保存位置（512MB以上）
/boot：根下boot分区，启动分区 /：根
根目录：逻辑上的概念，逻辑上一切硬盘分区都在根目录下
根分区：某个硬盘中某个分区 swap：交换分区，虚拟内存
/分区：系统中绝大多数文件保存的位置&lt;/p>
&lt;hr>
&lt;p>root用户：系统中权限最高用户
管理员用户：介于root与普通用户之间
普通用户：系统中权限较低用户&lt;/p></description></item></channel></rss>